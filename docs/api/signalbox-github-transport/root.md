<!-- generated by scripts/render_domain_spine.py, do not edit -->

# root

## REST_BASE_URL

```rust
pub const REST_BASE_URL: &str;
```

## GRAPHQL_URL

```rust
pub const GRAPHQL_URL: &str;
```

## API_ORIGIN

```rust
pub const API_ORIGIN: &str;
```

## API_VERSION

```rust
pub const API_VERSION: &str;
```

## DEFAULT_ACCEPT

```rust
pub const DEFAULT_ACCEPT: &str;
```

## client

```rust
pub fn client(
    timeout: option::Option<time::Duration>,
) -> result::Result<client::Client, signalbox_egress_transport::ReqwestWebFetchConstructionError>;
```

## admits_api_origin

```rust
pub fn admits_api_origin(url: &url::Url) -> bool;
```

## authorization

```rust
pub fn authorization(
    credential: &[u8],
) -> result::Result<value::HeaderValue, value::InvalidHeaderValue>;
```

## authenticated_request

```rust
pub fn authenticated_request(
    client: &client::Client,
    method: method::Method,
    url: url::Url,
    authorization: value::HeaderValue,
    body: option::Option<vec::Vec<u8>>,
) -> request::RequestBuilder;
```

## StatusClass

```rust
pub enum StatusClass {
    Informational,
    Success,
    Redirect,
    ClientError,
    ServerError,
    Other,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## classify_status

```rust
pub const fn classify_status(status: u16) -> StatusClass;
```

## status_is_definitive

```rust
pub const fn status_is_definitive(status: u16) -> bool;
```

## has_next_page

```rust
pub fn has_next_page(headers: &map::HeaderMap) -> bool;
```

## ResponseExtent

```rust
pub enum ResponseExtent {
    Complete,
    Truncated,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## read_bounded

```rust
pub async fn read_bounded<S, B, E>(
    stream: S,
    limit: usize,
) -> result::Result<
    (vec::Vec<u8>, ResponseExtent),
    signalbox_egress_transport::WebFetchTransportFailure,
>
where
    S: stream::Stream<Item = result::Result<B, E>> + marker::Unpin,
    B: convert::AsRef<[u8]>;
```
