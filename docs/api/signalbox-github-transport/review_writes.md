<!-- generated by scripts/render_domain_spine.py, do not edit -->

# review_writes

## ReviewWriteError

```rust
pub struct ReviewWriteError;
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ReviewWriteRecorder

```rust
pub trait ReviewWriteRecorder: fmt::Debug + marker::Send + marker::Sync {
    fn begin<'a>(
        &'a self,
        repository: &'a str,
    ) -> future::BoxFuture<'a, result::Result<boxed::Box<dyn PendingReviewWrite>, ReviewWriteError>>;
}
```

## PendingReviewWrite

```rust
pub trait PendingReviewWrite: marker::Send {
    fn record(
        self: boxed::Box<Self>,
        review: nonzero::NonZeroU64,
        comment: option::Option<string::String>,
    ) -> future::BoxFuture<'static, result::Result<(), ReviewWriteError>>;
}
```
