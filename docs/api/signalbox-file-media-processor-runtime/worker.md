<!-- generated by scripts/render_domain_spine.py, do not edit -->

# worker

## WorkerCatalog

```rust
pub struct WorkerCatalog {/* private */}
impl fmt::Debug for WorkerCatalog {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl WorkerCatalog {
    pub fn try_new(
        providers: vec::Vec<boxed::Box<dyn signalbox_file_media_runtime::FileMediaProvider>>,
    ) -> result::Result<Self, WorkerCatalogConstructionError>;
    pub fn declarations(
        &self,
    ) -> vec::Vec<signalbox_file_media_runtime::FileMediaProviderDeclaration>;
}
```

## WorkerCatalogConstructionError

```rust
pub enum WorkerCatalogConstructionError {
    Empty,
    DuplicateProvider,
    DuplicateReader,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for WorkerCatalogConstructionError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for WorkerCatalogConstructionError {}
```

## WorkerServiceError

```rust
pub enum WorkerServiceError {
    Protocol,
    Provider,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for WorkerServiceError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for WorkerServiceError {}
```

## serve_one

```rust
pub async fn serve_one(catalog: &WorkerCatalog) -> result::Result<(), WorkerServiceError>;
```
