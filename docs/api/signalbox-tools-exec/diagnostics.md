<!-- generated by scripts/render_domain_spine.py, do not edit -->

# diagnostics

## CARGO_DIAGNOSTICS_NAME

```rust
pub const CARGO_DIAGNOSTICS_NAME: &str;
```

## CargoDiagnosticsCommand

```rust
pub enum CargoDiagnosticsCommand {
    Check,
    Clippy,
    Test,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, de::Deserialize<'de>, ser::Serialize, schemars::JsonSchema
```

## CargoDiagnosticsArguments

```rust
pub struct CargoDiagnosticsArguments {
    pub command: CargoDiagnosticsCommand,
    pub timeout_seconds: u64,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, de::Deserialize<'de>, schemars::JsonSchema
```

## CargoDiagnosticsToolConstructionError

```rust
pub enum CargoDiagnosticsToolConstructionError {
    Exec(ExecToolConstructionError),
    Name,
    Schema,
    ErrorDetail,
    Duplicate,
}
// derives: fmt::Debug
impl fmt::Display for CargoDiagnosticsToolConstructionError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for CargoDiagnosticsToolConstructionError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
impl convert::From<ExecToolConstructionError> for CargoDiagnosticsToolConstructionError {
    fn from(value: ExecToolConstructionError) -> Self;
}
```

## CargoDiagnosticsTool

```rust
pub struct CargoDiagnosticsTool<Runner> {/* private */}
// derives: clone::Clone, fmt::Debug
impl<Runner: ProcessRunner> CargoDiagnosticsTool<Runner> {
    pub fn try_new(
        runner: Runner,
        workspace_root: impl convert::AsRef<path::Path>,
    ) -> result::Result<Self, CargoDiagnosticsToolConstructionError>;
    pub fn try_new_with_cargo_registry(
        runner: Runner,
        workspace_root: impl convert::AsRef<path::Path>,
        cargo_registry: impl convert::AsRef<path::Path>,
    ) -> result::Result<Self, CargoDiagnosticsToolConstructionError>;
    pub fn with_sandbox_configuration(self, configuration: SandboxConfiguration) -> Self;
    pub fn into_parts(
        self,
    ) -> (
        signalbox_application::CompiledToolCatalog,
        CargoDiagnosticsExecutor<Runner>,
    );
}
impl CargoDiagnosticsTool<TokioProcessRunner> {
    pub fn try_new_production(
        workspace_root: impl convert::AsRef<path::Path>,
        supervisor_program: impl convert::AsRef<path::Path>,
    ) -> result::Result<Self, CargoDiagnosticsToolConstructionError>;
}
```

## InvalidCargoDiagnosticsArguments

```rust
pub struct InvalidCargoDiagnosticsArguments;
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for InvalidCargoDiagnosticsArguments {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for InvalidCargoDiagnosticsArguments {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## CargoDiagnosticsExecutor

```rust
pub struct CargoDiagnosticsExecutor<Runner> {/* private */}
// derives: clone::Clone, fmt::Debug
impl<Runner: ProcessRunner> signalbox_application::ToolExecutor
    for CargoDiagnosticsExecutor<Runner>
{
    type Error = CargoDiagnosticsExecutorError;
    async fn execute(
        &mut self,
        invocation: signalbox_application::ToolExecutionInvocation,
    ) -> result::Result<
        signalbox_application::CorrelatedToolExecutorEvidence,
        <Self as signalbox_application::ToolExecutor>::Error,
    >;
}
```

## CargoDiagnosticsExecutorError

```rust
pub enum CargoDiagnosticsExecutorError {
    ArgumentValidationDrift,
    ResultEncoding,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for CargoDiagnosticsExecutorError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for CargoDiagnosticsExecutorError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
impl signalbox_application::ClassifyOperatorFailure for CargoDiagnosticsExecutorError {
    fn operator_failure_class(&self) -> signalbox_application::OperatorFailureClass;
}
```

## CargoDiagnosticsRunner

```rust
pub struct CargoDiagnosticsRunner<Runner> {/* private */}
// derives: clone::Clone, fmt::Debug
impl<Runner: ProcessRunner> CargoDiagnosticsRunner<Runner> {
    pub fn try_new(
        runner: Runner,
        workspace_root: impl convert::AsRef<path::Path>,
    ) -> result::Result<Self, ExecToolConstructionError>;
    pub fn try_new_with_cargo_registry(
        runner: Runner,
        workspace_root: impl convert::AsRef<path::Path>,
        cargo_registry: impl convert::AsRef<path::Path>,
    ) -> result::Result<Self, ExecToolConstructionError>;
    pub async fn try_run(
        &mut self,
        arguments: CargoDiagnosticsArguments,
    ) -> result::Result<CargoDiagnosticsResult, InvalidCargoDiagnosticsArguments>;
}
```

## CargoDiagnosticsResult

```rust
pub struct CargoDiagnosticsResult {
    pub command: CargoDiagnosticsCommand,
    pub execution: CargoDiagnosticsExecution,
    pub diagnostics: CargoDiagnosticRecords,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
```

## CargoDiagnosticsExecution

```rust
pub struct CargoDiagnosticsExecution {
    pub confinement: ExecutionConfinement,
    pub outcome: ProcessOutcome,
    pub stdout: CargoDiagnosticsStream,
    pub stderr: CargoDiagnosticsStream,
    pub cargo_failure: option::Option<CargoFailureDetail>,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
```

## CargoFailureDetail

```rust
pub struct CargoFailureDetail {
    pub message: string::String,
    pub message_completeness: CaptureCompleteness,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
```

## CargoDiagnosticsStream

```rust
pub struct CargoDiagnosticsStream {
    pub completeness: CaptureCompleteness,
    pub encoding: OutputEncoding,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
```

## CargoDiagnosticRecords

```rust
pub struct CargoDiagnosticRecords {
    pub values: vec::Vec<CargoDiagnostic>,
    pub limit_reached: bool,
    pub provenance: CargoEvidenceProvenance,
    pub known_truncated: bool,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
```

## CargoEvidenceProvenance

```rust
pub enum CargoEvidenceProvenance {
    WorkspaceInfluenced,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
```

## CargoDiagnostic

```rust
pub struct CargoDiagnostic {
    pub file: option::Option<string::String>,
    pub file_completeness: CaptureCompleteness,
    pub span: option::Option<CargoDiagnosticSpan>,
    pub level: string::String,
    pub level_completeness: CaptureCompleteness,
    pub message: string::String,
    pub message_completeness: CaptureCompleteness,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
```

## CargoDiagnosticSpan

```rust
pub struct CargoDiagnosticSpan {
    pub line_start: u64,
    pub column_start: u64,
    pub line_end: u64,
    pub column_end: u64,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
```
