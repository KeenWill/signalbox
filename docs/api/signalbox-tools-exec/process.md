<!-- generated by scripts/render_domain_spine.py, do not edit -->

# process

## SANDBOXED_EXEC_NAME

```rust
pub const SANDBOXED_EXEC_NAME: &str;
```

## UNSANDBOXED_EXEC_NAME

```rust
pub const UNSANDBOXED_EXEC_NAME: &str;
```

## ExecArguments

```rust
pub struct ExecArguments {
    pub program: string::String,
    pub arguments: vec::Vec<string::String>,
    pub working_directory: string::String,
    pub timeout_seconds: u64,
}
// derives: clone::Clone, fmt::Debug, de::Deserialize<'de>, schemars::JsonSchema
```

## SandboxedExecArguments

```rust
pub struct SandboxedExecArguments {
    pub command: ExecArguments,
    pub credential_purpose: option::Option<string::String>,
}
// derives: clone::Clone, fmt::Debug, de::Deserialize<'de>, schemars::JsonSchema
impl SandboxedExecArguments {
    pub fn decode(
        arguments: &signalbox_domain::NormalizedToolArguments,
        timeout_bound: option::Option<time::Duration>,
    ) -> result::Result<Self, InvalidExecArguments>;
}
```

## ExecToolConstructionError

```rust
pub enum ExecToolConstructionError {
    Name,
    Schema,
    ErrorDetail,
    Duplicate,
    WorkspaceRoot {
        path: path::PathBuf,
        source: option::Option<error::Error>,
    },
    CargoRegistry {
        path: path::PathBuf,
        source: option::Option<error::Error>,
    },
    SupervisorProgram {
        path: path::PathBuf,
        source: option::Option<error::Error>,
    },
}
// derives: fmt::Debug
impl fmt::Display for ExecToolConstructionError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for ExecToolConstructionError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## SandboxedExecTool

```rust
pub struct SandboxedExecTool<Runner> {/* private */}
// derives: clone::Clone, fmt::Debug
impl<Runner: ProcessRunner> SandboxedExecTool<Runner> {
    pub fn try_new(
        runner: Runner,
        workspace_root: impl convert::AsRef<path::Path>,
        timeout_bound: option::Option<time::Duration>,
    ) -> result::Result<Self, ExecToolConstructionError>;
    pub fn try_new_with_cargo_registry(
        runner: Runner,
        workspace_root: impl convert::AsRef<path::Path>,
        cargo_registry: impl convert::AsRef<path::Path>,
        timeout_bound: option::Option<time::Duration>,
    ) -> result::Result<Self, ExecToolConstructionError>;
    pub fn with_sandbox_configuration(self, configuration: SandboxConfiguration) -> Self;
    pub fn into_parts(
        self,
    ) -> (
        signalbox_application::CompiledToolCatalog,
        ExecExecutor<SandboxedCommandRunner<Runner>>,
    );
}
impl SandboxedExecTool<TokioProcessRunner> {
    pub fn try_new_production(
        workspace_root: impl convert::AsRef<path::Path>,
        supervisor_program: impl convert::AsRef<path::Path>,
        timeout_bound: option::Option<time::Duration>,
    ) -> result::Result<Self, ExecToolConstructionError>;
}
```

## UnsandboxedExecTool

```rust
pub struct UnsandboxedExecTool<Runner> {/* private */}
// derives: clone::Clone, fmt::Debug
impl<Runner: ProcessRunner> UnsandboxedExecTool<Runner> {
    pub fn try_new(
        runner: Runner,
        workspace_root: impl convert::AsRef<path::Path>,
    ) -> result::Result<Self, ExecToolConstructionError>;
    pub fn into_parts(
        self,
    ) -> (
        signalbox_application::CompiledToolCatalog,
        ExecExecutor<UnsandboxedCommandRunner<Runner>>,
    );
}
impl UnsandboxedExecTool<TokioProcessRunner> {
    pub fn try_new_production(
        workspace_root: impl convert::AsRef<path::Path>,
        supervisor_program: impl convert::AsRef<path::Path>,
    ) -> result::Result<Self, ExecToolConstructionError>;
}
```

## InvalidExecArguments

```rust
pub struct InvalidExecArguments;
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for InvalidExecArguments {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for InvalidExecArguments {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## ExecExecutor

```rust
pub struct ExecExecutor<CommandRunner> {/* private */}
// derives: clone::Clone, fmt::Debug
impl<Runner: ProcessRunner> ExecExecutor<SandboxedCommandRunner<Runner>> {
    pub async fn run_with_configuration(
        &mut self,
        arguments: ExecArguments,
        configuration: SandboxConfiguration,
    ) -> result::Result<ExecResult, ExecExecutorError>;
}
impl<CommandRunner: CommandExecution> signalbox_application::ToolExecutor
    for ExecExecutor<CommandRunner>
{
    type Error = ExecExecutorError;
    async fn execute(
        &mut self,
        invocation: signalbox_application::ToolExecutionInvocation,
    ) -> result::Result<
        signalbox_application::CorrelatedToolExecutorEvidence,
        <Self as signalbox_application::ToolExecutor>::Error,
    >;
}
```

## ExecExecutorError

```rust
pub enum ExecExecutorError {
    ArgumentValidationDrift,
    ResultEncoding,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for ExecExecutorError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for ExecExecutorError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
impl signalbox_application::ClassifyOperatorFailure for ExecExecutorError {
    fn operator_failure_class(&self) -> signalbox_application::OperatorFailureClass;
}
```

## ProcessRequest

```rust
pub struct ProcessRequest {
    pub program: os_str::OsString,
    pub arguments: vec::Vec<os_str::OsString>,
    pub working_directory: path::PathBuf,
    pub timeout: time::Duration,
    pub capture_bytes: usize,
    pub environment: map::BTreeMap<os_str::OsString, os_str::OsString>,
    pub environment_inheritance: ProcessEnvironment,
    pub status_protocol: ProcessStatusProtocol,
}
// derives: clone::Clone, cmp::Eq, cmp::PartialEq
impl fmt::Debug for ProcessRequest {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result;
}
```

## ProcessEnvironment

```rust
pub enum ProcessEnvironment {
    Inherit,
    Clear,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ProcessStatusProtocol

```rust
pub enum ProcessStatusProtocol {
    Direct,
    SandboxDispatch,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## BwrapAvailability

```rust
pub enum BwrapAvailability {
    Available,
    Missing,
    Unusable,
    TimedOut,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
```

## ProcessRunner

```rust
pub trait ProcessRunner: clone::Clone + marker::Send {
    fn sandbox_launcher_program(&self) -> &path::Path;
    fn sandbox_launcher_descriptor(&self) -> option::Option<i32>;
    fn bwrap_availability(
        &mut self,
        probe: ProcessRequest,
    ) -> impl future::Future<Output = BwrapAvailability> + marker::Send;
    fn run(
        &mut self,
        request: ProcessRequest,
    ) -> impl future::Future<Output = ProcessRunResult> + marker::Send;
}
```

## TokioProcessRunner

```rust
pub struct TokioProcessRunner {/* private */}
// derives: clone::Clone, fmt::Debug
impl TokioProcessRunner {
    pub fn try_new(
        supervisor_program: impl convert::AsRef<path::Path>,
    ) -> result::Result<Self, ExecToolConstructionError>;
}
impl ProcessRunner for TokioProcessRunner {
    fn sandbox_launcher_program(&self) -> &path::Path;
    fn sandbox_launcher_descriptor(&self) -> option::Option<i32>;
    async fn bwrap_availability(&mut self, probe: ProcessRequest) -> BwrapAvailability;
    async fn run(&mut self, request: ProcessRequest) -> ProcessRunResult;
}
```

## SandboxProcessNamespace

```rust
pub enum SandboxProcessNamespace {
    Private,
    Container,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, default::Default, cmp::Eq, cmp::PartialEq
```

## SandboxNetwork

```rust
pub enum SandboxNetwork {
    None,
    Host,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, default::Default, cmp::Eq, cmp::PartialEq
```

## SandboxConfiguration

```rust
pub struct SandboxConfiguration {
    pub network: SandboxNetwork,
    pub read_only_binds: vec::Vec<path::PathBuf>,
    pub read_only_mounts: vec::Vec<SandboxReadOnlyMount>,
    pub path_prepend: vec::Vec<path::PathBuf>,
    pub rustup_home: option::Option<path::PathBuf>,
    pub rustup_toolchain: option::Option<string::String>,
    pub environment: map::BTreeMap<os_str::OsString, os_str::OsString>,
}
// derives: clone::Clone, default::Default, cmp::Eq, cmp::PartialEq
impl fmt::Debug for SandboxConfiguration {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
```

## SandboxReadOnlyMount

```rust
pub struct SandboxReadOnlyMount {
    pub source: path::PathBuf,
    pub destination: path::PathBuf,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## SandboxedCommandRunner

```rust
pub struct SandboxedCommandRunner<Runner> {/* private */}
// derives: clone::Clone, fmt::Debug
impl<Runner: ProcessRunner> SandboxedCommandRunner<Runner> {
    pub fn try_new(
        runner: Runner,
        workspace_root: impl convert::AsRef<path::Path>,
    ) -> result::Result<Self, ExecToolConstructionError>;
    pub fn try_new_with_cargo_registry(
        runner: Runner,
        workspace_root: impl convert::AsRef<path::Path>,
        cargo_registry: impl convert::AsRef<path::Path>,
    ) -> result::Result<Self, ExecToolConstructionError>;
    pub fn try_new_with_process_namespace(
        runner: Runner,
        workspace_root: impl convert::AsRef<path::Path>,
        process_namespace: SandboxProcessNamespace,
    ) -> result::Result<Self, ExecToolConstructionError>;
    pub fn with_sandbox_configuration(self, configuration: SandboxConfiguration) -> Self;
    pub async fn try_run(
        &mut self,
        arguments: ExecArguments,
    ) -> result::Result<ExecResult, InvalidExecArguments>;
}
```

## UnsandboxedCommandRunner

```rust
pub struct UnsandboxedCommandRunner<Runner> {/* private */}
// derives: clone::Clone, fmt::Debug
impl<Runner: ProcessRunner> UnsandboxedCommandRunner<Runner> {
    pub fn try_new(
        runner: Runner,
        workspace_root: impl convert::AsRef<path::Path>,
    ) -> result::Result<Self, ExecToolConstructionError>;
}
```

## ExecResult

```rust
pub struct ExecResult {
    pub confinement: ExecutionConfinement,
    pub outcome: ProcessOutcome,
    pub diagnostic: option::Option<ExecutionDiagnostic>,
    pub stdout: OutputCapture,
    pub stderr: OutputCapture,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
```

## ExecutionDiagnostic

```rust
pub enum ExecutionDiagnostic {
    NetworkFenceActive,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
```

## ExecutionConfinement

```rust
pub enum ExecutionConfinement {
    FilesystemConfined,
    Unsandboxed,
    SandboxRefused { availability: BwrapAvailability },
    SandboxSetupFailed,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
```

## ProcessOutcome

```rust
pub enum ProcessOutcome {
    Exited { code: option::Option<i32> },
    TimedOut,
    SpawnFailed { reason: ProcessSpawnFailure },
    SupervisionFailed { reason: ProcessSupervisionFailure },
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
```

## ProcessSpawnFailure

```rust
pub enum ProcessSpawnFailure {
    NotFound,
    PermissionDenied,
    ProcessTreeUnsupported,
    SandboxUnavailable,
    SandboxSetup,
    Other,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
```

## ProcessSupervisionFailure

```rust
pub enum ProcessSupervisionFailure {
    Wait,
    Cleanup,
    Stdout,
    Stderr,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
```

## OutputCapture

```rust
pub struct OutputCapture {
    pub text: string::String,
    pub completeness: CaptureCompleteness,
    pub encoding: OutputEncoding,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
```

## CaptureCompleteness

```rust
pub enum CaptureCompleteness {
    Complete,
    Truncated,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
```

## OutputEncoding

```rust
pub enum OutputEncoding {
    Utf8,
    LossyUtf8,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
```

## ProcessRunResult

```rust
pub struct ProcessRunResult {
    pub outcome: ProcessOutcome,
    pub stdout: ProcessOutput,
    pub stderr: ProcessOutput,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ProcessOutput

```rust
pub struct ProcessOutput {
    pub bytes: vec::Vec<u8>,
    pub completeness: CaptureCompleteness,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```
