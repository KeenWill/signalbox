<!-- generated by scripts/render_domain_spine.py, do not edit -->

# program_primitives

## UnixMillis

```rust
pub struct UnixMillis(pub u64);
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl program_primitives::UnixMillis {
    pub fn encode(self) -> InlineFramePayload;
    pub fn decode(payload: &InlineFramePayload) -> option::Option<Self>;
}
```

## RandomValue

```rust
pub struct RandomValue(pub u64);
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl program_primitives::RandomValue {
    pub fn encode(self) -> InlineFramePayload;
    pub fn decode(payload: &InlineFramePayload) -> option::Option<Self>;
}
```

## SleepUntil

```rust
pub struct SleepUntil(pub program_primitives::UnixMillis);
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl program_primitives::SleepUntil {
    pub fn encode(self) -> InlineFramePayload;
    pub fn decode(payload: &InlineFramePayload) -> option::Option<Self>;
}
```

## ProgramEventSource

```rust
pub enum ProgramEventSource {
    ProgramAnswers(ProgramRunId),
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## AwaitProgramEvent

```rust
pub struct AwaitProgramEvent {
    pub source: program_primitives::ProgramEventSource,
    pub after: u64,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl program_primitives::AwaitProgramEvent {
    pub fn encode(self) -> InlineFramePayload;
    pub fn decode(payload: &InlineFramePayload) -> option::Option<Self>;
}
```

## ProgramEvent

```rust
pub struct ProgramEvent {
    pub position: u64,
    pub payload: InlineFramePayload,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl program_primitives::ProgramEvent {
    pub fn encode(&self) -> InlineFramePayload;
    pub fn decode(payload: &InlineFramePayload) -> option::Option<Self>;
}
```
