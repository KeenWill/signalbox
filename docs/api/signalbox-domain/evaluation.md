<!-- generated by scripts/render_domain_spine.py, do not edit -->

# evaluation

## EvaluationSnapshot

```rust
pub struct EvaluationSnapshot {
    pub run: ProgramRunId,
    pub registration: ProgramRegistrationId,
    pub input: vec::Vec<u8>,
    pub metadata: value::Value,
    pub scorecard_kind: string::String,
    pub scorecard: value::Value,
    pub trials: vec::Vec<evaluation::EvaluationTrial>,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## EvaluationTrial

```rust
pub struct EvaluationTrial {
    pub ordinal: u32,
    pub case_position: u32,
    pub repeat: u32,
    pub case: value::Value,
    pub evidence_position: JournalPosition,
    pub outcome: evaluation::EvaluationOutcome,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## EvaluationOutcome

```rust
pub enum EvaluationOutcome {
    Verdict(value::Value),
    Failed(value::Value),
    Ambiguous,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## EvaluationReceipt

```rust
pub struct EvaluationReceipt {
    pub run: ProgramRunId,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```
