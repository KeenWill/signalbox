<!-- generated by scripts/render_domain_spine.py, do not edit -->

# repo_watch

## RepoWatchTextError

```rust
pub enum RepoWatchTextError {
    Empty,
    ContainsNull,
    TooLong { bytes: usize, maximum: usize },
    TooManyCharacters { characters: usize, maximum: usize },
    Malformed,
    UnanchoredPattern,
    InvalidPattern { reason: string::String },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for repo_watch::RepoWatchTextError {
    pub fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for repo_watch::RepoWatchTextError {}
```

## RepositorySlug

```rust
pub struct RepositorySlug(/* private */);
// derives: clone::Clone, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl repo_watch::RepositorySlug {
    pub fn try_new(value: string::String) -> result::Result<Self, repo_watch::RepoWatchTextError>;
    pub fn as_str(&self) -> &str;
    pub fn into_string(self) -> string::String;
}
```

## BranchName

```rust
pub struct BranchName(/* private */);
// derives: clone::Clone, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl repo_watch::BranchName {
    pub fn try_new(value: string::String) -> result::Result<Self, repo_watch::RepoWatchTextError>;
    pub fn as_str(&self) -> &str;
    pub fn into_string(self) -> string::String;
}
```

## LabelName

```rust
pub struct LabelName(/* private */);
// derives: clone::Clone, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl repo_watch::LabelName {
    pub fn try_new(value: string::String) -> result::Result<Self, repo_watch::RepoWatchTextError>;
    pub fn as_str(&self) -> &str;
    pub fn into_string(self) -> string::String;
}
```

## RepoWatchAuthorLogin

```rust
pub struct RepoWatchAuthorLogin(/* private */);
// derives: clone::Clone, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl repo_watch::RepoWatchAuthorLogin {
    pub fn try_new(value: string::String) -> result::Result<Self, repo_watch::RepoWatchTextError>;
    pub fn as_str(&self) -> &str;
    pub fn into_string(self) -> string::String;
}
```

## CheckRunName

```rust
pub struct CheckRunName(/* private */);
// derives: clone::Clone, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl repo_watch::CheckRunName {
    pub fn try_new(value: string::String) -> result::Result<Self, repo_watch::RepoWatchTextError>;
    pub fn as_str(&self) -> &str;
    pub fn into_string(self) -> string::String;
}
```

## WorkflowName

```rust
pub struct WorkflowName(/* private */);
// derives: clone::Clone, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl repo_watch::WorkflowName {
    pub fn try_new(value: string::String) -> result::Result<Self, repo_watch::RepoWatchTextError>;
    pub fn as_str(&self) -> &str;
    pub fn into_string(self) -> string::String;
}
```

## ReactionContent

```rust
pub struct ReactionContent(/* private */);
// derives: clone::Clone, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl repo_watch::ReactionContent {
    pub fn try_new(value: string::String) -> result::Result<Self, repo_watch::RepoWatchTextError>;
    pub fn as_str(&self) -> &str;
    pub fn into_string(self) -> string::String;
}
```

## RepoWatchRuleId

```rust
pub struct RepoWatchRuleId(/* private */);
// derives: clone::Clone, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl repo_watch::RepoWatchRuleId {
    pub fn try_new(value: string::String) -> result::Result<Self, repo_watch::RepoWatchTextError>;
    pub fn as_str(&self) -> &str;
    pub fn into_string(self) -> string::String;
}
```

## ReviewThreadId

```rust
pub struct ReviewThreadId(/* private */);
// derives: clone::Clone, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl repo_watch::ReviewThreadId {
    pub fn try_new(value: string::String) -> result::Result<Self, repo_watch::RepoWatchTextError>;
    pub fn as_str(&self) -> &str;
    pub fn into_string(self) -> string::String;
}
```

## PullRequestTitle

```rust
pub struct PullRequestTitle(/* private */);
// derives: clone::Clone, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl repo_watch::PullRequestTitle {
    pub fn try_new(value: string::String) -> result::Result<Self, repo_watch::RepoWatchTextError>;
    pub fn as_str(&self) -> &str;
    pub fn into_string(self) -> string::String;
}
```

## PullRequestBody

```rust
pub struct PullRequestBody(/* private */);
// derives: clone::Clone, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
impl repo_watch::PullRequestBody {
    pub fn try_new(value: string::String) -> result::Result<Self, repo_watch::RepoWatchTextError>;
    pub fn as_str(&self) -> &str;
    pub fn into_string(self) -> string::String;
}
```

## CommitSha

```rust
pub struct CommitSha(/* private */);
// derives: clone::Clone, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl repo_watch::CommitSha {
    pub fn try_new(value: string::String) -> result::Result<Self, repo_watch::RepoWatchTextError>;
    pub fn as_str(&self) -> &str;
    pub fn into_string(self) -> string::String;
}
```

## PullRequestNumber

```rust
pub struct PullRequestNumber(/* private */);
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl repo_watch::PullRequestNumber {
    pub const fn new(value: nonzero::NonZeroU64) -> Self;
    pub const fn get(self) -> u64;
}
```

## GitHubObjectId

```rust
pub struct GitHubObjectId(/* private */);
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl repo_watch::GitHubObjectId {
    pub const fn new(value: nonzero::NonZeroU64) -> Self;
    pub const fn get(self) -> u64;
}
```

## RepoWatchWorkflowRunAttempt

```rust
pub struct RepoWatchWorkflowRunAttempt(/* private */);
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl repo_watch::RepoWatchWorkflowRunAttempt {
    pub const fn new(value: nonzero::NonZeroU64) -> Self;
    pub const fn get(self) -> u64;
}
```

## RepoWatchPattern

```rust
pub struct RepoWatchPattern { /* private */ }
// derives: clone::Clone
impl fmt::Debug for repo_watch::RepoWatchPattern {
    pub fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl cmp::PartialEq for repo_watch::RepoWatchPattern {
    pub fn eq(&self, other: &Self) -> bool;
}
impl cmp::Eq for repo_watch::RepoWatchPattern {}
impl hash::Hash for repo_watch::RepoWatchPattern {
    pub fn hash<H: hash::Hasher>(&self, state: &mut H);
}
impl repo_watch::RepoWatchPattern {
    const MAX_UTF8_BYTES: usize;
    pub fn try_new(value: string::String) -> result::Result<Self, repo_watch::RepoWatchTextError>;
    pub fn as_str(&self) -> &str;
    pub fn is_match(&self, candidate: &str) -> bool;
}
```

## RepoWatchRuleVersion

```rust
pub struct RepoWatchRuleVersion(/* private */);
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl repo_watch::RepoWatchRuleVersion {
    const V1: Self;
    pub const fn new(value: nonzero::NonZeroU64) -> option::Option<Self>;
    pub const fn get(self) -> u64;
}
```

## RepoWatchEventKindNameV1

```rust
pub enum RepoWatchEventKindNameV1 {
    PullRequestOpened,
    PullRequestClosed,
    PullRequestMerged,
    HeadChanged,
    MergeableStateChanged,
    ChecksCompleted,
    CheckRunCompleted,
    BranchWorkflowRunCompleted,
    ReviewSubmitted,
    ThreadOpened,
    ThreadResolved,
    Labeled,
    Unlabeled,
    BaseAdvanced,
    ReactionChanged,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl repo_watch::RepoWatchEventKindNameV1 {
    pub fn all() -> vec::Vec<Self>;
}
```

## ChecksOutcome

```rust
pub enum ChecksOutcome {
    Success,
    Failure,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
impl convert::From<repo_watch::ChecksOutcome> for repo_watch::CheckConclusion {
    pub fn from(value: repo_watch::ChecksOutcome) -> Self;
}
```

## CheckConclusion

```rust
pub enum CheckConclusion {
    Success,
    Failure,
    Neutral,
    Cancelled,
    Skipped,
    TimedOut,
    ActionRequired,
    Stale,
    StartupFailure,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl convert::From<repo_watch::ChecksOutcome> for repo_watch::CheckConclusion {
    pub fn from(value: repo_watch::ChecksOutcome) -> Self;
}
```

## MergeableState

```rust
pub enum MergeableState {
    Mergeable,
    Conflicting,
    Unknown,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
```

## ReviewState

```rust
pub enum ReviewState {
    Approved,
    ChangesRequested,
    Commented,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
```

## ReactionChange

```rust
pub enum ReactionChange {
    Added,
    Removed,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
```

## ReactionSubject

```rust
pub enum ReactionSubject {
    PullRequestBody,
    IssueComment { id: repo_watch::GitHubObjectId },
    ReviewComment { id: repo_watch::GitHubObjectId },
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
```

## RepoWatchEventKindV1

```rust
pub enum RepoWatchEventKindV1 {
    PullRequestOpened,
    PullRequestClosed,
    PullRequestMerged,
    HeadChanged { previous: repo_watch::CommitSha, current: repo_watch::CommitSha },
    MergeableStateChanged { current: repo_watch::MergeableState },
    ChecksCompleted { outcome: repo_watch::ChecksOutcome },
    CheckRunCompleted { name: repo_watch::CheckRunName, conclusion: repo_watch::CheckConclusion },
    BranchWorkflowRunCompleted { branch: repo_watch::BranchName, workflow: repo_watch::WorkflowName, conclusion: repo_watch::CheckConclusion },
    ReviewSubmitted { reviewer: repo_watch::RepoWatchAuthorLogin, state: repo_watch::ReviewState, commit: repo_watch::CommitSha },
    ThreadOpened { thread: repo_watch::ReviewThreadId },
    ThreadResolved { thread: repo_watch::ReviewThreadId },
    Labeled { label: repo_watch::LabelName },
    Unlabeled { label: repo_watch::LabelName },
    BaseAdvanced { branch: repo_watch::BranchName },
    ReactionChanged { subject: repo_watch::ReactionSubject, reactor: repo_watch::RepoWatchAuthorLogin, content: repo_watch::ReactionContent, change: repo_watch::ReactionChange },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl repo_watch::RepoWatchEventKindV1 {
    pub const fn name(&self) -> repo_watch::RepoWatchEventKindNameV1;
}
```

## PullRequestEventContext

```rust
pub struct PullRequestEventContext { /* private */ }
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl repo_watch::PullRequestEventContext {
    pub fn new(input: repo_watch::PullRequestEventContextInput) -> Self;
    pub const fn number(&self) -> repo_watch::PullRequestNumber;
    pub const fn head_sha(&self) -> &repo_watch::CommitSha;
    pub const fn head_repository(&self) -> &repo_watch::RepositorySlug;
    pub const fn base_branch(&self) -> &repo_watch::BranchName;
    pub const fn head_branch(&self) -> &repo_watch::BranchName;
    pub const fn title(&self) -> &repo_watch::PullRequestTitle;
    pub const fn body(&self) -> &repo_watch::PullRequestBody;
    pub fn labels(&self) -> &[repo_watch::LabelName];
    pub const fn draft(&self) -> bool;
    pub const fn author(&self) -> option::Option<&repo_watch::RepoWatchAuthorLogin>;
}
```

## PullRequestEventContextInput

```rust
pub struct PullRequestEventContextInput { pub number: repo_watch::PullRequestNumber, pub head_sha: repo_watch::CommitSha, pub head_repository: repo_watch::RepositorySlug, pub base_branch: repo_watch::BranchName, pub head_branch: repo_watch::BranchName, pub title: repo_watch::PullRequestTitle, pub body: repo_watch::PullRequestBody, pub labels: vec::Vec<repo_watch::LabelName>, pub draft: bool, pub author: option::Option<repo_watch::RepoWatchAuthorLogin> }
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## RepoWatchEventTarget

```rust
pub enum RepoWatchEventTarget {
    PullRequest(repo_watch::PullRequestEventContext),
    Branch,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## RepoWatchEvent

```rust
pub struct RepoWatchEvent { /* private */ }
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl repo_watch::RepoWatchEvent {
    pub fn try_pull_request(id: RepoWatchEventId, repository: repo_watch::RepositorySlug, context: repo_watch::PullRequestEventContext, kind: repo_watch::RepoWatchEventKindV1) -> result::Result<Self, repo_watch::RepoWatchEventConstructionError>;
    pub const fn branch_workflow(id: RepoWatchEventId, repository: repo_watch::RepositorySlug, branch: repo_watch::BranchName, workflow: repo_watch::WorkflowName, conclusion: repo_watch::CheckConclusion) -> Self;
    pub const fn id(&self) -> RepoWatchEventId;
    pub const fn repository(&self) -> &repo_watch::RepositorySlug;
    pub const fn target(&self) -> &repo_watch::RepoWatchEventTarget;
    pub const fn kind(&self) -> &repo_watch::RepoWatchEventKindV1;
}
```

## RepoWatchEventConstructionError

```rust
pub enum RepoWatchEventConstructionError {
    BranchKindOnPullRequest,
    HeadChangedCurrentMismatch,
    HeadChangedWithoutChange,
    BaseAdvancedBranchMismatch,
    LabeledContextMissingLabel,
    UnlabeledContextContainsLabel,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for repo_watch::RepoWatchEventConstructionError {
    pub fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for repo_watch::RepoWatchEventConstructionError {}
```

## RepoWatchLabelMatcher

```rust
pub struct RepoWatchLabelMatcher { /* private */ }
// derives: clone::Clone, fmt::Debug, default::Default, cmp::Eq, cmp::PartialEq
impl repo_watch::RepoWatchLabelMatcher {
    pub fn new(input: repo_watch::RepoWatchLabelMatcherInput) -> Self;
    pub fn any_of(&self) -> &[repo_watch::LabelName];
    pub fn all_of(&self) -> &[repo_watch::LabelName];
    pub fn none_of(&self) -> &[repo_watch::LabelName];
}
```

## RepoWatchLabelMatcherInput

```rust
pub struct RepoWatchLabelMatcherInput { pub any_of: vec::Vec<repo_watch::LabelName>, pub all_of: vec::Vec<repo_watch::LabelName>, pub none_of: vec::Vec<repo_watch::LabelName> }
// derives: clone::Clone, fmt::Debug, default::Default, cmp::Eq, cmp::PartialEq
```

## RepoWatchMatcherV1

```rust
pub struct RepoWatchMatcherV1 { /* private */ }
// derives: clone::Clone, fmt::Debug, default::Default, cmp::Eq, cmp::PartialEq
impl repo_watch::RepoWatchMatcherV1 {
    pub fn new(input: repo_watch::RepoWatchMatcherV1Input) -> Self;
    pub fn event_kinds(&self) -> &[repo_watch::RepoWatchEventKindNameV1];
    pub const fn repository(&self) -> option::Option<&repo_watch::RepositorySlug>;
    pub const fn base_branch(&self) -> option::Option<&repo_watch::BranchName>;
    pub const fn head_branch(&self) -> option::Option<&repo_watch::RepoWatchPattern>;
    pub const fn title(&self) -> option::Option<&repo_watch::RepoWatchPattern>;
    pub const fn body(&self) -> option::Option<&repo_watch::RepoWatchPattern>;
    pub const fn labels(&self) -> &repo_watch::RepoWatchLabelMatcher;
    pub const fn draft(&self) -> option::Option<bool>;
    pub const fn author(&self) -> option::Option<&repo_watch::RepoWatchAuthorLogin>;
    pub fn mergeable_state(&self) -> &[repo_watch::MergeableState];
    pub fn conclusion(&self) -> &[repo_watch::CheckConclusion];
    pub fn matches(&self, event: &repo_watch::RepoWatchEvent) -> bool;
}
```

## RepoWatchMatcherV1Input

```rust
pub struct RepoWatchMatcherV1Input { pub event_kinds: vec::Vec<repo_watch::RepoWatchEventKindNameV1>, pub repository: option::Option<repo_watch::RepositorySlug>, pub base_branch: option::Option<repo_watch::BranchName>, pub head_branch: option::Option<repo_watch::RepoWatchPattern>, pub title: option::Option<repo_watch::RepoWatchPattern>, pub body: option::Option<repo_watch::RepoWatchPattern>, pub labels: repo_watch::RepoWatchLabelMatcher, pub draft: option::Option<bool>, pub author: option::Option<repo_watch::RepoWatchAuthorLogin>, pub mergeable_state: vec::Vec<repo_watch::MergeableState>, pub conclusion: vec::Vec<repo_watch::CheckConclusion> }
// derives: clone::Clone, fmt::Debug, default::Default, cmp::Eq, cmp::PartialEq
```

## RepoWatchSingletonScope

```rust
pub enum RepoWatchSingletonScope {
    PullRequest,
    Stack,
    Rule,
    Repository,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, default::Default, cmp::Eq, hash::Hash, cmp::PartialEq
```

## RepoWatchDispatchContextShape

```rust
pub enum RepoWatchDispatchContextShape {
    PullRequest,
    Branch,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl fmt::Display for repo_watch::RepoWatchDispatchContextShape {
    pub fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
```

## RepoWatchTemplateContextDeclaration

```rust
pub struct RepoWatchTemplateContextDeclaration { /* private */ }
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl repo_watch::RepoWatchTemplateContextDeclaration {
    pub fn try_new(template: session_template::SessionTemplateName, accepted: vec::Vec<repo_watch::RepoWatchDispatchContextShape>) -> result::Result<Self, repo_watch::RepoWatchTemplateContextDeclarationError>;
    pub const fn template(&self) -> &session_template::SessionTemplateName;
    pub fn accepted(&self) -> &[repo_watch::RepoWatchDispatchContextShape];
    pub fn accepts(&self, shape: repo_watch::RepoWatchDispatchContextShape) -> bool;
}
```

## RepoWatchTemplateContextDeclarationError

```rust
pub enum RepoWatchTemplateContextDeclarationError {
    NoAcceptedContextShape { template: session_template::SessionTemplateName },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for repo_watch::RepoWatchTemplateContextDeclarationError {
    pub fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for repo_watch::RepoWatchTemplateContextDeclarationError {}
```

## PullRequestContext

```rust
pub struct PullRequestContext { /* private */ }
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl repo_watch::PullRequestContext {
    pub const fn repository(&self) -> &repo_watch::RepositorySlug;
    pub const fn number(&self) -> repo_watch::PullRequestNumber;
    pub const fn head_sha(&self) -> &repo_watch::CommitSha;
    pub const fn head_repository(&self) -> &repo_watch::RepositorySlug;
    pub const fn head_branch(&self) -> &repo_watch::BranchName;
    pub const fn base_branch(&self) -> &repo_watch::BranchName;
    pub const fn event(&self) -> &repo_watch::RepoWatchEvent;
}
```

## BranchContext

```rust
pub struct BranchContext { /* private */ }
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl repo_watch::BranchContext {
    pub const fn repository(&self) -> &repo_watch::RepositorySlug;
    pub const fn branch(&self) -> &repo_watch::BranchName;
    pub const fn workflow(&self) -> &repo_watch::WorkflowName;
    pub const fn conclusion(&self) -> repo_watch::CheckConclusion;
    pub const fn event(&self) -> &repo_watch::RepoWatchEvent;
}
```

## DispatchSessionParameters

```rust
pub enum DispatchSessionParameters {
    PullRequest(repo_watch::PullRequestContext),
    Branch(repo_watch::BranchContext),
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl repo_watch::DispatchSessionParameters {
    pub fn try_from_event(event: repo_watch::RepoWatchEvent) -> result::Result<Self, repo_watch::RepoWatchDispatchContextError>;
    pub const fn shape(&self) -> repo_watch::RepoWatchDispatchContextShape;
    pub const fn event(&self) -> &repo_watch::RepoWatchEvent;
}
```

## RepoWatchDispatchContextError

```rust
pub enum RepoWatchDispatchContextError {
    InvalidBranchEvent,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for repo_watch::RepoWatchDispatchContextError {
    pub fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for repo_watch::RepoWatchDispatchContextError {}
```

## RepoWatchRuleActionV1

```rust
pub enum RepoWatchRuleActionV1 {
    DispatchSession { template: session_template::SessionTemplateName },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl repo_watch::RepoWatchRuleActionV1 {
    pub const fn template(&self) -> &session_template::SessionTemplateName;
}
```

## DispatchSessionAction

```rust
pub struct DispatchSessionAction { /* private */ }
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl repo_watch::DispatchSessionAction {
    pub const fn new(template: session_template::SessionTemplateName, params: repo_watch::DispatchSessionParameters) -> Self;
    pub const fn template(&self) -> &session_template::SessionTemplateName;
    pub const fn params(&self) -> &repo_watch::DispatchSessionParameters;
    pub fn synthesized_goal_statement(&self, rule: &repo_watch::RepoWatchRuleId) -> result::Result<goal::GoalStatement, goal::GoalTextError>;
}
```

## RepoWatchActionV1

```rust
pub enum RepoWatchActionV1 {
    DispatchSession(repo_watch::DispatchSessionAction),
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## RepoWatchRuleContentDigest

```rust
pub struct RepoWatchRuleContentDigest(/* private */);
// derives: clone::Clone, marker::Copy, cmp::Eq, hash::Hash, cmp::PartialEq
impl repo_watch::RepoWatchRuleContentDigest {
    pub const fn as_bytes(&self) -> &[u8; 32];
}
impl fmt::Debug for repo_watch::RepoWatchRuleContentDigest {
    pub fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
```

## RepoWatchRuleIdentityField

```rust
pub enum RepoWatchRuleIdentityField {
    MatcherEventKinds,
    MatcherRepository,
    MatcherBaseBranch,
    MatcherHeadBranchRegex,
    MatcherTitleRegex,
    MatcherBodyRegex,
    MatcherLabelsAnyOf,
    MatcherLabelsAllOf,
    MatcherLabelsNoneOf,
    MatcherDraft,
    MatcherAuthor,
    MatcherMergeableStateAnyOf,
    MatcherConclusionAnyOf,
    Actions,
    SingletonPer,
    CooldownSeconds,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl repo_watch::RepoWatchRuleIdentityField {
    pub const fn configuration_path(self) -> &'static str;
}
```

## RepoWatchRuleIdentityFieldDigest

```rust
pub struct RepoWatchRuleIdentityFieldDigest(/* private */);
// derives: clone::Clone, marker::Copy, cmp::Eq, hash::Hash, cmp::PartialEq
impl repo_watch::RepoWatchRuleIdentityFieldDigest {
    pub const fn as_bytes(&self) -> &[u8; 32];
}
impl fmt::Debug for repo_watch::RepoWatchRuleIdentityFieldDigest {
    pub fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
```

## RepoWatchRule

```rust
pub struct RepoWatchRule { /* private */ }
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl repo_watch::RepoWatchRule {
    pub fn try_new(id: repo_watch::RepoWatchRuleId, version: repo_watch::RepoWatchRuleVersion, matcher: repo_watch::RepoWatchMatcherV1, actions: vec::Vec<repo_watch::RepoWatchRuleActionV1>, singleton_per: repo_watch::RepoWatchSingletonScope, cooldown: time::Duration) -> result::Result<Self, repo_watch::RepoWatchRuleValidationError>;
    pub const fn id(&self) -> &repo_watch::RepoWatchRuleId;
    pub const fn version(&self) -> repo_watch::RepoWatchRuleVersion;
    pub const fn matcher(&self) -> &repo_watch::RepoWatchMatcherV1;
    pub fn actions(&self) -> &[repo_watch::RepoWatchRuleActionV1];
    pub const fn singleton_per(&self) -> repo_watch::RepoWatchSingletonScope;
    pub const fn cooldown(&self) -> time::Duration;
    pub fn content_digest(&self) -> repo_watch::RepoWatchRuleContentDigest;
    pub fn identity_field_digests(&self) -> vec::Vec<(repo_watch::RepoWatchRuleIdentityField, repo_watch::RepoWatchRuleIdentityFieldDigest)>;
    pub fn required_context_shapes(&self) -> vec::Vec<repo_watch::RepoWatchDispatchContextShape>;
    pub fn validate_template_contexts(&self, declarations: &[repo_watch::RepoWatchTemplateContextDeclaration]) -> result::Result<(), repo_watch::RepoWatchRuleValidationError>;
    pub fn actions_for_event(&self, event: &repo_watch::RepoWatchEvent) -> result::Result<vec::Vec<repo_watch::RepoWatchActionV1>, repo_watch::RepoWatchDispatchContextError>;
}
```

## RepoWatchRuleValidationError

```rust
pub enum RepoWatchRuleValidationError {
    NoActions,
    SubsecondCooldown,
    BranchEventWithPullRequestSingleton { scope: repo_watch::RepoWatchSingletonScope },
    TemplateNotDeclared { template: session_template::SessionTemplateName },
    TemplateRejectsContext { template: session_template::SessionTemplateName, shape: repo_watch::RepoWatchDispatchContextShape },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for repo_watch::RepoWatchRuleValidationError {
    pub fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for repo_watch::RepoWatchRuleValidationError {}
```
