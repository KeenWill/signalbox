<!-- generated by scripts/render_domain_spine.py, do not edit -->

# session_lifecycle

## DispatchingModule

```rust
pub enum DispatchingModule {
    RepositoryWatch,
    CommissionedDispatch,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
```

## ModuleDispatch

```rust
pub enum ModuleDispatch {
    RepositoryWatch { dispatch: RepoWatchDispatchId },
    Commissioned { dispatch: CommissionedDispatchId },
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
impl session_lifecycle::ModuleDispatch {
    pub const fn module(&self) -> session_lifecycle::DispatchingModule;
}
```

## CoreAgency

```rust
pub enum CoreAgency {
    Daemon,
    Model { turn: TurnId },
    Tool { request: ToolRequestId },
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
```

## LifecycleActor

```rust
pub enum LifecycleActor {
    Core { agency: session_lifecycle::CoreAgency },
    Operator,
    Module { module: session_lifecycle::DispatchingModule },
    Watchdog,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
impl session_lifecycle::LifecycleActor {
    pub const fn classify(actor: actor::Actor) -> Self;
}
```

## SessionOwnership

```rust
pub enum SessionOwnership {
    Owned,
    Unmonitored,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
impl session_lifecycle::SessionOwnership {
    pub const fn is_owned(&self) -> bool;
}
```

## SessionWaitKind

```rust
pub enum SessionWaitKind {
    Approval,
    External,
    Child,
    ProviderRetry,
    Pipeline,
    Scheduler,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
```

## SessionWaker

```rust
pub enum SessionWaker {
    ApprovalDecision,
    ExternalRecheck,
    ChildSettlement,
    ProviderBackoff,
    PipelineDrain,
    SchedulerSweep,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
```

## SessionWait

```rust
pub enum SessionWait {
    Approval,
    External,
    Child { session: SessionId },
    ProviderRetry,
    Pipeline,
    Scheduler,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
impl session_lifecycle::SessionWait {
    pub const fn kind(&self) -> session_lifecycle::SessionWaitKind;
    pub const fn waker(&self) -> session_lifecycle::SessionWaker;
}
```

## SessionRecoveryOperation

```rust
pub enum SessionRecoveryOperation {
    ModelCall,
    Tool,
    Runner,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
```

## SessionParkCause

```rust
pub enum SessionParkCause {
    RetryBudgetExhausted,
    StructuralFailure,
    UnknownFailure,
    ActiveStallDeadlineExpired,
    WaitingDeadlineExpired,
    RecoveringDeadlineExpired,
    OperatorHold,
    ModulePark,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
impl session_lifecycle::SessionParkCause {
    pub const fn admits_standing(self, standing: option::Option<session_lifecycle::SessionFailureCause>) -> bool;
}
```

## SessionParkResponder

```rust
pub enum SessionParkResponder {
    Operator,
    Module { module: session_lifecycle::DispatchingModule },
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
```

## SessionRetryableCause

```rust
pub enum SessionRetryableCause {
    ProviderTransient,
    ProviderQuotaExhausted,
    ProviderOverloaded,
    InfrastructureFailure,
    RetryBudgetExhausted,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
```

## SessionStructuralCause

```rust
pub enum SessionStructuralCause {
    ContextCompactionWall,
    ContextHeadroomExhausted,
    BrokenToolchain,
    ModerationBlock,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
```

## SessionRetirementCause

```rust
pub enum SessionRetirementCause {
    AdmissionDeadlineExpired,
    StrandedQueuedTurn,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
```

## SessionFailureCause

```rust
pub enum SessionFailureCause {
    Retryable(session_lifecycle::SessionRetryableCause),
    Structural(session_lifecycle::SessionStructuralCause),
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
```

## StopStickiness

```rust
pub enum StopStickiness {
    Sticky,
    Redispatchable,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
```

## SessionTerminalOutcome

```rust
pub enum SessionTerminalOutcome {
    AchievedVerified,
    AchievedDeclared,
    FailedRetryable { cause: session_lifecycle::SessionRetryableCause },
    FailedStructural { cause: session_lifecycle::SessionStructuralCause },
    FailedUnknown,
    Stopped { sticky: session_lifecycle::StopStickiness },
    Superseded { by: option::Option<SessionId> },
    Abandoned,
    Retired { cause: session_lifecycle::SessionRetirementCause },
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl session_lifecycle::SessionTerminalOutcome {
    pub const fn closure_outcome(&self) -> option::Option<session_lifecycle::SessionClosureOutcome>;
    pub const fn forbids_further_escalation(&self) -> bool;
    pub const fn releases_resources(&self) -> bool;
}
```

## SessionClosureOutcome

```rust
pub enum SessionClosureOutcome {
    FailedRetryable,
    FailedStructural,
    FailedUnknown,
    Stopped,
    Superseded,
    Abandoned,
    Retired,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
```

## SessionLifecycleState

```rust
pub enum SessionLifecycleState {
    Created,
    Dispatched,
    Active,
    Waiting { wait: session_lifecycle::SessionWait },
    Recovering { operation: session_lifecycle::SessionRecoveryOperation },
    Blocked { reason: goal::GoalBlockedReasonKind, cycle: u64 },
    Parked { cause: session_lifecycle::SessionParkCause, responder: session_lifecycle::SessionParkResponder, standing: option::Option<session_lifecycle::SessionFailureCause> },
    Terminal { outcome: session_lifecycle::SessionTerminalOutcome },
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl session_lifecycle::SessionLifecycleState {
    pub const fn is_terminal(&self) -> bool;
    pub const fn is_parked(&self) -> bool;
    pub const fn admits(&self, next: &Self) -> bool;
    pub fn transition(self, next: Self) -> result::Result<Self, session_lifecycle::SessionLifecycleTransitionError>;
}
```

## SessionLifecycleTransitionError

```rust
pub struct SessionLifecycleTransitionError { /* private */ }
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl session_lifecycle::SessionLifecycleTransitionError {
    pub const fn from(&self) -> session_lifecycle::SessionLifecycleState;
    pub const fn to(&self) -> session_lifecycle::SessionLifecycleState;
}
impl fmt::Display for session_lifecycle::SessionLifecycleTransitionError {
    pub fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for session_lifecycle::SessionLifecycleTransitionError {}
```

## SessionDeadlineExpiry

```rust
pub enum SessionDeadlineExpiry {
    Retire,
    Park,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
```

## SessionDeadlineKind

```rust
pub enum SessionDeadlineKind {
    Admission,
    ActiveStall,
    Waiting,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
impl session_lifecycle::SessionDeadlineKind {
    pub const fn on_expiry(&self) -> session_lifecycle::SessionDeadlineExpiry;
    pub const fn for_state(state: &session_lifecycle::SessionLifecycleState) -> option::Option<Self>;
}
```

## SessionOwnershipTransition

```rust
pub enum SessionOwnershipTransition {
    CreatedOwned,
    CreatedUnmonitored,
    Adopted,
    Released,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
impl session_lifecycle::SessionOwnershipTransition {
    pub const fn ownership(&self) -> session_lifecycle::SessionOwnership;
}
```
