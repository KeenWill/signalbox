<!-- generated by scripts/render_domain_spine.py, do not edit -->

# workspace_command

## WorkspaceOperation

```rust
pub enum WorkspaceOperation {
    Register {
        root: WorkspaceRootPath,
    },
    MintRemote {
        workspace: WorkspaceId,
        name: GitRemoteName,
        url: GitRemoteUrl,
    },
    WithdrawRemote {
        mint: GitRemoteMintId,
    },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## WorkspaceCommand

```rust
pub struct WorkspaceCommand {/* private */}
// derives: clone::Clone, fmt::Debug
impl WorkspaceCommand {
    pub const fn new(command_id: DurableCommandId, operation: WorkspaceOperation) -> Self;
    pub const fn command_id(&self) -> DurableCommandId;
    pub const fn operation(&self) -> &WorkspaceOperation;
}
impl cmp::PartialEq for WorkspaceCommand {
    fn eq(&self, other: &Self) -> bool;
}
impl cmp::Eq for WorkspaceCommand {}
```

## WorkspaceCommandResult

```rust
pub enum WorkspaceCommandResult {
    Registered(WorkspaceId),
    Minted(GitRemoteMintId),
    Withdrawn(GitRemoteWithdrawalId),
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```
