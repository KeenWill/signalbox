<!-- generated by scripts/render_domain_spine.py, do not edit -->

# program_registration

## ProgramContentDigest

```rust
pub struct ProgramContentDigest(/* private */);
// derives: clone::Clone, marker::Copy, cmp::Eq, cmp::PartialEq
impl program_registration::ProgramContentDigest {
    pub fn of(bytes: &[u8]) -> Self;
    pub const fn from_bytes(bytes: [u8; 32]) -> Self;
    pub const fn as_bytes(&self) -> &[u8; 32];
}
impl fmt::Debug for program_registration::ProgramContentDigest {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
```

## ProgramGrants

```rust
pub struct ProgramGrants(/* private */);
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl program_registration::ProgramGrants {
    pub fn new(capabilities: impl collect::IntoIterator<Item = ProgramCapability>) -> Self;
    pub fn capabilities(&self) -> &[ProgramCapability];
    pub fn contains(&self, capability: ProgramCapability) -> bool;
    pub fn permits_child(&self, child: &Self) -> bool;
}
```

## ProgramRegistrationRequest

```rust
pub struct ProgramRegistrationRequest {
    pub name: string::String,
    pub revision: string::String,
    pub source: vec::Vec<u8>,
    pub artifact: string::String,
    pub grants: program_registration::ProgramGrants,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl program_registration::ProgramRegistrationRequest {
    pub fn into_content(self) -> program_registration::ProgramRegistrationContent;
}
```

## NativeProgramRegistrationRequest

```rust
pub struct NativeProgramRegistrationRequest {
    pub name: string::String,
    pub revision: string::String,
    pub entry: string::String,
    pub native_revision: string::String,
    pub binary_digest: program_registration::ProgramContentDigest,
    pub grants: program_registration::ProgramGrants,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl program_registration::NativeProgramRegistrationRequest {
    pub fn into_content(self) -> program_registration::ProgramRegistrationContent;
}
```

## ProgramExecutable

```rust
pub enum ProgramExecutable {
    JavaScript {
        source_digest: program_registration::ProgramContentDigest,
        artifact: string::String,
    },
    Native {
        entry: string::String,
        revision: string::String,
        binary_digest: program_registration::ProgramContentDigest,
    },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ProgramRegistrationContent

```rust
pub struct ProgramRegistrationContent {
    pub name: string::String,
    pub revision: string::String,
    pub executable: program_registration::ProgramExecutable,
    pub grants: program_registration::ProgramGrants,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ProgramRegistration

```rust
pub struct ProgramRegistration {
    pub id: ProgramRegistrationId,
    pub content: program_registration::ProgramRegistrationContent,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```
