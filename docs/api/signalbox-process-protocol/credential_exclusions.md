<!-- generated by scripts/render_domain_spine.py, do not edit -->

# credential_exclusions

## CredentialExclusionTarget

```rust
pub enum CredentialExclusionTarget {
    ProfileQuarantine {
        profile: string::String,
        record_generation: CanonicalU64,
    },
    MembershipExclusion {
        pool_policy_id: CanonicalUuid,
        profile: string::String,
        record_generation: CanonicalU64,
    },
    SessionDisplacement {
        session_id: CanonicalUuid,
        pool_policy_id: CanonicalUuid,
        profile: string::String,
        record_generation: CanonicalU64,
    },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize, de::Deserialize<'de>
impl CredentialExclusionTarget {
    pub fn profile(&self) -> &str;
    pub const fn generation(&self) -> CanonicalU64;
}
impl cmp::Ord for CredentialExclusionTarget {
    fn cmp(&self, other: &Self) -> cmp::Ordering;
}
impl cmp::PartialOrd for CredentialExclusionTarget {
    fn partial_cmp(&self, other: &Self) -> option::Option<cmp::Ordering>;
}
```

## CredentialExclusionClearOutcome

```rust
pub enum CredentialExclusionClearOutcome {
    Cleared,
    AlreadyCleared,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize, de::Deserialize<'de>
```
