<!-- generated by scripts/render_domain_spine.py, do not edit -->

# credential_pool

## CredentialPoolExclusion

```rust
pub enum CredentialPoolExclusion {
    ProfileQuarantine {
        record_generation: option::Option<CanonicalU64>,
    },
    MembershipExclusion {
        record_generation: option::Option<CanonicalU64>,
    },
    SessionDisplacement {
        record_generation: option::Option<CanonicalU64>,
    },
    ChainExclusion {
        predecessor_model_call_id: CanonicalUuid,
    },
    TransientExclusion {
        observation_model_call_id: CanonicalUuid,
    },
    HeadroomReserve {
        observed_headroom_percent: i64,
        reserve_percent: u8,
    },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize, de::Deserialize<'de>
```

## CredentialPoolMemberEvidence

```rust
pub struct CredentialPoolMemberEvidence {
    pub profile: string::String,
    pub reset_at_unix_ms: option::Option<i64>,
    pub exclusion: CredentialPoolExclusion,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize, de::Deserialize<'de>
```

## valid_credential_pool_members

```rust
pub fn valid_credential_pool_members(policy_members: &[string::String]) -> bool;
```

## valid_credential_pool_evidence

```rust
pub fn valid_credential_pool_evidence(
    policy_members: &[string::String],
    members: &[CredentialPoolMemberEvidence],
) -> bool;
```
