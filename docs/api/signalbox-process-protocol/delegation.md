<!-- generated by scripts/render_domain_spine.py, do not edit -->

# delegation

## BoundChildAction

```rust
pub enum BoundChildAction {
    KeepRunning,
    Stop,
    Cancel,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize, de::Deserialize<'de>
```

## DelegationPolicy

```rust
pub enum DelegationPolicy {
    Background {},
    Bound {
        on_parent_stopped: BoundChildAction,
        on_parent_cancelled: BoundChildAction,
    },
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize, de::Deserialize<'de>
```

## DelegationWaitMode

```rust
pub enum DelegationWaitMode {
    Foreground,
    Background,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize, de::Deserialize<'de>
```

## DelegationMessageDirection

```rust
pub enum DelegationMessageDirection {
    ParentToChild,
    ChildToParent,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize, de::Deserialize<'de>
```

## DelegationToolRequestState

```rust
pub enum DelegationToolRequestState {
    AwaitingApproval,
    Denied,
    Approved,
    Prepared,
    Closed,
    AttemptEnded,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize, de::Deserialize<'de>
impl DelegationToolRequestState {
    pub const fn as_str(self) -> &'static str;
}
```

## DelegationOutcome

```rust
pub enum DelegationOutcome {
    Returned,
    Failed,
    Stopped,
    Cancelled,
    ContinueRunning,
    AlreadyTerminal,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize, de::Deserialize<'de>
```

## DelegationReason

```rust
pub enum DelegationReason {
    ChildCompleted,
    ChildExecutionFailed,
    ChildResultUnavailable,
    ChildCancelled,
    ParentStopped,
    ParentCancelled,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize, de::Deserialize<'de>
```

## DelegationProvenance

```rust
pub enum DelegationProvenance {
    ChildTurn {
        child_session_id: CanonicalUuid,
        child_turn_id: CanonicalUuid,
    },
    ParentTurnCommand {
        parent_session_id: CanonicalUuid,
        parent_turn_id: CanonicalUuid,
        command_id: CanonicalUuid,
        descendant_scope: DescendantTerminationScope,
    },
    ParentGoalCommand {
        parent_session_id: CanonicalUuid,
        goal_generation: CanonicalU64,
        command_id: CanonicalUuid,
        descendant_scope: DescendantTerminationScope,
    },
    ParentLifecycleCommand {
        parent_session_id: CanonicalUuid,
        command_id: CanonicalUuid,
        descendant_scope: DescendantTerminationScope,
    },
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize, de::Deserialize<'de>
```

## ToolApprovalEventDecision

```rust
pub enum ToolApprovalEventDecision {
    Approve {},
    Deny {
        reason: option::Option<string::String>,
    },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize, de::Deserialize<'de>
```

## ToolApprovalEventDecider

```rust
pub enum ToolApprovalEventDecider {
    User {
        command_id: CanonicalUuid,
    },
    Delegate {
        model_selection_id: CanonicalUuid,
        model_call_id: CanonicalUuid,
    },
    UserOverride {
        command_id: CanonicalUuid,
        overridden_tool_request_id: CanonicalUuid,
    },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize, de::Deserialize<'de>
```

## TranscriptToolApproval

```rust
pub struct TranscriptToolApproval {
    pub decision: ToolApprovalEventDecision,
    pub decider: ToolApprovalEventDecider,
    pub rationale: option::Option<string::String>,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize, de::Deserialize<'de>
```
