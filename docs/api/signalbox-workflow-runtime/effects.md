<!-- generated by scripts/render_domain_spine.py, do not edit -->

# effects

## EffectRecovery

```rust
pub enum EffectRecovery {
    Idempotent,
    Ambiguous,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## EffectInvocation

```rust
pub struct EffectInvocation<'a> {
    pub run: signalbox_domain::ProgramRunId,
    pub ordinal: signalbox_domain::RequestOrdinal,
    pub request: &'a signalbox_domain::EffectRequest,
}
// derives: clone::Clone, marker::Copy, fmt::Debug
```

## EffectExecutor

```rust
pub trait EffectExecutor {
    fn recovery(&self, request: &signalbox_domain::EffectRequest) -> effects::EffectRecovery;
    fn adopt<'a>(
        &'a mut self,
        invocation: effects::EffectInvocation<'a>,
    ) -> pin::Pin<
        boxed::Box<
            dyn future::Future<
                    Output = result::Result<
                        option::Option<signalbox_domain::InlineFramePayload>,
                        LiveDeliveryFailure,
                    >,
                > + 'a,
        >,
    >;
    fn execute<'a>(
        &'a mut self,
        invocation: effects::EffectInvocation<'a>,
    ) -> pin::Pin<
        boxed::Box<
            dyn future::Future<
                    Output = result::Result<
                        signalbox_domain::InlineFramePayload,
                        LiveDeliveryFailure,
                    >,
                > + 'a,
        >,
    >;
}
```
