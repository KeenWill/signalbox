<!-- generated by scripts/render_domain_spine.py, do not edit -->

# root

## FilesystemBlobStore

```rust
pub struct FilesystemBlobStore {/* private */}
// derives: clone::Clone
impl fmt::Debug for FilesystemBlobStore {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl FilesystemBlobStore {
    pub fn try_new(
        root: path::PathBuf,
    ) -> result::Result<Self, FilesystemBlobStoreConstructionError>;
    pub fn try_new_bound(
        root: path::PathBuf,
        namespace_id: uuid::Uuid,
        binding_state: NamespaceBindingState,
    ) -> result::Result<(Self, FilesystemNamespaceIdentity), FilesystemBlobStoreConstructionError>;
    pub fn from_opened_bound(
        opened: OpenedFilesystemBlobRoot,
        namespace_id: uuid::Uuid,
        binding_state: NamespaceBindingState,
    ) -> result::Result<(Self, FilesystemNamespaceIdentity), FilesystemBlobStoreConstructionError>;
    pub fn try_new_bound_for_conformance(
        root: path::PathBuf,
        namespace_id: uuid::Uuid,
        binding_state: NamespaceBindingState,
    ) -> result::Result<(Self, FilesystemNamespaceIdentity), FilesystemBlobStoreConstructionError>;
    pub fn try_new_for_conformance(
        root: path::PathBuf,
    ) -> result::Result<Self, FilesystemBlobStoreConstructionError>;
}
impl signalbox_blob_store::BlobStore for FilesystemBlobStore {
    fn put<'a>(
        &'a self,
        expected: signalbox_blob_store::ExpectedBlob,
        source: signalbox_blob_store::BlobReader,
    ) -> signalbox_blob_store::BlobStoreFuture<'a, signalbox_blob_store::BlobPutOutcome>;
    fn open<'a>(
        &'a self,
        key: &'a signalbox_blob_store::BlobObjectKey,
    ) -> signalbox_blob_store::BlobStoreFuture<'a, signalbox_blob_store::OpenedBlob>;
    fn open_verified<'a>(
        &'a self,
        expected: signalbox_blob_store::ExpectedBlob,
        key: &'a signalbox_blob_store::BlobObjectKey,
    ) -> signalbox_blob_store::BlobStoreFuture<'a, signalbox_blob_store::OpenedBlob>;
    fn open_range<'a>(
        &'a self,
        expected: signalbox_blob_store::ExpectedBlob,
        key: &'a signalbox_blob_store::BlobObjectKey,
        offset: u64,
        byte_length: nonzero::NonZeroU64,
    ) -> signalbox_blob_store::BlobStoreFuture<'a, signalbox_blob_store::OpenedBlob>;
}
```

## NamespaceBindingState

```rust
pub enum NamespaceBindingState {
    Recorded,
    New,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## FilesystemNamespaceIdentity

```rust
pub struct FilesystemNamespaceIdentity {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl FilesystemNamespaceIdentity {
    pub fn canonical_path(&self) -> &path::Path;
    pub const fn device_inode(&self) -> (u64, u64);
    pub fn physical_path(&self) -> &path::Path;
}
```

## OpenedFilesystemBlobRoot

```rust
pub struct OpenedFilesystemBlobRoot {/* private */}
impl fmt::Debug for OpenedFilesystemBlobRoot {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl OpenedFilesystemBlobRoot {
    pub fn open(root: path::PathBuf) -> result::Result<Self, FilesystemBlobStoreConstructionError>;
    pub fn open_without_locality_check_for_test(
        root: path::PathBuf,
    ) -> result::Result<Self, FilesystemBlobStoreConstructionError>;
    pub const fn identity(&self) -> &FilesystemNamespaceIdentity;
}
```

## FilesystemBlobStaging

```rust
pub struct FilesystemBlobStaging {/* private */}
impl fmt::Debug for FilesystemBlobStaging {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl FilesystemBlobStaging {
    pub fn try_new(
        root: path::PathBuf,
    ) -> result::Result<Self, FilesystemBlobStoreConstructionError>;
    pub fn try_new_without_locality_check_for_test(
        root: path::PathBuf,
    ) -> result::Result<Self, FilesystemBlobStoreConstructionError>;
    pub fn from_opened(
        opened: OpenedFilesystemBlobRoot,
    ) -> result::Result<Self, FilesystemBlobStoreConstructionError>;
    pub const fn identity(&self) -> &FilesystemNamespaceIdentity;
    pub fn sweep(&self) -> error::Result<()>;
    pub async fn create_upload(&self) -> error::Result<FilesystemBlobUpload>;
    pub fn disarm_sweep_on_drop(&self);
}
impl drop::Drop for FilesystemBlobStaging {
    fn drop(&mut self);
}
```

## FilesystemBlobUpload

```rust
pub struct FilesystemBlobUpload {/* private */}
impl fmt::Debug for FilesystemBlobUpload {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl FilesystemBlobUpload {
    pub async fn append(&mut self, chunk: &[u8]) -> error::Result<()>;
    pub async fn into_reader(self) -> error::Result<signalbox_blob_store::BlobReader>;
}
```

## FilesystemBlobStoreConstructionError

```rust
pub enum FilesystemBlobStoreConstructionError {
    NotAbsolute {
        root: path::PathBuf,
    },
    Inspect {
        root: path::PathBuf,
        source: error::Error,
    },
    NotDirectory {
        root: path::PathBuf,
    },
    NotPrivate {
        root: path::PathBuf,
    },
    UnclassifiedFilesystem {
        root: path::PathBuf,
    },
    UnstableIdentity {
        root: path::PathBuf,
    },
    PrepareNamespaceMarker {
        root: path::PathBuf,
        source: error::Error,
    },
    PrepareStagingDirectory {
        root: path::PathBuf,
        source: error::Error,
    },
    PreparePublicationDirectory {
        root: path::PathBuf,
        source: error::Error,
    },
}
impl fmt::Debug for FilesystemBlobStoreConstructionError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl fmt::Display for FilesystemBlobStoreConstructionError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for FilesystemBlobStoreConstructionError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```
