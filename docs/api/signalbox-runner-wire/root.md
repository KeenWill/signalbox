<!-- generated by scripts/render_domain_spine.py, do not edit -->

# root

## MAX_FRAME_BYTES

```rust
pub const MAX_FRAME_BYTES: usize;
```

## Frame

```rust
pub struct Frame {
    pub message: frame::Message,
}
// derives: clone::Clone, fmt::Debug, cmp::PartialEq
impl Frame {
    pub fn try_new(message: frame::Message) -> result::Result<Self, FrameError>;
}
```

## FrameError

```rust
pub enum FrameError {
    MissingNewline,
    TooLarge { bytes: usize },
    MalformedJson(error::Error),
    UnsupportedVersion(u64),
    InvalidValue(value::ValueError),
}
// derives: fmt::Debug
impl fmt::Display for FrameError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for FrameError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
impl convert::From<error::Error> for FrameError {
    fn from(value: error::Error) -> Self;
}
```

## encode_line

```rust
pub fn encode_line(frame: &Frame) -> result::Result<vec::Vec<u8>, FrameError>;
```

## decode_line

```rust
pub fn decode_line(line: &[u8]) -> result::Result<Frame, FrameError>;
```
