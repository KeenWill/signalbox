<!-- generated by scripts/render_domain_spine.py, do not edit -->

# root

## S3BlobStore

```rust
pub struct S3BlobStore {/* private */}
impl fmt::Debug for S3BlobStore {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl S3BlobStore {
    pub fn try_new(
        endpoint: url::Url,
        region: impl convert::Into<string::String>,
        bucket: impl convert::Into<string::String>,
        credentials_file: path::PathBuf,
    ) -> result::Result<Self, S3BlobStoreConstructionError>;
    pub fn try_new_bound(
        endpoint: url::Url,
        region: impl convert::Into<string::String>,
        bucket: impl convert::Into<string::String>,
        credentials_file: path::PathBuf,
        namespace_marker_body: impl convert::Into<string::String>,
    ) -> result::Result<Self, S3BlobStoreConstructionError>;
    pub async fn prepare_namespace(
        &self,
        state: S3NamespaceBindingState,
    ) -> result::Result<(), signalbox_blob_store::BlobStoreError>;
    pub async fn verify_multipart_lifecycle(
        &self,
    ) -> result::Result<(), signalbox_blob_store::BlobStoreError>;
    #[cfg(feature = "test-support")]
    pub async fn delete_for_conformance(
        &self,
        expected: signalbox_blob_store::ExpectedBlob,
    ) -> result::Result<(), signalbox_blob_store::BlobStoreError>;
    #[cfg(feature = "test-support")]
    pub async fn corrupt_for_conformance(
        &self,
        expected: signalbox_blob_store::ExpectedBlob,
        bytes: &'static [u8],
    ) -> result::Result<(), signalbox_blob_store::BlobStoreError>;
}
impl signalbox_blob_store::BlobStore for S3BlobStore {
    fn put<'a>(
        &'a self,
        expected: signalbox_blob_store::ExpectedBlob,
        source: signalbox_blob_store::BlobReader,
    ) -> signalbox_blob_store::BlobStoreFuture<'a, signalbox_blob_store::BlobPutOutcome>;
    fn open<'a>(
        &'a self,
        key: &'a signalbox_blob_store::BlobObjectKey,
    ) -> signalbox_blob_store::BlobStoreFuture<'a, signalbox_blob_store::OpenedBlob>;
    fn open_verified<'a>(
        &'a self,
        expected: signalbox_blob_store::ExpectedBlob,
        key: &'a signalbox_blob_store::BlobObjectKey,
    ) -> signalbox_blob_store::BlobStoreFuture<'a, signalbox_blob_store::OpenedBlob>;
    fn open_range<'a>(
        &'a self,
        expected: signalbox_blob_store::ExpectedBlob,
        key: &'a signalbox_blob_store::BlobObjectKey,
        offset: u64,
        length: nonzero::NonZeroU64,
    ) -> signalbox_blob_store::BlobStoreFuture<'a, signalbox_blob_store::OpenedBlob>;
}
```

## S3BlobStoreConstructionError

```rust
pub enum S3BlobStoreConstructionError {
    Endpoint,
    CredentialPath,
    Transport,
    NamespaceMarker,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for S3BlobStoreConstructionError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for S3BlobStoreConstructionError {}
```

## S3NamespaceBindingState

```rust
pub enum S3NamespaceBindingState {
    New,
    Recorded,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```
