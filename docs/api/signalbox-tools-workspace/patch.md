<!-- generated by scripts/render_domain_spine.py, do not edit -->

# patch

## MAX_PATCH_BYTES

```rust
pub const MAX_PATCH_BYTES: usize;
```

## MAX_PATCH_OPERATIONS

```rust
pub const MAX_PATCH_OPERATIONS: usize;
```

## MAX_PATCH_HUNKS

```rust
pub const MAX_PATCH_HUNKS: usize;
```

## WorkspacePatch

```rust
pub struct WorkspacePatch {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> dyn_clone::DynClone for WorkspacePatch
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl WorkspacePatch {
    pub fn parse(input: &str) -> result::Result<Self, PatchParseError>;
    pub fn operations(&self) -> &[PatchOperation];
}
impl traits::FromStr for WorkspacePatch {
    type Err = PatchParseError;
    fn from_str(input: &str) -> result::Result<Self, <Self as traits::FromStr>::Err>;
}
```

## PatchOperation

```rust
pub enum PatchOperation {
    Add {
        path: string::String,
        content: string::String,
    },
    Update {
        path: string::String,
        hunks: vec::Vec<PatchHunk>,
    },
    Delete {
        path: string::String,
    },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> dyn_clone::DynClone for PatchOperation
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl PatchOperation {
    pub fn path(&self) -> &str;
}
```

## PatchHunk

```rust
pub struct PatchHunk {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> dyn_clone::DynClone for PatchHunk
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl PatchHunk {
    pub fn before(&self) -> &str;
    pub fn after(&self) -> &str;
}
```

## PatchLocation

```rust
pub struct PatchLocation {
    pub line: usize,
    pub operation: option::Option<usize>,
    pub hunk: option::Option<usize>,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> dyn_clone::DynClone for PatchLocation
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
```

## ExpectedPatchSyntax

```rust
pub enum ExpectedPatchSyntax {
    BeginPatch,
    FileOperation,
    AddLine,
    UpdateHunk,
    HunkLine,
    EndPatch,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> dyn_clone::DynClone for ExpectedPatchSyntax
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
```

## MalformedPatchReason

```rust
pub enum MalformedPatchReason {
    ExpectedFileOperation,
    UnknownDirective,
    InvalidAddLine,
    ExpectedHunkMarker,
    InvalidHunkLine,
    HunkHasNoSource,
    HunkMakesNoChange,
    DeleteHasBody,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> dyn_clone::DynClone for MalformedPatchReason
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
```

## PatchPathRejection

```rust
pub enum PatchPathRejection {
    Absolute,
    ParentTraversal,
    Invalid,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> dyn_clone::DynClone for PatchPathRejection
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
```

## PatchParseErrorKind

```rust
pub enum PatchParseErrorKind {
    TooLarge {
        actual_bytes: usize,
    },
    Truncated {
        expected: ExpectedPatchSyntax,
    },
    Malformed {
        reason: MalformedPatchReason,
    },
    TooManyOperations,
    TooManyHunks,
    PathRejected {
        path: string::String,
        reason: PatchPathRejection,
    },
    PathOverlap {
        first_operation: usize,
        first_path: string::String,
        path: string::String,
    },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> dyn_clone::DynClone for PatchParseErrorKind
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
```

## PatchParseError

```rust
pub struct PatchParseError {
    pub location: PatchLocation,
    pub kind: PatchParseErrorKind,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> dyn_clone::DynClone for PatchParseError
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl fmt::Display for PatchParseError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for PatchParseError {}
```

## parse_patch

```rust
pub fn parse_patch(input: &str) -> result::Result<WorkspacePatch, PatchParseError>;
```

## PatchPlan

```rust
pub struct PatchPlan {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> dyn_clone::DynClone for PatchPlan
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl PatchPlan {
    pub fn operations(&self) -> &[PlannedPatchOperation];
}
```

## PlannedPatchOperation

```rust
pub enum PlannedPatchOperation {
    Add {
        path: string::String,
        content: string::String,
    },
    Update {
        path: string::String,
        content: string::String,
    },
    Delete {
        path: string::String,
    },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> dyn_clone::DynClone for PlannedPatchOperation
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl PlannedPatchOperation {
    pub fn path(&self) -> &str;
}
```

## PatchApplyErrorKind

```rust
pub enum PatchApplyErrorKind {
    AddTargetExists,
    SourceMissing,
    ContextNotFound,
    ContextAmbiguous { matches: usize },
    OverlappingHunks { first_hunk: usize },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> dyn_clone::DynClone for PatchApplyErrorKind
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
```

## PatchApplyError

```rust
pub struct PatchApplyError {
    pub operation: usize,
    pub hunk: option::Option<usize>,
    pub path: string::String,
    pub kind: PatchApplyErrorKind,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> dyn_clone::DynClone for PatchApplyError
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl fmt::Display for PatchApplyError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for PatchApplyError {}
```

## plan_patch

```rust
pub fn plan_patch(
    patch: &WorkspacePatch,
    contents: &map::BTreeMap<string::String, string::String>,
) -> result::Result<PatchPlan, PatchApplyError>;
```

## apply_patch_to_contents

```rust
pub fn apply_patch_to_contents(
    patch: &WorkspacePatch,
    contents: &mut map::BTreeMap<string::String, string::String>,
) -> result::Result<PatchPlan, PatchApplyError>;
```
