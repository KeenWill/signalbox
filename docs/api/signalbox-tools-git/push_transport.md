<!-- generated by scripts/render_domain_spine.py, do not edit -->

# push_transport

## ConfiguredGitRemote

```rust
pub struct ConfiguredGitRemote {/* private */}
// derives: clone::Clone, cmp::Eq, cmp::PartialEq
impl ConfiguredGitRemote {
    pub fn try_new(
        name: impl convert::Into<string::String>,
        url: impl convert::Into<string::String>,
    ) -> result::Result<Self, InvalidConfiguredGitRemote>;
    pub fn name(&self) -> &str;
    pub fn url(&self) -> &str;
}
impl fmt::Debug for ConfiguredGitRemote {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
```

## InvalidConfiguredGitRemote

```rust
pub struct InvalidConfiguredGitRemote;
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for InvalidConfiguredGitRemote {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for InvalidConfiguredGitRemote {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## GitPushRequest

```rust
pub struct GitPushRequest {/* private */}
// derives: clone::Clone, cmp::Eq, cmp::PartialEq
impl GitPushRequest {
    pub fn repository_root(&self) -> &path::Path;
    pub const fn remote(&self) -> &ConfiguredGitRemote;
    pub fn branch(&self) -> &str;
    pub fn commit(&self) -> &str;
    pub fn refspec(&self) -> string::String;
}
impl fmt::Debug for GitPushRequest {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
```

## GitPushReceipt

```rust
pub struct GitPushReceipt {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl GitPushReceipt {
    pub fn try_new(
        commit: impl convert::Into<string::String>,
    ) -> result::Result<Self, InvalidGitPushReceipt>;
    pub fn commit(&self) -> &str;
}
```

## InvalidGitPushReceipt

```rust
pub struct InvalidGitPushReceipt;
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for InvalidGitPushReceipt {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for InvalidGitPushReceipt {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## GitPushTransportFailure

```rust
pub enum GitPushTransportFailure {
    Rejected,
    PreDispatchInfrastructure,
    DispatchUnknown,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for GitPushTransportFailure {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for GitPushTransportFailure {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## GitPushTransport

```rust
pub trait GitPushTransport: marker::Send {
    fn push(
        &mut self,
        request: GitPushRequest,
    ) -> result::Result<GitPushReceipt, GitPushTransportFailure>;
}
```
