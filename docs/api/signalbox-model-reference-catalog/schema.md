<!-- generated by scripts/render_domain_spine.py, do not edit -->

# schema

## Provider

```rust
pub enum Provider {
    Openai,
    Anthropic,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, de::Deserialize<'de>, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
```

## CommercialChannel

```rust
pub enum CommercialChannel {
    Api,
    BatchApi,
    ChatgptSubscription,
    CodexSubscription,
    CodexCliSubscription,
    ClaudeSubscription,
    ClaudeCodeSubscription,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, de::Deserialize<'de>, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl CommercialChannel {
    pub fn actual_billing_kind(self) -> ActualBillingKind;
}
```

## ActualBillingKind

```rust
pub enum ActualBillingKind {
    ApiMetered,
    Subscription,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ModelIdentityKind

```rust
pub enum ModelIdentityKind {
    Family,
    RollingAlias,
    DatedSnapshot,
    PinnedRelease,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, de::Deserialize<'de>, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
```

## MappingQuality

```rust
pub enum MappingQuality {
    Exact,
    Strong,
    FamilyOnly,
    Approximate,
    Unknown,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, de::Deserialize<'de>, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
```

## Confidence

```rust
pub enum Confidence {
    High,
    Medium,
    Low,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, de::Deserialize<'de>, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
```

## RateDimension

```rust
pub enum RateDimension {
    Input,
    Output,
    CachedInput,
    CacheWrite,
    CombinedTokens,
    Operation,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, de::Deserialize<'de>, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
```

## DatePrecision

```rust
pub enum DatePrecision {
    ExactDay,
    ObservationWindow,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, de::Deserialize<'de>, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
```

## Catalog

```rust
pub struct Catalog {/* private */}
// derives: clone::Clone, fmt::Debug
impl Catalog {
    pub fn from_json(json: &str) -> result::Result<Self, CatalogError>;
    pub fn verified_through(&self, provider: Provider) -> &str;
    pub fn model_count(&self) -> usize;
    pub fn resolve(
        &self,
        provider: Provider,
        model_hint: &str,
        date: &str,
        commercial_channel: CommercialChannel,
    ) -> result::Result<ReferenceResolution, CatalogError>;
}
```

## CatalogError

```rust
pub struct CatalogError {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for CatalogError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for CatalogError {}
```

## ResolvedRate

```rust
pub struct ResolvedRate {
    pub dimension: RateDimension,
    pub qualifier: string::String,
    pub usd_per_million_tokens: option::Option<decimal::Decimal>,
    pub original_amount: string::String,
    pub original_currency: string::String,
    pub original_unit: string::String,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ResolvedDateWindow

```rust
pub struct ResolvedDateWindow {
    pub precision: DatePrecision,
    pub effective_from: option::Option<string::String>,
    pub effective_until: option::Option<string::String>,
    pub first_observed_new_rate: string::String,
    pub last_observed_old_rate: option::Option<string::String>,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ResolvedRateSet

```rust
pub struct ResolvedRateSet {
    pub id: string::String,
    pub rates: vec::Vec<ResolvedRate>,
    pub interval: string::String,
    pub window: ResolvedDateWindow,
    pub confidence: Confidence,
    pub source_ids: vec::Vec<string::String>,
    pub limitations: vec::Vec<string::String>,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl ResolvedRateSet {
    pub fn rate(&self, dimension: RateDimension, qualifier: &str) -> option::Option<&ResolvedRate>;
}
```

## PriceResolution

```rust
pub enum PriceResolution {
    Resolved(vec::Vec<ResolvedRateSet>),
    TransitionAmbiguous {
        last_observed_old_rate: string::String,
        first_observed_new_rate: string::String,
        candidate_rate_set_ids: vec::Vec<string::String>,
    },
    Unknown,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl PriceResolution {
    pub fn resolved_rate_sets(&self) -> option::Option<&[ResolvedRateSet]>;
}
```

## ReferenceResolution

```rust
pub enum ReferenceResolution {
    Resolved {
        model_id: string::String,
        mapping_quality: MappingQuality,
        mapping_confidence: Confidence,
        price: PriceResolution,
        mapping_source_ids: vec::Vec<string::String>,
        limitations: vec::Vec<string::String>,
    },
    FamilyOnly {
        family_id: string::String,
        mapping_confidence: Confidence,
        mapping_source_ids: vec::Vec<string::String>,
        limitations: vec::Vec<string::String>,
    },
    Ambiguous {
        candidate_model_ids: vec::Vec<string::String>,
    },
    Unknown,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl ReferenceResolution {
    pub fn resolved_model_id(&self) -> option::Option<&str>;
    pub fn price(&self) -> option::Option<&PriceResolution>;
    pub fn resolved_mapping_quality(&self) -> option::Option<MappingQuality>;
}
```
