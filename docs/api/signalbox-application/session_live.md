<!-- generated by scripts/render_domain_spine.py, do not edit -->

# session_live

## max_session_live_queued_turns

```rust
pub const fn max_session_live_queued_turns() -> u8;
```

## SessionLiveActiveState

```rust
pub enum SessionLiveActiveState {
    Running { model_call: option::Option<signalbox_domain::ModelCallId> },
    AwaitingModelCallRecovery { call: signalbox_domain::ModelCallId },
    AwaitingToolApproval { request: signalbox_domain::ToolRequestId },
    AwaitingChild { request: signalbox_domain::ToolRequestId, child: signalbox_domain::SessionId },
    AwaitingToolRecovery { attempt: signalbox_domain::ToolAttemptId },
    AwaitingRunnerRecovery { runner: signalbox_domain::RunnerId, placement_revision: u64 },
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## SessionLiveActiveTurn

```rust
pub struct SessionLiveActiveTurn { pub turn: signalbox_domain::TurnId, pub state: session_live::SessionLiveActiveState }
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## SessionLiveReconciliation

```rust
pub enum SessionLiveReconciliation {
    ModelCall { turn: signalbox_domain::TurnId, call: signalbox_domain::ModelCallId },
    ToolAttempt { turn: signalbox_domain::TurnId, attempt: signalbox_domain::ToolAttemptId },
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## SessionLiveRunnerState

```rust
pub enum SessionLiveRunnerState {
    Unpinned,
    Pinned,
    RunnerLostBeforePin,
    RunnerLost,
    RunnerAbandoned,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## SessionLiveRunnerConnectionHealth

```rust
pub enum SessionLiveRunnerConnectionHealth {
    Connected,
    Suspect,
    Shutdown,
    Lost,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## SessionLiveRunner

```rust
pub struct SessionLiveRunner { pub runner: option::Option<signalbox_domain::RunnerId>, pub placement_revision: u64, pub state: session_live::SessionLiveRunnerState, pub connection_health: option::Option<session_live::SessionLiveRunnerConnectionHealth> }
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## SessionLiveSnapshot

```rust
pub struct SessionLiveSnapshot { pub session: signalbox_domain::SessionId, pub observed_through: u64, pub active: option::Option<session_live::SessionLiveActiveTurn>, pub queued_turn_count: u64, pub queued_turns: vec::Vec<signalbox_domain::TurnId>, pub reconciliation: option::Option<session_live::SessionLiveReconciliation>, pub runner: option::Option<session_live::SessionLiveRunner> }
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## SessionLiveReader

```rust
pub trait SessionLiveReader {
    type Error;
    pub fn read_live_snapshot(&self, session: signalbox_domain::SessionId) -> impl future::Future<Output = result::Result<option::Option<session_live::SessionLiveSnapshot>, <Self as session_live::SessionLiveReader>::Error>> + marker::Send;
}
```

## ReadSessionLiveService

```rust
pub struct ReadSessionLiveService<Reader> { /* private */ }
// derives: fmt::Debug
impl<Reader> session_live::ReadSessionLiveService<Reader> {
    pub const fn new(reader: Reader) -> Self;
}
impl<Reader: session_live::SessionLiveReader> session_live::ReadSessionLiveService<Reader> {
    pub async fn snapshot(&self, session: signalbox_domain::SessionId) -> result::Result<option::Option<session_live::SessionLiveSnapshot>, <Reader as session_live::SessionLiveReader>::Error>;
}
```
