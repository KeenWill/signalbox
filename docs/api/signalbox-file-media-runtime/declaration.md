<!-- generated by scripts/render_domain_spine.py, do not edit -->

# declaration

## ProbeStrength

```rust
pub enum ProbeStrength {
    DeclaredCandidate,
    ProvisionalStructuralCandidate,
    StructuralCandidate,
    Strong,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::Ord, cmp::PartialEq, cmp::PartialOrd, de::Deserialize<'de>, ser::Serialize
impl<T> de::DeserializeOwned for ProbeStrength where T: for<'de> de::Deserialize<'de> {}
```

## ProbeDeclaration

```rust
pub struct ProbeDeclaration {/* private */}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl ProbeDeclaration {
    pub const fn prefix_bytes(self) -> u64;
    pub const fn suffix_bytes(self) -> u64;
    pub const fn range_count(self) -> u32;
    pub const fn cumulative_bytes(self) -> u64;
}
impl ProbeDeclaration {
    pub const fn prefix_only(prefix_bytes: u64) -> Self;
    pub const fn new(input: ProbeDeclarationInput) -> Self;
}
```

## ProbeDeclarationInput

```rust
pub struct ProbeDeclarationInput {
    pub prefix_bytes: u64,
    pub suffix_bytes: u64,
    pub range_count: u32,
    pub cumulative_bytes: u64,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ValidationDeclaration

```rust
pub struct ValidationDeclaration {/* private */}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl ValidationDeclaration {
    pub const fn source_bytes(self) -> u64;
    pub const fn range_count(self) -> u32;
}
impl ValidationDeclaration {
    pub const fn new(source_bytes: u64, range_count: u32) -> Self;
}
```

## StreamingTextFallback

```rust
pub enum StreamingTextFallback {
    Disabled,
    Enabled,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ReadAccessPattern

```rust
pub enum ReadAccessPattern {
    Streaming { maximum_ranges: u32 },
    RandomAccess { maximum_ranges: u32 },
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ReadOutputKind

```rust
pub enum ReadOutputKind {
    Text,
    Structured,
    Image,
    Audio,
    File,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ReadViewBounds

```rust
pub enum ReadViewBounds {
    Text {
        source_bytes: u64,
        output_bytes: usize,
    },
    Structured {
        source_bytes: u64,
        output_bytes: usize,
        depth: u32,
        nodes: u64,
        string_bytes: usize,
    },
    Image {
        source_bytes: u64,
        width: u32,
        height: u32,
        pixels: u64,
        output_bytes: u64,
    },
    Audio {
        source_bytes: u64,
        channels: u16,
        sample_rate_hz: u32,
        duration_seconds: u32,
        output_bytes: u64,
    },
    File {
        source_bytes: u64,
        output_bytes: u64,
    },
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl ReadViewBounds {
    pub const fn output_kind(self) -> ReadOutputKind;
    pub const fn source_bytes(self) -> u64;
}
```

## ReadViewDeclaration

```rust
pub struct ReadViewDeclaration {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl ReadViewDeclaration {
    pub const fn name(&self) -> &ReadViewName;
    pub fn description(&self) -> &str;
    pub const fn arguments_schema(&self) -> &CanonicalJsonObjectSchema;
}
impl ReadViewDeclaration {
    pub fn try_new(
        name: ReadViewName,
        description: string::String,
        arguments_schema: CanonicalJsonObjectSchema,
        access: ReadAccessPattern,
        bounds: ReadViewBounds,
    ) -> result::Result<Self, RegistryDeclarationError>;
    pub const fn access(&self) -> ReadAccessPattern;
    pub const fn bounds(&self) -> ReadViewBounds;
    pub const fn output_kind(&self) -> ReadOutputKind;
}
```

## ReaderDeclaration

```rust
pub struct ReaderDeclaration {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl ReaderDeclaration {
    pub const fn identity(&self) -> &ReaderIdentity;
    pub fn media_types(&self) -> &[CanonicalMediaType];
    pub fn views(&self) -> &[ReadViewDeclaration];
    pub fn reason_codes(&self) -> &[ReasonCode];
}
impl ReaderDeclaration {
    pub fn try_new(input: ReaderDeclarationInput)
        -> result::Result<Self, RegistryDeclarationError>;
    pub const fn probe(&self) -> ProbeDeclaration;
    pub const fn validation(&self) -> ValidationDeclaration;
    pub const fn streaming_text_fallback(&self) -> StreamingTextFallback;
}
```

## ReaderDeclarationInput

```rust
pub struct ReaderDeclarationInput {
    pub provider: FileReaderProviderName,
    pub reader: FileReaderName,
    pub revision: FileReaderRevision,
    pub media_types: vec::Vec<CanonicalMediaType>,
    pub probe: ProbeDeclaration,
    pub validation: ValidationDeclaration,
    pub views: vec::Vec<ReadViewDeclaration>,
    pub reason_codes: vec::Vec<ReasonCode>,
    pub streaming_text_fallback: StreamingTextFallback,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## FileMediaProviderDeclaration

```rust
pub struct FileMediaProviderDeclaration {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl FileMediaProviderDeclaration {
    pub const fn provider(&self) -> &FileReaderProviderName;
    pub fn readers(&self) -> &[ReaderDeclaration];
}
impl FileMediaProviderDeclaration {
    pub fn try_new(
        provider: FileReaderProviderName,
        readers: vec::Vec<ReaderDeclaration>,
    ) -> result::Result<Self, RegistryDeclarationError>;
    pub fn try_new_with_container_entries(
        provider: FileReaderProviderName,
        readers: vec::Vec<ReaderDeclaration>,
        observed_container_entries: option::Option<u64>,
    ) -> result::Result<Self, RegistryDeclarationError>;
    pub const fn observed_container_entries(&self) -> option::Option<u64>;
}
```

## FileMediaProviderValidationRequest

```rust
pub struct FileMediaProviderValidationRequest {
    pub source: FileUse,
    pub media_type: CanonicalMediaType,
    pub evidence: ValidationEvidence,
    pub maximum_source_bytes: u64,
    pub maximum_ranges: u32,
    pub maximum_image_axis: u32,
    pub maximum_decoded_image_pixels: u64,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## FileMediaProviderReadRequest

```rust
pub struct FileMediaProviderReadRequest {
    pub source: FileUse,
    pub detected_media_type: CanonicalMediaType,
    pub validation: ValidationEvidence,
    pub metadata: BoundedMetadata,
    pub maximum_source_bytes: u64,
    pub view: ReadViewName,
    pub input: FileReadInput,
    pub maximum_image_axis: u32,
    pub maximum_decoded_image_pixels: u64,
    pub maximum_container_entries: u64,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## FileMediaProviderFailure

```rust
pub enum FileMediaProviderFailure {
    Failed,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for FileMediaProviderFailure {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for FileMediaProviderFailure {}
```

## FileMediaProviderFuture

```rust
pub type FileMediaProviderFuture<'a, Output> = pin::Pin<
    boxed::Box<
        dyn future::Future<Output = result::Result<Output, FileMediaProviderFailure>>
            + marker::Send
            + 'a,
    >,
>;
```

## FileMediaProvider

```rust
pub trait FileMediaProvider: marker::Send + marker::Sync {
    fn declaration(&self) -> FileMediaProviderDeclaration;
    fn probe<'a>(
        &'a self,
        reader: &'a ReaderIdentity,
        source: &'a dyn VerifiedBlobSource,
        cancellation: &'a dyn CancellationSignal,
    ) -> FileMediaProviderFuture<'a, ProcessorProbeOutput>;
    fn inspect<'a>(
        &'a self,
        reader: &'a ReaderIdentity,
        request: FileMediaProviderValidationRequest,
        source: &'a dyn VerifiedBlobSource,
        cancellation: &'a dyn CancellationSignal,
    ) -> FileMediaProviderFuture<'a, ProcessorValidationOutput>;
    fn read<'a>(
        &'a self,
        reader: &'a ReaderIdentity,
        request: FileMediaProviderReadRequest,
        source: &'a dyn VerifiedBlobSource,
        cancellation: &'a dyn CancellationSignal,
    ) -> FileMediaProviderFuture<'a, ProcessorReadOutput>;
}
```

## RegistryDeclarationError

```rust
pub enum RegistryDeclarationError {
    EmptyInventory,
    ForeignReader,
    Description,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for RegistryDeclarationError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for RegistryDeclarationError {}
```
