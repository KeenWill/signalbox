<!-- generated by scripts/render_domain_spine.py, do not edit -->

# detection

## SourceReadFuture

```rust
pub type SourceReadFuture<'a> = pin::Pin<
    boxed::Box<
        dyn future::Future<Output = result::Result<vec::Vec<u8>, SourceReadError>>
            + marker::Send
            + 'a,
    >,
>;
```

## VerifiedBlobSource

```rust
pub trait VerifiedBlobSource: marker::Send + marker::Sync {
    fn digest(&self) -> FileDigest;
    fn byte_length(&self) -> nonzero::NonZeroU64;
    fn read_range(&self, offset: u64, length: nonzero::NonZeroU64) -> SourceReadFuture<'_>;
}
```

## SourceReadError

```rust
pub enum SourceReadError {
    Missing,
    Corrupt,
    Unavailable,
    RangeOutOfBounds,
    Integrity,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for SourceReadError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for SourceReadError {}
```

## CancellationSignal

```rust
pub trait CancellationSignal: marker::Send + marker::Sync {
    fn is_cancelled(&self) -> bool;
}
```

## NeverCancelled

```rust
pub struct NeverCancelled;
// derives: clone::Clone, marker::Copy, fmt::Debug, default::Default
impl CancellationSignal for NeverCancelled {
    fn is_cancelled(&self) -> bool;
}
```

## InspectionRequest

```rust
pub struct InspectionRequest {
    pub source: FileUse,
    pub visible_part: option::Option<VisiblePartSelector>,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ValidationEvidence

```rust
pub enum ValidationEvidence {
    StrongSignature,
    StructuralValidation,
    DeclaredCandidateStructurallyValidated,
    StreamingTextValidation,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, de::Deserialize<'de>, ser::Serialize
```

## ValidatedFile

```rust
pub struct ValidatedFile {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl ValidatedFile {
    pub const fn source(&self) -> &FileUse;
    pub const fn detected_media_type(&self) -> &CanonicalMediaType;
    pub const fn reader(&self) -> &ReaderIdentity;
    pub const fn metadata(&self) -> &BoundedMetadata;
    pub fn views(&self) -> &[ReadViewDeclaration];
}
impl ValidatedFile {
    pub const fn validation(&self) -> ValidationEvidence;
}
```

## FileInspectionStatus

```rust
pub enum FileInspectionStatus {
    Validated,
    Unknown,
    Malformed,
    DeclaredMismatch,
    EncryptedOrLocked,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## FileInspection

```rust
pub enum FileInspection {
    Validated(ValidatedFile),
    Unknown {
        source: FileUse,
    },
    Malformed {
        source: FileUse,
        media_type: CanonicalMediaType,
        reason_code: ReasonCode,
    },
    DeclaredMismatch {
        source: FileUse,
        declared: CanonicalMediaType,
        detected: CanonicalMediaType,
    },
    EncryptedOrLocked {
        source: FileUse,
        media_type: CanonicalMediaType,
    },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl FileInspection {
    pub const fn status(&self) -> FileInspectionStatus;
    pub const fn source(&self) -> &FileUse;
}
```

## FileReadRequest

```rust
pub struct FileReadRequest {
    pub inspection: InspectionRequest,
    pub view: ReadViewName,
    pub input: FileReadInput,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## FileReadInput

```rust
pub enum FileReadInput {
    Initial { options: value::Value },
    Continuation { cursor: ReadContinuationCursor },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ReadContinuation

```rust
pub enum ReadContinuation {
    Complete,
    More { cursor: ReadContinuationCursor },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## FileReadResult

```rust
pub enum FileReadResult {
    Reference(FileMediaReference),
    Text {
        body: string::String,
        continuation: ReadContinuation,
    },
    Structured {
        body: value::Value,
        continuation: ReadContinuation,
    },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ProcessorProbeOutput

```rust
pub enum ProcessorProbeOutput {
    NoMatch,
    Candidate {
        media_type: string::String,
        strength: ProbeStrength,
        evidence_bytes: u64,
    },
    RecognizedMalformed {
        media_type: string::String,
        reason_code: string::String,
    },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, de::Deserialize<'de>, ser::Serialize
```

## ProcessorValidationOutput

```rust
pub enum ProcessorValidationOutput {
    Validated {
        media_type: string::String,
        evidence: ValidationEvidence,
        metadata_json: string::String,
    },
    Malformed {
        media_type: string::String,
        reason_code: string::String,
    },
    EncryptedOrLocked {
        media_type: string::String,
    },
    NoMatch,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, de::Deserialize<'de>, ser::Serialize
```

## ProcessorReadOutput

```rust
pub enum ProcessorReadOutput {
    ImageDescription,
    GeneratedImage {
        media_type: string::String,
        provider: string::String,
        reader: string::String,
        revision: string::String,
        byte_length: u64,
        bytes: vec::Vec<u8>,
    },
    DirectReference {
        media_type: string::String,
    },
    Text {
        body: string::String,
        truncated: bool,
        cursor: option::Option<string::String>,
    },
    Structured {
        body_json: string::String,
        truncated: bool,
        cursor: option::Option<string::String>,
    },
    InvalidViewArguments,
    UnsupportedView,
    SourceTooLarge {
        maximum_bytes: u64,
    },
    ExpansionLimitExceeded {
        limit_kind: string::String,
    },
    OutputUnitTooLarge,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, de::Deserialize<'de>, ser::Serialize
```

## ProcessorFailure

```rust
pub enum ProcessorFailure {
    Unavailable,
    Failed,
    TimedOut,
    Cancelled,
    Protocol,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, de::Deserialize<'de>, ser::Serialize
impl fmt::Display for ProcessorFailure {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for ProcessorFailure {}
```

## ProcessorBoundaryFailure

```rust
pub enum ProcessorBoundaryFailure {
    Processor(ProcessorFailure),
    Source(SourceReadError),
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for ProcessorBoundaryFailure {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for ProcessorBoundaryFailure {}
impl convert::From<ProcessorFailure> for ProcessorBoundaryFailure {
    fn from(value: ProcessorFailure) -> Self;
}
impl convert::From<SourceReadError> for ProcessorBoundaryFailure {
    fn from(value: SourceReadError) -> Self;
}
```

## FileMediaProcessorFuture

```rust
pub type FileMediaProcessorFuture<'a, Output> = pin::Pin<
    boxed::Box<
        dyn future::Future<Output = result::Result<Output, ProcessorBoundaryFailure>>
            + marker::Send
            + 'a,
    >,
>;
```

## FileMediaProcessor

```rust
pub trait FileMediaProcessor: marker::Send + marker::Sync {
    fn probe<'a>(
        &'a self,
        reader: &'a ReaderIdentity,
        source: &'a dyn VerifiedBlobSource,
        cancellation: &'a dyn CancellationSignal,
    ) -> FileMediaProcessorFuture<'a, ProcessorProbeOutput>;
    fn validate<'a>(
        &'a self,
        reader: &'a ReaderIdentity,
        request: FileMediaProviderValidationRequest,
        source: &'a dyn VerifiedBlobSource,
        cancellation: &'a dyn CancellationSignal,
    ) -> FileMediaProcessorFuture<'a, ProcessorValidationOutput>;
    fn read<'a>(
        &'a self,
        reader: &'a ReaderIdentity,
        request: FileMediaProviderReadRequest,
        source: &'a dyn VerifiedBlobSource,
        cancellation: &'a dyn CancellationSignal,
    ) -> FileMediaProcessorFuture<'a, ProcessorReadOutput>;
}
```

## FileMediaFailure

```rust
pub enum FileMediaFailure {
    SourceIntegrity,
    BlobNotVisible,
    BlobMissing,
    BlobCorrupt,
    BlobUnavailable,
    UnknownType,
    DeclaredTypeMismatch {
        declared: CanonicalMediaType,
        detected: CanonicalMediaType,
    },
    Malformed {
        media_type: CanonicalMediaType,
        reason_code: ReasonCode,
    },
    EncryptedOrLocked {
        media_type: CanonicalMediaType,
    },
    UnsupportedView,
    InvalidViewArguments,
    SourceTooLarge {
        maximum_bytes: u64,
    },
    ExpansionLimitExceeded {
        limit_kind: ReasonCode,
    },
    OutputUnitTooLarge,
    ProcessorUnavailable,
    ProcessorFailed,
    ProcessorTimedOut,
    Cancelled,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for FileMediaFailure {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for FileMediaFailure {}
impl convert::From<ProcessorFailure> for FileMediaFailure {
    fn from(value: ProcessorFailure) -> Self;
}
impl convert::From<ProcessorBoundaryFailure> for FileMediaFailure {
    fn from(value: ProcessorBoundaryFailure) -> Self;
}
impl convert::From<SourceReadError> for FileMediaFailure {
    fn from(value: SourceReadError) -> Self;
}
```
