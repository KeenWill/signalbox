<!-- generated by scripts/render_domain_spine.py, do not edit -->

# registry

## MAX_READERS_PER_PROVIDER

```rust
pub const MAX_READERS_PER_PROVIDER: usize;
```

## MAX_REGISTRY_READERS

```rust
pub const MAX_REGISTRY_READERS: usize;
```

## FileMediaRegistry

```rust
pub struct FileMediaRegistry {/* private */}
// derives: clone::Clone, fmt::Debug
impl FileMediaRegistry {
    pub fn providers(&self) -> &[FileMediaProviderDeclaration];
}
impl FileMediaRegistry {
    pub fn try_new(
        providers: vec::Vec<FileMediaProviderDeclaration>,
        ceilings: FileMediaCeilings,
        isolation: ProcessorIsolation,
    ) -> result::Result<Self, FileMediaRegistryConstructionError>;
    pub fn empty() -> Self;
    pub const fn ceilings(&self) -> FileMediaCeilings;
    pub async fn inspect(
        &self,
        processor: &dyn FileMediaProcessor,
        request: InspectionRequest,
        source: &dyn VerifiedBlobSource,
        cancellation: &dyn CancellationSignal,
    ) -> result::Result<FileInspection, FileMediaFailure>;
    pub async fn read(
        &self,
        processor: &dyn FileMediaProcessor,
        request: FileReadRequest,
        source: &dyn VerifiedBlobSource,
        cancellation: &dyn CancellationSignal,
    ) -> result::Result<FileReadResult, FileMediaFailure>;
    pub async fn read_with_reader(
        &self,
        processor: &dyn FileMediaProcessor,
        request: FileReadRequest,
        source: &dyn VerifiedBlobSource,
        cancellation: &dyn CancellationSignal,
        expected_reader: option::Option<&ReaderIdentity>,
    ) -> result::Result<(ReaderIdentity, FileReadResult), FileMediaFailure>;
}
```

## ProcessorIsolation

```rust
pub enum ProcessorIsolation {
    Available,
    Unavailable,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## read_options_fit

```rust
pub fn read_options_fit(options: &value::Value) -> bool;
```

## provider_declaration_inventory_fits

```rust
pub fn provider_declaration_inventory_fits<'a>(
    providers: impl collect::IntoIterator<Item = &'a FileMediaProviderDeclaration>,
) -> bool;
```

## FileMediaRegistryConstructionError

```rust
pub enum FileMediaRegistryConstructionError {
    Ceilings,
    Inventory,
    IsolationUnavailable,
    DuplicateProvider,
    DuplicateReader,
    DuplicateMediaTypeClaim,
    DuplicateReaderMember,
    ProbeBounds,
    ValidationBounds,
    ViewBounds,
    ContainerBounds,
    TextFallback,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for FileMediaRegistryConstructionError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for FileMediaRegistryConstructionError {}
```
