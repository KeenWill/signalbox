<!-- generated by scripts/render_domain_spine.py, do not edit -->

# value

## FileDigest

```rust
pub struct FileDigest(/* private */);
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl FileDigest {
    pub const fn as_bytes(&self) -> &[u8; 32];
}
impl FileDigest {
    pub const fn from_bytes(bytes: [u8; 32]) -> Self;
}
impl fmt::Display for FileDigest {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl traits::FromStr for FileDigest {
    type Err = RegistryValueError;
    fn from_str(value: &str) -> result::Result<Self, <Self as traits::FromStr>::Err>;
}
```

## AttachmentKind

```rust
pub enum AttachmentKind {
    Image,
    Document,
    File,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
```

## DeclaredMediaType

```rust
pub struct DeclaredMediaType(/* private */);
// derives: clone::Clone, cmp::Eq, hash::Hash, cmp::PartialEq
impl DeclaredMediaType {
    pub fn as_str(&self) -> &str;
}
impl DeclaredMediaType {
    pub fn try_new(
        value: impl convert::Into<sync::Arc<str>>,
    ) -> result::Result<Self, RegistryValueError>;
    pub fn canonical_essence(&self) -> result::Result<CanonicalMediaType, MediaTypeParseError>;
}
impl fmt::Debug for DeclaredMediaType {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
```

## DisplayFilename

```rust
pub struct DisplayFilename(/* private */);
// derives: clone::Clone, cmp::Eq, hash::Hash, cmp::PartialEq
impl DisplayFilename {
    pub fn as_str(&self) -> &str;
}
impl DisplayFilename {
    pub fn try_new(
        value: impl convert::Into<sync::Arc<str>>,
    ) -> result::Result<Self, RegistryValueError>;
}
impl fmt::Debug for DisplayFilename {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
```

## FileUse

```rust
pub struct FileUse {/* private */}
// derives: clone::Clone, cmp::Eq, cmp::PartialEq
impl FileUse {
    pub const fn declared_media_type(&self) -> &DeclaredMediaType;
}
impl FileUse {
    pub const fn new(
        digest: FileDigest,
        byte_length: nonzero::NonZeroU64,
        attachment_kind: AttachmentKind,
        declared_media_type: DeclaredMediaType,
        display_filename: option::Option<DisplayFilename>,
    ) -> Self;
    pub const fn digest(&self) -> FileDigest;
    pub const fn byte_length(&self) -> nonzero::NonZeroU64;
    pub const fn attachment_kind(&self) -> AttachmentKind;
    pub const fn display_filename(&self) -> option::Option<&DisplayFilename>;
}
impl fmt::Debug for FileUse {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
```

## CanonicalMediaType

```rust
pub struct CanonicalMediaType(/* private */);
// derives: clone::Clone, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl CanonicalMediaType {
    pub fn as_str(&self) -> &str;
}
impl fmt::Display for CanonicalMediaType {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl traits::FromStr for CanonicalMediaType {
    type Err = MediaTypeParseError;
    fn from_str(value: &str) -> result::Result<Self, <Self as traits::FromStr>::Err>;
}
```

## MediaTypeParseError

```rust
pub struct MediaTypeParseError;
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for MediaTypeParseError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for MediaTypeParseError {}
```

## FileReaderProviderName

```rust
pub struct FileReaderProviderName(/* private */);
// derives: clone::Clone, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl FileReaderProviderName {
    pub fn try_new(
        value: impl convert::Into<sync::Arc<str>>,
    ) -> result::Result<Self, RegistryValueError>;
    pub fn as_str(&self) -> &str;
}
impl fmt::Display for FileReaderProviderName {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
```

## FileReaderName

```rust
pub struct FileReaderName(/* private */);
// derives: clone::Clone, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl FileReaderName {
    pub fn try_new(
        value: impl convert::Into<sync::Arc<str>>,
    ) -> result::Result<Self, RegistryValueError>;
    pub fn as_str(&self) -> &str;
}
impl fmt::Display for FileReaderName {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
```

## ReadViewName

```rust
pub struct ReadViewName(/* private */);
// derives: clone::Clone, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl ReadViewName {
    pub fn try_new(
        value: impl convert::Into<sync::Arc<str>>,
    ) -> result::Result<Self, RegistryValueError>;
    pub fn as_str(&self) -> &str;
}
impl fmt::Display for ReadViewName {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
```

## ReasonCode

```rust
pub struct ReasonCode(/* private */);
// derives: clone::Clone, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl ReasonCode {
    pub fn try_new(
        value: impl convert::Into<sync::Arc<str>>,
    ) -> result::Result<Self, RegistryValueError>;
    pub fn as_str(&self) -> &str;
}
impl fmt::Display for ReasonCode {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
```

## FileReaderRevision

```rust
pub struct FileReaderRevision(/* private */);
// derives: clone::Clone, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl FileReaderRevision {
    pub fn as_str(&self) -> &str;
}
impl FileReaderRevision {
    pub fn try_new(
        value: impl convert::Into<sync::Arc<str>>,
    ) -> result::Result<Self, RegistryValueError>;
}
```

## ReaderIdentity

```rust
pub struct ReaderIdentity {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl ReaderIdentity {
    pub const fn provider(&self) -> &FileReaderProviderName;
    pub const fn reader(&self) -> &FileReaderName;
    pub const fn revision(&self) -> &FileReaderRevision;
}
impl ReaderIdentity {
    pub const fn new(
        provider: FileReaderProviderName,
        reader: FileReaderName,
        revision: FileReaderRevision,
    ) -> Self;
}
```

## CanonicalJsonObjectSchema

```rust
pub struct CanonicalJsonObjectSchema {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl CanonicalJsonObjectSchema {
    pub fn as_str(&self) -> &str;
    pub const fn value(&self) -> &value::Value;
}
impl CanonicalJsonObjectSchema {
    pub fn try_new(value: &str) -> result::Result<Self, RegistryValueError>;
}
```

## BoundedMetadata

```rust
pub struct BoundedMetadata {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl BoundedMetadata {
    pub fn as_str(&self) -> &str;
    pub const fn value(&self) -> &value::Value;
}
impl BoundedMetadata {
    pub fn try_new(value: &str) -> result::Result<Self, RegistryValueError>;
}
```

## parse_json_without_duplicate_members

```rust
pub fn parse_json_without_duplicate_members(
    value: &str,
) -> result::Result<value::Value, error::Error>;
```

## JsonParseLimits

```rust
pub struct JsonParseLimits {
    pub maximum_nodes: u64,
    pub maximum_container_entries: u64,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## parse_json_without_duplicate_members_bounded

```rust
pub fn parse_json_without_duplicate_members_bounded(
    value: &str,
    limits: JsonParseLimits,
) -> result::Result<value::Value, error::Error>;
```

## VisiblePartSelector

```rust
pub struct VisiblePartSelector(/* private */);
// derives: clone::Clone, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
impl VisiblePartSelector {
    pub fn as_str(&self) -> &str;
}
impl VisiblePartSelector {
    pub fn try_new(
        value: impl convert::Into<sync::Arc<str>>,
    ) -> result::Result<Self, RegistryValueError>;
}
```

## ReadContinuationCursor

```rust
pub struct ReadContinuationCursor(/* private */);
// derives: clone::Clone, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
impl ReadContinuationCursor {
    pub fn as_str(&self) -> &str;
}
impl ReadContinuationCursor {
    pub fn try_new(
        value: impl convert::Into<sync::Arc<str>>,
    ) -> result::Result<Self, RegistryValueError>;
    pub fn into_string(self) -> string::String;
}
```

## RegistryValueError

```rust
pub enum RegistryValueError {
    Digest,
    DeclaredMediaType,
    DisplayFilename,
    Name,
    Revision,
    Schema,
    Metadata,
    Selector,
    Continuation,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for RegistryValueError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for RegistryValueError {}
```
