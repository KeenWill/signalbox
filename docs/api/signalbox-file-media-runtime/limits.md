<!-- generated by scripts/render_domain_spine.py, do not edit -->

# limits

## MAX_PROBE_PREFIX_BYTES

```rust
pub const MAX_PROBE_PREFIX_BYTES: u64;
```

## MAX_PROBE_SUFFIX_BYTES

```rust
pub const MAX_PROBE_SUFFIX_BYTES: u64;
```

## MAX_PROBE_RANGES

```rust
pub const MAX_PROBE_RANGES: u32;
```

## MAX_PROBE_CUMULATIVE_BYTES

```rust
pub const MAX_PROBE_CUMULATIVE_BYTES: u64;
```

## MAX_VALIDATION_SOURCE_BYTES

```rust
pub const MAX_VALIDATION_SOURCE_BYTES: u64;
```

## MAX_VALIDATION_RANGES

```rust
pub const MAX_VALIDATION_RANGES: u32;
```

## MAX_READ_SOURCE_BYTES

```rust
pub const MAX_READ_SOURCE_BYTES: u64;
```

## MAX_READ_RANGES

```rust
pub const MAX_READ_RANGES: u32;
```

## MAX_PROCESSOR_FRAME_BYTES

```rust
pub const MAX_PROCESSOR_FRAME_BYTES: usize;
```

## MAX_READ_OPTIONS_BYTES

```rust
pub const MAX_READ_OPTIONS_BYTES: usize;
```

## MAX_TEXT_OR_JSON_BYTES

```rust
pub const MAX_TEXT_OR_JSON_BYTES: usize;
```

## MAX_TEXT_BODY_BYTES

```rust
pub const MAX_TEXT_BODY_BYTES: usize;
```

## MAX_STRUCTURED_DEPTH

```rust
pub const MAX_STRUCTURED_DEPTH: u32;
```

## MAX_STRUCTURED_NODES

```rust
pub const MAX_STRUCTURED_NODES: u64;
```

## MAX_OBSERVED_CONTAINER_ENTRIES

```rust
pub const MAX_OBSERVED_CONTAINER_ENTRIES: u64;
```

## MAX_IMAGE_AXIS

```rust
pub const MAX_IMAGE_AXIS: u32;
```

## MAX_DECODED_IMAGE_PIXELS

```rust
pub const MAX_DECODED_IMAGE_PIXELS: u64;
```

## MAX_PRESENTED_IMAGE_BYTES

```rust
pub const MAX_PRESENTED_IMAGE_BYTES: u64;
```

## MAX_AUDIO_CHANNELS

```rust
pub const MAX_AUDIO_CHANNELS: u16;
```

## MAX_AUDIO_SAMPLE_RATE_HZ

```rust
pub const MAX_AUDIO_SAMPLE_RATE_HZ: u32;
```

## MAX_AUDIO_CLIP_SECONDS

```rust
pub const MAX_AUDIO_CLIP_SECONDS: u32;
```

## MAX_PRESENTED_AUDIO_BYTES

```rust
pub const MAX_PRESENTED_AUDIO_BYTES: u64;
```

## MAX_PRESENTED_FILE_BYTES

```rust
pub const MAX_PRESENTED_FILE_BYTES: u64;
```

## MAX_MEDIA_REFERENCES_PER_CALL

```rust
pub const MAX_MEDIA_REFERENCES_PER_CALL: u16;
```

## MAX_AGGREGATE_MEDIA_BYTES_PER_CALL

```rust
pub const MAX_AGGREGATE_MEDIA_BYTES_PER_CALL: u64;
```

## MAX_WORKER_MEMORY_BYTES

```rust
pub const MAX_WORKER_MEMORY_BYTES: u64;
```

## MAX_WORKER_CPU_SECONDS

```rust
pub const MAX_WORKER_CPU_SECONDS: u64;
```

## MAX_WORKER_WALL_SECONDS

```rust
pub const MAX_WORKER_WALL_SECONDS: u64;
```

## MAX_WORKER_DESCENDANTS

```rust
pub const MAX_WORKER_DESCENDANTS: u32;
```

## MAX_WORKER_TASKS

```rust
pub const MAX_WORKER_TASKS: u64;
```

## MAX_WORKER_FILE_DESCRIPTORS

```rust
pub const MAX_WORKER_FILE_DESCRIPTORS: u64;
```

## MIN_WORKER_FILE_DESCRIPTORS

```rust
pub const MIN_WORKER_FILE_DESCRIPTORS: u64;
```

## MAX_WORKER_STDERR_BYTES

```rust
pub const MAX_WORKER_STDERR_BYTES: usize;
```

## FileMediaCeilings

```rust
pub struct FileMediaCeilings {
    pub probe_prefix_bytes: u64,
    pub probe_suffix_bytes: u64,
    pub probe_ranges: u32,
    pub probe_cumulative_bytes: u64,
    pub validation_source_bytes: u64,
    pub validation_ranges: u32,
    pub read_source_bytes: u64,
    pub read_ranges: u32,
    pub text_or_json_bytes: usize,
    pub structured_depth: u32,
    pub structured_nodes: u64,
    pub observed_container_entries: u64,
    pub image_axis: u32,
    pub decoded_image_pixels: u64,
    pub presented_image_bytes: u64,
    pub audio_channels: u16,
    pub audio_sample_rate_hz: u32,
    pub audio_clip_seconds: u32,
    pub presented_audio_bytes: u64,
    pub presented_file_bytes: u64,
    pub media_references_per_call: u16,
    pub aggregate_media_bytes_per_call: u64,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl FileMediaCeilings {
    pub const fn version_one() -> Self;
    pub const fn admits(self, candidate: Self) -> bool;
}
impl default::Default for FileMediaCeilings {
    fn default() -> Self;
}
```

## FileMediaProcessLimitOverrides

```rust
pub struct FileMediaProcessLimitOverrides {
    pub memory_bytes: u64,
    pub cpu_seconds: u64,
    pub wall_seconds: u64,
    pub file_descriptors: u64,
    pub stderr_bytes: usize,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## FileMediaProcessCeilings

```rust
pub struct FileMediaProcessCeilings {/* private */}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl FileMediaProcessCeilings {
    pub const fn frame_bytes(self) -> usize;
    pub const fn memory_bytes(self) -> u64;
    pub const fn cpu_seconds(self) -> u64;
    pub const fn wall_seconds(self) -> u64;
    pub const fn file_descriptors(self) -> u64;
    pub const fn stderr_bytes(self) -> usize;
}
impl FileMediaProcessCeilings {
    pub const fn version_one() -> Self;
    pub const fn try_lower(overrides: FileMediaProcessLimitOverrides) -> option::Option<Self>;
    pub const fn admits(self, candidate: Self) -> bool;
}
impl default::Default for FileMediaProcessCeilings {
    fn default() -> Self;
}
```
