<!-- generated by scripts/render_domain_spine.py, do not edit -->

# credential

## CredentialReference

```rust
pub struct CredentialReference(/* private */);
// derives: fmt::Debug, clone::Clone, cmp::PartialEq, cmp::Eq, hash::Hash
impl CredentialReference {
    pub fn new(value: impl convert::Into<string::String>) -> Self;
    pub fn as_str(&self) -> &str;
}
impl fmt::Display for CredentialReference {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result;
}
```

## CredentialValue

```rust
pub struct CredentialValue(/* private */);
// derives: clone::Clone, cmp::PartialEq, cmp::Eq
impl CredentialValue {
    pub fn new(value: impl convert::Into<vec::Vec<u8>>) -> Self;
    pub fn expose_bytes(&self) -> &[u8];
}
impl fmt::Debug for CredentialValue {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result;
}
```

## CredentialAccessFailure

```rust
pub enum CredentialAccessFailure {
    Unmapped,
    Unavailable,
    Unreadable,
    InvalidUtf8,
    NotRegularFile,
    WrongOwner,
    InsecurePermissions,
    TooLarge,
    OauthTupleMismatch,
    OauthRefreshAmbiguous,
    OauthRefreshRejected,
    OauthIdentityChanged,
    OauthCredentialHome,
}
// derives: fmt::Debug, clone::Clone, marker::Copy, cmp::PartialEq, cmp::Eq
```

## CredentialAccessError

```rust
pub struct CredentialAccessError {
    pub reference: CredentialReference,
    pub failure: CredentialAccessFailure,
}
// derives: fmt::Debug, clone::Clone, cmp::PartialEq, cmp::Eq
impl CredentialAccessError {
    pub fn new(reference: CredentialReference, failure: CredentialAccessFailure) -> Self;
}
impl fmt::Display for CredentialAccessError {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for CredentialAccessError {}
```

## CredentialAccess

```rust
pub trait CredentialAccess: marker::Send + marker::Sync {
    fn resolve(
        &self,
        reference: &CredentialReference,
    ) -> impl future::Future<Output = result::Result<CredentialValue, CredentialAccessError>>
           + marker::Send;
}
```
