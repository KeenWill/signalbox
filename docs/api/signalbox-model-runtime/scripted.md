<!-- generated by scripts/render_domain_spine.py, do not edit -->

# scripted

## Script

```rust
pub struct Script {
    pub observations: vec::Vec<ObservationFact>,
    pub terminal: TerminalEvidence,
}
// derives: fmt::Debug, clone::Clone, cmp::PartialEq
impl<T> dyn_clone::DynClone for Script
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl Script {
    pub fn delivering(terminal: TerminalEvidence) -> Self;
    #[must_use]
    pub fn observing(self, fact: ObservationFact) -> Self;
}
```

## ScriptedModel

```rust
pub struct ScriptedModel<C> {/* private */}
// derives: clone::Clone, fmt::Debug
impl<T> dyn_clone::DynClone for ScriptedModel<C>
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<C> ScriptedModel<C> {
    pub fn following(scripts: impl collect::IntoIterator<Item = Script>) -> Self;
    pub fn single(script: Script) -> Self;
    pub fn received_operations(&self) -> vec::Vec<ModelOperation<C>>
    where
        C: clone::Clone;
}
impl<C: clone::Clone + marker::Send + marker::Sync> ModelRuntime<C> for ScriptedModel<C> {
    type Prepared = ScriptedPrepared<C>;
    async fn prepare(
        &self,
        operation: ModelOperation<C>,
        _cancellation: CancellationSignal,
    ) -> PreparationOutcome<C, <Self as ModelRuntime>::Prepared>;
    async fn execute(
        &self,
        prepared: <Self as ModelRuntime>::Prepared,
        sink: &mut (dyn ObservationSink<C> + marker::Send),
        _cancellation: CancellationSignal,
    ) -> TerminalReport<C>;
}
```

## ScriptedPrepared

```rust
pub struct ScriptedPrepared<C> {/* private */}
```
