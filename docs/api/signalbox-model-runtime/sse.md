<!-- generated by scripts/render_domain_spine.py, do not edit -->

# sse

## SseRecord

```rust
pub struct SseRecord {
    pub event: option::Option<string::String>,
    pub data: string::String,
}
// derives: fmt::Debug, clone::Clone, cmp::PartialEq, cmp::Eq
impl<T> dyn_clone::DynClone for SseRecord
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
```

## SseFramingError

```rust
pub enum SseFramingError {
    RecordTooLarge { limit: usize },
    InvalidUtf8 { detail: string::String },
}
// derives: fmt::Debug, clone::Clone, cmp::PartialEq, cmp::Eq
impl<T> dyn_clone::DynClone for SseFramingError
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl fmt::Display for SseFramingError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for SseFramingError {}
```

## SsePushOutcome

```rust
pub struct SsePushOutcome {
    pub records: vec::Vec<SseRecord>,
    pub error: option::Option<SseFramingError>,
}
// derives: fmt::Debug, clone::Clone, cmp::PartialEq, cmp::Eq
impl<T> dyn_clone::DynClone for SsePushOutcome
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
```

## SseTermination

```rust
pub enum SseTermination {
    Clean,
    TruncatedRecord,
}
// derives: fmt::Debug, clone::Clone, cmp::PartialEq, cmp::Eq
impl<T> dyn_clone::DynClone for SseTermination
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
```

## SseFraming

```rust
pub struct SseFraming {/* private */}
impl fmt::Debug for SseFraming {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl SseFraming {
    pub fn new(record_limit: usize) -> Self;
    pub fn push(&mut self, chunk: &[u8]) -> SsePushOutcome;
    pub fn holds_unframed_bytes(&self) -> bool;
    pub fn finish(self) -> SseTermination;
}
```
