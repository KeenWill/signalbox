<!-- generated by scripts/render_domain_spine.py, do not edit -->

# document

## DocumentReference

```rust
pub struct DocumentReference {
    pub authority: string::String,
    pub digest: [u8; 32],
    pub byte_length: nonzero::NonZeroU64,
    pub media_type: string::String,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## DocumentInput

```rust
pub struct DocumentInput {
    pub media_type: string::String,
    pub bytes: sync::Arc<[u8]>,
}
// derives: clone::Clone, cmp::Eq, cmp::PartialEq
impl fmt::Debug for DocumentInput {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
```

## DocumentPresentationCapability

```rust
pub struct DocumentPresentationCapability {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl DocumentPresentationCapability {
    pub fn new(
        media_types: set::BTreeSet<string::String>,
        maximum_document_bytes: nonzero::NonZeroU64,
        maximum_request_bytes: nonzero::NonZeroUsize,
        envelope_bytes: usize,
    ) -> Self;
    pub fn limited_by(self, document_bytes: u64, request_bytes: usize) -> Self;
    pub fn admits(&self, media_type: &str, byte_length: u64) -> bool;
    pub const fn maximum_request_bytes(&self) -> usize;
    pub const fn maximum_document_bytes(&self) -> u64;
}
```

## document_request_byte_limit

```rust
pub fn document_request_byte_limit<C>(
    operation: &ModelOperation<C>,
    adapter: &DocumentPresentationCapability,
) -> result::Result<option::Option<usize>, PreparationFailure>;
```
