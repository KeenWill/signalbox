<!-- generated by scripts/render_domain_spine.py, do not edit -->

# provider_json

## PROVIDER_JSON_NESTING_LIMIT

```rust
pub const PROVIDER_JSON_NESTING_LIMIT: usize;
```

## ProviderJsonNestingExceeded

```rust
pub struct ProviderJsonNestingExceeded;
// derives: fmt::Debug, clone::Clone, marker::Copy, cmp::PartialEq, cmp::Eq
impl<T> dyn_clone::DynClone for ProviderJsonNestingExceeded
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl fmt::Display for ProviderJsonNestingExceeded {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for ProviderJsonNestingExceeded {}
```

## ProviderJsonNestingValidator

```rust
pub struct ProviderJsonNestingValidator {/* private */}
// derives: fmt::Debug, default::Default
impl ProviderJsonNestingValidator {
    #[must_use]
    pub fn new() -> Self;
    pub fn validate_fragment(
        &mut self,
        bytes: &[u8],
    ) -> result::Result<(), ProviderJsonNestingExceeded>;
}
```

## validate_provider_json_nesting

```rust
pub fn validate_provider_json_nesting(
    bytes: &[u8],
) -> result::Result<(), ProviderJsonNestingExceeded>;
```

## provider_json_has_duplicate_members

```rust
pub fn provider_json_has_duplicate_members(
    text: &str,
) -> result::Result<bool, ProviderJsonNestingExceeded>;
```
