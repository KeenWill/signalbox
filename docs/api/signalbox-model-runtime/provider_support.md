<!-- generated by scripts/render_domain_spine.py, do not edit -->

# provider_support

## MAX_BUFFERED_PROVIDER_RESPONSE_BYTES

```rust
pub const MAX_BUFFERED_PROVIDER_RESPONSE_BYTES: usize;
```

## MAX_STREAMED_PROVIDER_RESPONSE_BYTES

```rust
pub const MAX_STREAMED_PROVIDER_RESPONSE_BYTES: usize;
```

## ResponsePrefixBudget

```rust
pub enum ResponsePrefixBudget {
    Accepted { len: usize },
    Overflowed { accepted_len: usize },
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> dyn_clone::DynClone for ResponsePrefixBudget
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
```

## provider_response_prefix_len

```rust
pub fn provider_response_prefix_len(current: usize, chunk: usize) -> ResponsePrefixBudget;
```

## provider_response_body_too_large

```rust
pub fn provider_response_body_too_large() -> LossCause;
```

## serialize_provider_request

```rust
pub fn serialize_provider_request(
    value: &impl ser::Serialize,
) -> result::Result<vec::Vec<u8>, PreparationDefect>;
```

## transport_facts_from_error

```rust
pub fn transport_facts_from_error(error: &(impl error::Error + ?marker::Sized)) -> TransportFacts;
```

## emit_provider_observation

```rust
pub fn emit_provider_observation<C: clone::Clone>(
    correlation: &C,
    sink: &mut (dyn ObservationSink<C> + marker::Send),
    fact: ObservationFact,
);
```

## proven_unsent_evidence

```rust
pub fn proven_unsent_evidence(cause: UnsentCause) -> TerminalEvidence;
```

## pre_exchange_loss_evidence

```rust
pub fn pre_exchange_loss_evidence(cause: LossCause) -> TerminalEvidence;
```

## boundary_loss_evidence

```rust
pub fn boundary_loss_evidence(cause: LossCause, exchange: ExchangeFacts) -> TerminalEvidence;
```
