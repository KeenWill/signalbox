<!-- generated by scripts/render_domain_spine.py, do not edit -->

# capability

## FastModeTarget

```rust
pub enum FastModeTarget {
    SameTarget,
    Mapped(ResolvedTarget),
}
// derives: fmt::Debug, clone::Clone, cmp::PartialEq, cmp::Eq
```

## ModelCapabilities

```rust
pub struct ModelCapabilities {/* private */}
// derives: fmt::Debug, clone::Clone, cmp::PartialEq, cmp::Eq
impl ModelCapabilities {
    pub const fn new(
        reasoning_levels: set::BTreeSet<ReasoningLevel>,
        fast_mode: option::Option<FastModeTarget>,
        service_tiers: set::BTreeSet<ServiceTier>,
    ) -> Self;
    pub fn with_reasoning_replay_family(self, family: option::Option<string::String>) -> Self;
    pub fn reasoning_replay_family(&self) -> option::Option<&str>;
    pub const fn reasoning_levels(&self) -> &set::BTreeSet<ReasoningLevel>;
    pub const fn fast_mode(&self) -> option::Option<&FastModeTarget>;
    pub const fn service_tiers(&self) -> &set::BTreeSet<ServiceTier>;
    pub fn effective_target<'a>(
        &'a self,
        selected: &'a ResolvedTarget,
        fast_mode: FastMode,
        retained_mapped_target: option::Option<&'a ResolvedTarget>,
    ) -> result::Result<(&'a ResolvedTarget, FastMode), ModelCapabilityError>;
}
```

## ModelCapabilityDefinition

```rust
pub struct ModelCapabilityDefinition {/* private */}
// derives: fmt::Debug, clone::Clone, cmp::PartialEq, cmp::Eq
impl ModelCapabilityDefinition {
    pub const fn new(target: ResolvedTarget, capabilities: ModelCapabilities) -> Self;
    pub const fn target(&self) -> &ResolvedTarget;
    pub const fn capabilities(&self) -> &ModelCapabilities;
}
```

## ModelCapabilityCatalog

```rust
pub struct ModelCapabilityCatalog {/* private */}
// derives: fmt::Debug, clone::Clone, cmp::PartialEq, cmp::Eq
impl ModelCapabilityCatalog {
    pub fn empty() -> Self;
    pub fn try_from_definitions(
        definitions: impl collect::IntoIterator<Item = ModelCapabilityDefinition>,
    ) -> result::Result<Self, ModelCapabilityCatalogError>;
    pub fn resolve(&self, target: &ResolvedTarget) -> option::Option<&ModelCapabilities>;
    pub fn validate(
        &self,
        target: &ResolvedTarget,
        settings: &ModelSettings,
    ) -> result::Result<&ModelCapabilities, ModelCapabilityError>;
    pub fn validate_explicit(
        &self,
        target: &ResolvedTarget,
        settings: &ModelSettings,
    ) -> result::Result<option::Option<&ModelCapabilities>, ModelCapabilityError>;
    pub fn iter(&self) -> impl iterator::Iterator<Item = &ModelCapabilityDefinition>;
}
impl default::Default for ModelCapabilityCatalog {
    fn default() -> Self;
}
```

## ModelCapabilityError

```rust
pub enum ModelCapabilityError {
    UnknownTarget { target: ResolvedTarget },
    UnsupportedReasoningLevel { reasoning_level: ReasoningLevel },
    UnsupportedFastMode,
    UnsupportedServiceTier { service_tier: ServiceTier },
}
// derives: fmt::Debug, clone::Clone, cmp::PartialEq, cmp::Eq
impl fmt::Display for ModelCapabilityError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for ModelCapabilityError {}
```

## ModelCapabilityCatalogError

```rust
pub enum ModelCapabilityCatalogError {
    DuplicateTarget { target: ResolvedTarget },
    SelfMappedFastTarget { target: ResolvedTarget },
}
// derives: fmt::Debug, clone::Clone, cmp::PartialEq, cmp::Eq
impl fmt::Display for ModelCapabilityCatalogError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for ModelCapabilityCatalogError {}
```
