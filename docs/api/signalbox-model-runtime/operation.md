<!-- generated by scripts/render_domain_spine.py, do not edit -->

# operation

## ModelOperationValidationError

```rust
pub enum ModelOperationValidationError {
    DuplicateToolName { name: ToolName },
    OutputContractToolNameCollision { name: ToolName },
    UndeclaredToolChoice { name: ToolName },
}
// derives: fmt::Debug, clone::Clone, cmp::PartialEq, cmp::Eq
impl fmt::Display for ModelOperationValidationError {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for ModelOperationValidationError {}
```

## DeliveryMode

```rust
pub enum DeliveryMode {
    Buffered,
    Streamed,
}
// derives: fmt::Debug, clone::Clone, marker::Copy, cmp::PartialEq, cmp::Eq
```

## ProviderCompactionMode

```rust
pub enum ProviderCompactionMode {
    Allowed,
    Suppressed,
}
// derives: fmt::Debug, clone::Clone, marker::Copy, cmp::PartialEq, cmp::Eq
```

## ToolChoice

```rust
pub enum ToolChoice {
    Automatic,
    AnyTool,
    Named(ToolName),
}
// derives: fmt::Debug, clone::Clone, cmp::PartialEq, cmp::Eq
```

## ModelOperation

```rust
pub struct ModelOperation<C> {
    pub correlation: C,
    pub credential_reference: CredentialReference,
    pub requested_target: RequestedTarget,
    pub resolved_target: ResolvedTarget,
    pub retained_mapped_target: option::Option<ResolvedTarget>,
    pub system: option::Option<string::String>,
    pub messages: vec::Vec<ConversationMessage>,
    pub settings: ModelSettings,
    pub tools: vec::Vec<ToolDefinition>,
    pub tool_choice: ToolChoice,
    pub output_contract: option::Option<StructuredOutputContract>,
    pub delivery: DeliveryMode,
    pub provider_compaction: ProviderCompactionMode,
    pub provider_compaction_supported: bool,
}
// derives: fmt::Debug, clone::Clone, cmp::PartialEq
impl<C> ModelOperation<C> {
    pub fn new(
        correlation: C,
        credential_reference: CredentialReference,
        requested_target: RequestedTarget,
        resolved_target: ResolvedTarget,
        messages: vec::Vec<ConversationMessage>,
        settings: ModelSettings,
    ) -> Self;
    pub fn validate(&self) -> result::Result<(), ModelOperationValidationError>;
}
```
