<!-- generated by scripts/render_domain_spine.py, do not edit -->

# session

## SessionCorruption

```rust
pub enum SessionCorruption {
    Missing(&'static str),
    Unsupported {
        field: &'static str,
        value: string::String,
    },
    Inconsistent(&'static str),
    InvalidOrdinal {
        field: &'static str,
        reason: mapping::PositiveOrdinalMappingError,
    },
    Domain(signalbox_domain::SessionReconstitutionFailure),
    Imported(create_session_from_imported_frontier::ImportedSessionCorruption),
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for session::SessionCorruption {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for session::SessionCorruption {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## SessionRepositoryError

```rust
pub enum SessionRepositoryError {
    Database(error::Error),
    Corruption(session::SessionCorruption),
}
// derives: fmt::Debug
impl fmt::Display for session::SessionRepositoryError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for session::SessionRepositoryError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
impl convert::From<error::Error> for session::SessionRepositoryError {
    fn from(error: error::Error) -> Self;
}
impl convert::From<session::SessionCorruption> for session::SessionRepositoryError {
    fn from(error: session::SessionCorruption) -> Self;
}
```

## RepositoryWatchCreationDispatch

```rust
pub struct RepositoryWatchCreationDispatch {
    pub dispatch: signalbox_domain::RepoWatchDispatchId,
    pub command: signalbox_domain::DurableCommandId,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## SessionRepository

```rust
pub struct SessionRepository {/* private */}
// derives: clone::Clone, fmt::Debug
impl session::SessionRepository {
    pub fn new(pool: sqlx_postgres::PgPool) -> Self;
    pub async fn repository_watch_creation_dispatch(
        &self,
        session: signalbox_domain::SessionId,
    ) -> result::Result<
        option::Option<session::RepositoryWatchCreationDispatch>,
        session::SessionRepositoryError,
    >;
    pub async fn load_session(
        &self,
        requested_session: signalbox_domain::SessionId,
    ) -> result::Result<option::Option<signalbox_domain::Session>, session::SessionRepositoryError>;
}
impl signalbox_application::SessionReader for session::SessionRepository {
    type Error = session::SessionRepositoryError;
    async fn load_session(
        &self,
        requested_session: signalbox_domain::SessionId,
    ) -> result::Result<
        option::Option<signalbox_domain::Session>,
        <Self as signalbox_application::SessionReader>::Error,
    >;
}
```
