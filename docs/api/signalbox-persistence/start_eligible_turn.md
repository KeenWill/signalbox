<!-- generated by scripts/render_domain_spine.py, do not edit -->

# start_eligible_turn

## StartEligibleTurnIdentityCollision

```rust
pub enum StartEligibleTurnIdentityCollision {
    ModelIdentityEntry,
    OriginEntry,
    StartingFrontier,
    InitialAttempt,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for start_eligible_turn::StartEligibleTurnIdentityCollision {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for start_eligible_turn::StartEligibleTurnIdentityCollision {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## StartEligibleTurnCorruption

```rust
pub enum StartEligibleTurnCorruption {
    Missing(&'static str),
    Inconsistent(&'static str),
    CurrentSession(session::SessionCorruption),
    Scheduling(submit_input::SubmitInputCorruption),
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for start_eligible_turn::StartEligibleTurnCorruption {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for start_eligible_turn::StartEligibleTurnCorruption {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## StartEligibleTurnRepositoryError

```rust
pub enum StartEligibleTurnRepositoryError {
    Database {
        source: error::Error,
        commit_ambiguous: bool,
    },
    Corruption(start_eligible_turn::StartEligibleTurnCorruption),
    IdentityCollision(start_eligible_turn::StartEligibleTurnIdentityCollision),
    HubInvariant(&'static str),
}
// derives: fmt::Debug
impl fmt::Display for start_eligible_turn::StartEligibleTurnRepositoryError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for start_eligible_turn::StartEligibleTurnRepositoryError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
impl signalbox_application::ClassifyOperatorFailure
    for start_eligible_turn::StartEligibleTurnRepositoryError
{
    fn operator_failure_class(&self) -> signalbox_application::OperatorFailureClass;
}
impl convert::From<start_eligible_turn::StartEligibleTurnCorruption>
    for start_eligible_turn::StartEligibleTurnRepositoryError
{
    fn from(error: start_eligible_turn::StartEligibleTurnCorruption) -> Self;
}
impl convert::From<error::Error> for start_eligible_turn::StartEligibleTurnRepositoryError {
    fn from(error: error::Error) -> Self;
}
```

## CommitActivationPreviewError

```rust
pub enum CommitActivationPreviewError {
    Activation(start_eligible_turn::StartEligibleTurnRepositoryError),
    ModelCall(model_execution::ModelCallRepositoryError),
    WorkspaceInstructions(workspace_instructions::WorkspaceInstructionRepositoryError),
}
// derives: fmt::Debug
impl fmt::Display for start_eligible_turn::CommitActivationPreviewError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for start_eligible_turn::CommitActivationPreviewError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
impl signalbox_application::ClassifyOperatorFailure
    for start_eligible_turn::CommitActivationPreviewError
{
    fn operator_failure_class(&self) -> signalbox_application::OperatorFailureClass;
}
```

## PreparedActivationPreview

```rust
pub struct PreparedActivationPreview {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl start_eligible_turn::PreparedActivationPreview {
    pub const fn prepared(&self) -> &signalbox_domain::PreparedTurnActivation;
}
```

## CommitActivationPreviewOutcome

```rust
pub enum CommitActivationPreviewOutcome {
    Activated(boxed::Box<signalbox_domain::ActivatedTurn>),
    PoolExhausted(signalbox_domain::TurnId),
    Stale,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## CommitCompactionFailurePreviewOutcome

```rust
pub enum CommitCompactionFailurePreviewOutcome {
    Failed(signalbox_domain::TurnId),
    Stale,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## CommitCountedAttachmentFailurePreviewOutcome

```rust
pub enum CommitCountedAttachmentFailurePreviewOutcome {
    CredentialWait(signalbox_domain::TurnId),
    Failed(signalbox_domain::TurnId),
    Stale,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## StartEligibleTurnRepository

```rust
pub struct StartEligibleTurnRepository {/* private */}
// derives: clone::Clone, fmt::Debug
impl start_eligible_turn::StartEligibleTurnRepository {
    pub fn new(pool: sqlx_postgres::PgPool) -> Self;
    pub async fn complete_checkout_provisioning(
        &self,
        session: signalbox_domain::SessionId,
    ) -> result::Result<(), start_eligible_turn::StartEligibleTurnRepositoryError>;
    pub async fn preview(
        &self,
        session: signalbox_domain::SessionId,
        identities: signalbox_domain::AcceptedInputTurnActivationIdentities,
    ) -> result::Result<
        option::Option<start_eligible_turn::PreparedActivationPreview>,
        start_eligible_turn::StartEligibleTurnRepositoryError,
    >;
    pub async fn commit_preview(
        &self,
        preview: start_eligible_turn::PreparedActivationPreview,
    ) -> result::Result<
        start_eligible_turn::CommitActivationPreviewOutcome,
        start_eligible_turn::StartEligibleTurnRepositoryError,
    >;
    pub async fn commit_counted_preview(
        &self,
        preview: start_eligible_turn::PreparedActivationPreview,
        prospective: model_execution::ProspectiveModelCall,
        model_calls: &model_execution::PostgresModelCallRepository,
        failure_identities: signalbox_domain::FailedModelCallTurnIdentities,
        instruction_evidence: option::Option<
            workspace_instructions::CountedActivationInstructionEvidence<'_>,
        >,
    ) -> result::Result<
        start_eligible_turn::CommitActivationPreviewOutcome,
        start_eligible_turn::CommitActivationPreviewError,
    >;
    pub async fn commit_counted_attachment_failure_preview(
        &self,
        preview: start_eligible_turn::PreparedActivationPreview,
        prospective: model_execution::ProspectiveModelCall,
        model_calls: &model_execution::PostgresModelCallRepository,
        failure: signalbox_application::AttachmentPreparationFailure,
        identities: signalbox_domain::FailedModelCallTurnIdentities,
        instruction_evidence: option::Option<
            workspace_instructions::CountedActivationInstructionEvidence<'_>,
        >,
    ) -> result::Result<
        start_eligible_turn::CommitCountedAttachmentFailurePreviewOutcome,
        start_eligible_turn::CommitActivationPreviewError,
    >;
    pub async fn commit_compaction_failure_preview(
        &self,
        preview: start_eligible_turn::PreparedActivationPreview,
        model_calls: &model_execution::PostgresModelCallRepository,
        identities: signalbox_domain::FailedModelCallTurnIdentities,
        terminal_cause: signalbox_domain::TurnTerminalCause,
        recovery_cause: option::Option<goal::GoalExecutionFailureRecoveryCause>,
    ) -> result::Result<
        start_eligible_turn::CommitCompactionFailurePreviewOutcome,
        start_eligible_turn::CommitActivationPreviewError,
    >;
    pub async fn handle(
        &self,
        session: signalbox_domain::SessionId,
        identities: signalbox_domain::AcceptedInputTurnActivationIdentities,
    ) -> result::Result<
        signalbox_application::StartEligibleTurnOutcome,
        start_eligible_turn::StartEligibleTurnRepositoryError,
    >;
}
impl signalbox_application::StartEligibleTurnTransaction
    for start_eligible_turn::StartEligibleTurnRepository
{
    type Error = start_eligible_turn::StartEligibleTurnRepositoryError;
    async fn handle(
        &mut self,
        session: signalbox_domain::SessionId,
        identities: signalbox_domain::AcceptedInputTurnActivationIdentities,
    ) -> result::Result<
        signalbox_application::StartEligibleTurnOutcome,
        <Self as signalbox_application::StartEligibleTurnTransaction>::Error,
    >;
    async fn handle_with_activation_observer(
        &mut self,
        session: signalbox_domain::SessionId,
        identities: signalbox_domain::AcceptedInputTurnActivationIdentities,
        observer: sync::Arc<
            dyn function::Fn(signalbox_domain::TurnId) + marker::Send + marker::Sync,
        >,
    ) -> result::Result<
        signalbox_application::StartEligibleTurnOutcome,
        <Self as signalbox_application::StartEligibleTurnTransaction>::Error,
    >;
}
```
