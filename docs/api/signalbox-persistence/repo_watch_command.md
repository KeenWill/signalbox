<!-- generated by scripts/render_domain_spine.py, do not edit -->

# repo_watch_command

## RepoWatchCommandRecord

```rust
pub enum RepoWatchCommandRecord {
    Create(boxed::Box<session::CreateSession>),
    Lifecycle(session_lifecycle_command::SessionLifecycleCommand),
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch_command::RepoWatchCommandRecord
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch_command::RepoWatchCommandRecord
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch_command::RepoWatchCommandRecord {}
impl<T> parse_display::IntoResult<T> for repo_watch_command::RepoWatchCommandRecord {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch_command::RepoWatchCommandRecord
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch_command::RepoWatchCommandRecord {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch_command::RepoWatchCommandRecord {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch_command::RepoWatchCommandRecord
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch_command::RepoWatchCommandRecord
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch_command::RepoWatchCommandRecord
where
    T: ?marker::Sized,
{
}
impl repo_watch_command::RepoWatchCommandRecord {
    pub fn encode(&self) -> option::Option<vec::Vec<u8>>;
    pub fn decode(payload: &[u8]) -> option::Option<Self>;
}
```
