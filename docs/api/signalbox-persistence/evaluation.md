<!-- generated by scripts/render_domain_spine.py, do not edit -->

# evaluation

## EvaluationError

```rust
pub enum EvaluationError {
    Conflict,
    InvalidSnapshot,
    Corrupt,
    Database(error::Error),
}
// derives: fmt::Debug
impl fmt::Display for evaluation::EvaluationError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for evaluation::EvaluationError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
impl convert::From<error::Error> for evaluation::EvaluationError {
    fn from(error: error::Error) -> Self;
}
```

## EvaluationRepository

```rust
pub struct EvaluationRepository {/* private */}
// derives: clone::Clone
impl evaluation::EvaluationRepository {
    pub const fn new(pool: sqlx_postgres::PgPool) -> Self;
    pub async fn seal(
        &self,
        snapshot: &signalbox_domain::evaluation::EvaluationSnapshot,
    ) -> result::Result<signalbox_domain::evaluation::EvaluationReceipt, evaluation::EvaluationError>;
    pub async fn load(
        &self,
        run: signalbox_domain::ProgramRunId,
    ) -> result::Result<
        option::Option<signalbox_domain::evaluation::EvaluationSnapshot>,
        evaluation::EvaluationError,
    >;
}
```
