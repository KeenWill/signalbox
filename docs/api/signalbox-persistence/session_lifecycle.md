<!-- generated by scripts/render_domain_spine.py, do not edit -->

# session_lifecycle

## SessionLifecycleCorruption

```rust
pub enum SessionLifecycleCorruption {
    Missing(&'static str),
    Unsupported {
        field: &'static str,
        value: string::String,
    },
    Inconsistent(&'static str),
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for session_lifecycle::SessionLifecycleCorruption {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for session_lifecycle::SessionLifecycleCorruption {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## SessionLifecycleRejection

```rust
pub enum SessionLifecycleRejection {
    TransitionNotAdmitted,
    ReleaseWhileParked,
    OwnershipUnchanged,
    GoalGenerationStillOpen,
    NoPendingTerminal,
    ParkWhileUnmonitored,
    PendingTerminalConflict,
    GoalOutcomeMismatch,
    StandingCauseMismatch,
    AbandonRequiresParkedOperator,
    ParkStandingMismatch,
    FinishConditionAlreadyDeclared,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for session_lifecycle::SessionLifecycleRejection {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
```

## SessionLifecycleRepositoryError

```rust
pub enum SessionLifecycleRepositoryError {
    Database(error::Error),
    CommitAmbiguous(error::Error),
    UnknownSession(signalbox_domain::SessionId),
    Rejected(session_lifecycle::SessionLifecycleRejection),
    Goal(boxed::Box<goal::GoalRepositoryError>),
    Corruption(session_lifecycle::SessionLifecycleCorruption),
}
// derives: fmt::Debug
impl fmt::Display for session_lifecycle::SessionLifecycleRepositoryError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for session_lifecycle::SessionLifecycleRepositoryError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
impl convert::From<error::Error> for session_lifecycle::SessionLifecycleRepositoryError {
    fn from(error: error::Error) -> Self;
}
impl convert::From<session_lifecycle::SessionLifecycleCorruption>
    for session_lifecycle::SessionLifecycleRepositoryError
{
    fn from(error: session_lifecycle::SessionLifecycleCorruption) -> Self;
}
impl convert::From<goal::GoalRepositoryError>
    for session_lifecycle::SessionLifecycleRepositoryError
{
    fn from(error: goal::GoalRepositoryError) -> Self;
}
```

## SessionLifecycleRecord

```rust
pub struct SessionLifecycleRecord {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl session_lifecycle::SessionLifecycleRecord {
    pub const fn session(&self) -> signalbox_domain::SessionId;
    pub const fn state(&self) -> signalbox_domain::SessionLifecycleState;
    pub const fn ownership(&self) -> signalbox_domain::SessionOwnership;
    pub const fn actor(&self) -> signalbox_domain::LifecycleActor;
    pub const fn pending_terminal(
        &self,
    ) -> option::Option<signalbox_domain::SessionTerminalOutcome>;
    pub const fn pending_terminal_actor(&self) -> option::Option<signalbox_domain::LifecycleActor>;
    pub const fn finish_condition(&self) -> option::Option<&signalbox_domain::FinishCondition>;
}
```

## SessionLifecycleRepository

```rust
pub struct SessionLifecycleRepository {/* private */}
// derives: clone::Clone, fmt::Debug
impl session_lifecycle::SessionLifecycleRepository {
    pub const fn new(pool: sqlx_postgres::PgPool) -> Self;
    pub async fn load(
        &self,
        session: signalbox_domain::SessionId,
    ) -> result::Result<
        option::Option<session_lifecycle::SessionLifecycleRecord>,
        session_lifecycle::SessionLifecycleRepositoryError,
    >;
    pub async fn park(
        &self,
        session: signalbox_domain::SessionId,
        cause: signalbox_domain::SessionParkCause,
        responder: signalbox_domain::SessionParkResponder,
        standing: option::Option<signalbox_domain::SessionFailureCause>,
        actor: signalbox_domain::LifecycleActor,
    ) -> result::Result<
        signalbox_domain::SessionLifecycleState,
        session_lifecycle::SessionLifecycleRepositoryError,
    >;
    pub async fn resume(
        &self,
        session: signalbox_domain::SessionId,
    ) -> result::Result<
        signalbox_domain::SessionLifecycleState,
        session_lifecycle::SessionLifecycleRepositoryError,
    >;
    pub async fn close(
        &self,
        session: signalbox_domain::SessionId,
        outcome: signalbox_domain::SessionTerminalOutcome,
        actor: signalbox_domain::LifecycleActor,
    ) -> result::Result<
        signalbox_domain::SessionLifecycleState,
        session_lifecycle::SessionLifecycleRepositoryError,
    >;
    pub async fn commit_pending_terminal(
        &self,
        session: signalbox_domain::SessionId,
        outcome: signalbox_domain::SessionTerminalOutcome,
        actor: signalbox_domain::LifecycleActor,
    ) -> result::Result<(), session_lifecycle::SessionLifecycleRepositoryError>;
    pub async fn settle_pending_terminal(
        &self,
        session: signalbox_domain::SessionId,
    ) -> result::Result<
        signalbox_domain::SessionLifecycleState,
        session_lifecycle::SessionLifecycleRepositoryError,
    >;
    pub async fn adopt(
        &self,
        session: signalbox_domain::SessionId,
        actor: signalbox_domain::LifecycleActor,
    ) -> result::Result<(), session_lifecycle::SessionLifecycleRepositoryError>;
    pub async fn release(
        &self,
        session: signalbox_domain::SessionId,
        actor: signalbox_domain::LifecycleActor,
    ) -> result::Result<(), session_lifecycle::SessionLifecycleRepositoryError>;
}
```
