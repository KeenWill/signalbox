<!-- generated by scripts/render_domain_spine.py, do not edit -->

# repo_watch

## RepoWatchCursorGeneration

```rust
pub struct RepoWatchCursorGeneration(/* private */);
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl<T> from_ref::FromRef<T> for repo_watch::RepoWatchCursorGeneration
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch::RepoWatchCursorGeneration
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch::RepoWatchCursorGeneration {}
impl<T> parse_display::IntoResult<T> for repo_watch::RepoWatchCursorGeneration {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch::RepoWatchCursorGeneration
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch::RepoWatchCursorGeneration {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch::RepoWatchCursorGeneration {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch::RepoWatchCursorGeneration
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch::RepoWatchCursorGeneration
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch::RepoWatchCursorGeneration
where
    T: ?marker::Sized,
{
}
impl repo_watch::RepoWatchCursorGeneration {
    pub const fn get(self) -> u64;
}
impl repo_watch::RepoWatchCursorGeneration {
    pub const INITIAL: Self;
    pub fn next(self) -> option::Option<Self>;
}
```

## RepoWatchCursorCandidate

```rust
pub struct RepoWatchCursorCandidate {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch::RepoWatchCursorCandidate
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch::RepoWatchCursorCandidate
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch::RepoWatchCursorCandidate {}
impl<T> parse_display::IntoResult<T> for repo_watch::RepoWatchCursorCandidate {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch::RepoWatchCursorCandidate
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch::RepoWatchCursorCandidate {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch::RepoWatchCursorCandidate {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch::RepoWatchCursorCandidate
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch::RepoWatchCursorCandidate
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch::RepoWatchCursorCandidate
where
    T: ?marker::Sized,
{
}
impl repo_watch::RepoWatchCursorCandidate {
    pub const fn observation(&self) -> &repo_watch::RepoWatchObservation;
    pub const fn event_identity_frontier(&self) -> &repo_watch::RepoWatchEventIdentityFrontierV1;
    pub fn merged_pull_request_baselines(
        &self,
    ) -> &[repo_watch::RepoWatchMergedPullRequestBaselineV1];
}
impl repo_watch::RepoWatchCursorCandidate {
    pub fn new(observation: repo_watch::RepoWatchObservation) -> Self;
    pub fn with_event_identity_frontier(
        observation: repo_watch::RepoWatchObservation,
        event_identity_frontier: repo_watch::RepoWatchEventIdentityFrontierV1,
    ) -> Self;
    pub fn try_with_event_identity_frontier_and_merged_baselines(
        observation: repo_watch::RepoWatchObservation,
        event_identity_frontier: repo_watch::RepoWatchEventIdentityFrontierV1,
        merged_pull_request_baselines: vec::Vec<repo_watch::RepoWatchMergedPullRequestBaselineV1>,
    ) -> result::Result<Self, repo_watch::RepoWatchRepositoryStateError>;
}
```

## RepoWatchCursor

```rust
pub struct RepoWatchCursor {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch::RepoWatchCursor
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch::RepoWatchCursor
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch::RepoWatchCursor {}
impl<T> parse_display::IntoResult<T> for repo_watch::RepoWatchCursor {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch::RepoWatchCursor
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch::RepoWatchCursor {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch::RepoWatchCursor {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch::RepoWatchCursor
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch::RepoWatchCursor
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch::RepoWatchCursor
where
    T: ?marker::Sized,
{
}
impl repo_watch::RepoWatchCursor {
    pub const fn repository(&self) -> &repo_watch::RepositorySlug;
    pub const fn candidate(&self) -> &repo_watch::RepoWatchCursorCandidate;
}
impl repo_watch::RepoWatchCursor {
    pub const fn generation(&self) -> repo_watch::RepoWatchCursorGeneration;
}
```

## RepoWatchEventProducer

```rust
pub enum RepoWatchEventProducer {
    Poll,
    Webhook,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch::RepoWatchEventProducer
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch::RepoWatchEventProducer
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch::RepoWatchEventProducer {}
impl<T> parse_display::IntoResult<T> for repo_watch::RepoWatchEventProducer {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch::RepoWatchEventProducer
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch::RepoWatchEventProducer {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch::RepoWatchEventProducer {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch::RepoWatchEventProducer
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch::RepoWatchEventProducer
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch::RepoWatchEventProducer
where
    T: ?marker::Sized,
{
}
```

## RepoWatchCommitRequest

```rust
pub struct RepoWatchCommitRequest {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch::RepoWatchCommitRequest
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch::RepoWatchCommitRequest
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch::RepoWatchCommitRequest {}
impl<T> parse_display::IntoResult<T> for repo_watch::RepoWatchCommitRequest {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch::RepoWatchCommitRequest
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch::RepoWatchCommitRequest {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch::RepoWatchCommitRequest {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch::RepoWatchCommitRequest
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch::RepoWatchCommitRequest
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch::RepoWatchCommitRequest
where
    T: ?marker::Sized,
{
}
impl repo_watch::RepoWatchCommitRequest {
    pub const fn candidate(&self) -> &repo_watch::RepoWatchCursorCandidate;
    pub fn events(&self) -> &[repo_watch::RepoWatchEventOccurrenceV1];
}
impl repo_watch::RepoWatchCommitRequest {
    pub fn new(
        expected_generation: option::Option<repo_watch::RepoWatchCursorGeneration>,
        candidate: repo_watch::RepoWatchCursorCandidate,
        events: vec::Vec<repo_watch::RepoWatchEventOccurrenceV1>,
    ) -> Self;
    pub fn from_webhook(
        expected_generation: option::Option<repo_watch::RepoWatchCursorGeneration>,
        candidate: repo_watch::RepoWatchCursorCandidate,
        events: vec::Vec<repo_watch::RepoWatchEventOccurrenceV1>,
    ) -> Self;
    pub const fn expected_generation(
        &self,
    ) -> option::Option<repo_watch::RepoWatchCursorGeneration>;
    pub const fn producer(&self) -> repo_watch::RepoWatchEventProducer;
}
```

## RepoWatchCommitOutcome

```rust
pub enum RepoWatchCommitOutcome {
    Committed(repo_watch::RepoWatchCursor),
    Replayed(repo_watch::RepoWatchCursor),
    Unchanged(repo_watch::RepoWatchCursor),
    Conflict {
        current: option::Option<repo_watch::RepoWatchCursorGeneration>,
    },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch::RepoWatchCommitOutcome
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch::RepoWatchCommitOutcome
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch::RepoWatchCommitOutcome {}
impl<T> parse_display::IntoResult<T> for repo_watch::RepoWatchCommitOutcome {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch::RepoWatchCommitOutcome
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch::RepoWatchCommitOutcome {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch::RepoWatchCommitOutcome {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch::RepoWatchCommitOutcome
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch::RepoWatchCommitOutcome
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch::RepoWatchCommitOutcome
where
    T: ?marker::Sized,
{
}
```

## RepoWatchEventPosition

```rust
pub struct RepoWatchEventPosition {/* private */}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl<T> from_ref::FromRef<T> for repo_watch::RepoWatchEventPosition
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch::RepoWatchEventPosition
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch::RepoWatchEventPosition {}
impl<T> parse_display::IntoResult<T> for repo_watch::RepoWatchEventPosition {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch::RepoWatchEventPosition
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch::RepoWatchEventPosition {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch::RepoWatchEventPosition {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch::RepoWatchEventPosition
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch::RepoWatchEventPosition
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch::RepoWatchEventPosition
where
    T: ?marker::Sized,
{
}
impl repo_watch::RepoWatchEventPosition {
    pub const fn generation(self) -> repo_watch::RepoWatchCursorGeneration;
    pub const fn ordinal(self) -> nonzero::NonZeroU32;
}
```

## RepoWatchEventPageSize

```rust
pub struct RepoWatchEventPageSize(/* private */);
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch::RepoWatchEventPageSize
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch::RepoWatchEventPageSize
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch::RepoWatchEventPageSize {}
impl<T> parse_display::IntoResult<T> for repo_watch::RepoWatchEventPageSize {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch::RepoWatchEventPageSize
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch::RepoWatchEventPageSize {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch::RepoWatchEventPageSize {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch::RepoWatchEventPageSize
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch::RepoWatchEventPageSize
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch::RepoWatchEventPageSize
where
    T: ?marker::Sized,
{
}
impl repo_watch::RepoWatchEventPageSize {
    pub const fn get(self) -> u16;
}
impl repo_watch::RepoWatchEventPageSize {
    pub fn try_new(
        value: nonzero::NonZeroU16,
    ) -> result::Result<Self, repo_watch::RepoWatchPageSizeError>;
}
```

## RepoWatchPageSizeError

```rust
pub struct RepoWatchPageSizeError;
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch::RepoWatchPageSizeError
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch::RepoWatchPageSizeError
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch::RepoWatchPageSizeError {}
impl<T> parse_display::IntoResult<T> for repo_watch::RepoWatchPageSizeError {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch::RepoWatchPageSizeError
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch::RepoWatchPageSizeError {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch::RepoWatchPageSizeError {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch::RepoWatchPageSizeError
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch::RepoWatchPageSizeError
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch::RepoWatchPageSizeError
where
    T: ?marker::Sized,
{
}
impl fmt::Display for repo_watch::RepoWatchPageSizeError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for repo_watch::RepoWatchPageSizeError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## PositionedRepoWatchEvent

```rust
pub struct PositionedRepoWatchEvent {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch::PositionedRepoWatchEvent
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch::PositionedRepoWatchEvent
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch::PositionedRepoWatchEvent {}
impl<T> parse_display::IntoResult<T> for repo_watch::PositionedRepoWatchEvent {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch::PositionedRepoWatchEvent
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch::PositionedRepoWatchEvent {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch::PositionedRepoWatchEvent {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch::PositionedRepoWatchEvent
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch::PositionedRepoWatchEvent
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch::PositionedRepoWatchEvent
where
    T: ?marker::Sized,
{
}
impl repo_watch::PositionedRepoWatchEvent {
    pub const fn event(&self) -> &repo_watch::RepoWatchEvent;
}
impl repo_watch::PositionedRepoWatchEvent {
    pub const fn position(&self) -> repo_watch::RepoWatchEventPosition;
    pub const fn producer(&self) -> repo_watch::RepoWatchEventProducer;
}
```

## RepoWatchEventPage

```rust
pub struct RepoWatchEventPage {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch::RepoWatchEventPage
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch::RepoWatchEventPage
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch::RepoWatchEventPage {}
impl<T> parse_display::IntoResult<T> for repo_watch::RepoWatchEventPage {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch::RepoWatchEventPage
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch::RepoWatchEventPage {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch::RepoWatchEventPage {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch::RepoWatchEventPage
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch::RepoWatchEventPage
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch::RepoWatchEventPage
where
    T: ?marker::Sized,
{
}
impl repo_watch::RepoWatchEventPage {
    pub fn events(&self) -> &[repo_watch::PositionedRepoWatchEvent];
}
impl repo_watch::RepoWatchEventPage {
    pub const fn next_after(&self) -> option::Option<repo_watch::RepoWatchEventPosition>;
}
```

## RepoWatchPersistenceCorruption

```rust
pub enum RepoWatchPersistenceCorruption {
    InvalidCursorGeneration,
    MalformedCursorDocument,
    UnsupportedCursorVersion,
    InvalidCursorField(&'static str),
    UnknownCursorDiscriminator(&'static str),
    NonCanonicalCursor,
    InvalidEventPosition,
    UnsupportedEventVersion,
    UnsupportedEventContentIdentityVersion,
    InvalidEventContentIdentity,
    UnknownEventProducer,
    InvalidEventField(&'static str),
    UnknownEventDiscriminator(&'static str),
    InvalidStoredDomainValue,
    EventShapeMismatch,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch::RepoWatchPersistenceCorruption
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch::RepoWatchPersistenceCorruption
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch::RepoWatchPersistenceCorruption {}
impl<T> parse_display::IntoResult<T> for repo_watch::RepoWatchPersistenceCorruption {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch::RepoWatchPersistenceCorruption
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch::RepoWatchPersistenceCorruption {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch::RepoWatchPersistenceCorruption {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch::RepoWatchPersistenceCorruption
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch::RepoWatchPersistenceCorruption
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch::RepoWatchPersistenceCorruption
where
    T: ?marker::Sized,
{
}
impl fmt::Display for repo_watch::RepoWatchPersistenceCorruption {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for repo_watch::RepoWatchPersistenceCorruption {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## RepoWatchStoreError

```rust
pub enum RepoWatchStoreError {
    Database(error::Error),
    CommitAmbiguous(error::Error),
    CursorEncoding(error::Error),
    Corruption(repo_watch::RepoWatchPersistenceCorruption),
    EventRepositoryMismatch,
    DuplicateEventIdentity(signalbox_domain::RepoWatchEventId),
    DuplicateEventContentIdentity(repo_watch::RepoWatchEventContentIdentityV1),
    EventsWithoutStateChange,
    CursorGenerationExhausted,
    EventBatchTooLarge,
    ConvergenceEvidenceTooLarge,
    ConvergenceEvidenceMismatch,
    StaleReviewClearanceMismatch,
}
// derives: fmt::Debug
impl<T> into_either::IntoEither for repo_watch::RepoWatchStoreError {}
impl<T> parse_display::IntoResult<T> for repo_watch::RepoWatchStoreError {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch::RepoWatchStoreError
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch::RepoWatchStoreError {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch::RepoWatchStoreError {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch::RepoWatchStoreError
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch::RepoWatchStoreError
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch::RepoWatchStoreError
where
    T: ?marker::Sized,
{
}
impl fmt::Display for repo_watch::RepoWatchStoreError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for repo_watch::RepoWatchStoreError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
impl convert::From<error::Error> for repo_watch::RepoWatchStoreError {
    fn from(error: error::Error) -> Self;
}
impl convert::From<repo_watch::RepoWatchPersistenceCorruption> for repo_watch::RepoWatchStoreError {
    fn from(error: repo_watch::RepoWatchPersistenceCorruption) -> Self;
}
impl convert::From<repo_watch::RepoWatchTextError> for repo_watch::RepoWatchStoreError {
    fn from(_error: repo_watch::RepoWatchTextError) -> Self;
}
```

## RepoWatchStaleReviewClearanceId

```rust
pub struct RepoWatchStaleReviewClearanceId(/* private */);
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch::RepoWatchStaleReviewClearanceId
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch::RepoWatchStaleReviewClearanceId
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch::RepoWatchStaleReviewClearanceId {}
impl<T> parse_display::IntoResult<T> for repo_watch::RepoWatchStaleReviewClearanceId {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch::RepoWatchStaleReviewClearanceId
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch::RepoWatchStaleReviewClearanceId {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch::RepoWatchStaleReviewClearanceId {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch::RepoWatchStaleReviewClearanceId
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch::RepoWatchStaleReviewClearanceId
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch::RepoWatchStaleReviewClearanceId
where
    T: ?marker::Sized,
{
}
impl repo_watch::RepoWatchStaleReviewClearanceId {
    pub const fn get(self) -> uuid::Uuid;
}
impl repo_watch::RepoWatchStaleReviewClearanceId {
    pub const fn new(value: uuid::Uuid) -> Self;
}
```

## RepoWatchStaleReviewClearanceClaimToken

```rust
pub struct RepoWatchStaleReviewClearanceClaimToken(/* private */);
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch::RepoWatchStaleReviewClearanceClaimToken
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch::RepoWatchStaleReviewClearanceClaimToken
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch::RepoWatchStaleReviewClearanceClaimToken {}
impl<T> parse_display::IntoResult<T> for repo_watch::RepoWatchStaleReviewClearanceClaimToken {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch::RepoWatchStaleReviewClearanceClaimToken
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch::RepoWatchStaleReviewClearanceClaimToken {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch::RepoWatchStaleReviewClearanceClaimToken {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch::RepoWatchStaleReviewClearanceClaimToken
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch::RepoWatchStaleReviewClearanceClaimToken
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch::RepoWatchStaleReviewClearanceClaimToken
where
    T: ?marker::Sized,
{
}
impl repo_watch::RepoWatchStaleReviewClearanceClaimToken {
    pub const fn get(self) -> uuid::Uuid;
}
impl repo_watch::RepoWatchStaleReviewClearanceClaimToken {
    pub const fn new(value: uuid::Uuid) -> Self;
}
```

## RepoWatchStaleReviewClearanceRenewal

```rust
pub enum RepoWatchStaleReviewClearanceRenewal {
    Retained,
    Lost,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch::RepoWatchStaleReviewClearanceRenewal
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch::RepoWatchStaleReviewClearanceRenewal
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch::RepoWatchStaleReviewClearanceRenewal {}
impl<T> parse_display::IntoResult<T> for repo_watch::RepoWatchStaleReviewClearanceRenewal {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch::RepoWatchStaleReviewClearanceRenewal
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch::RepoWatchStaleReviewClearanceRenewal {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch::RepoWatchStaleReviewClearanceRenewal {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch::RepoWatchStaleReviewClearanceRenewal
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch::RepoWatchStaleReviewClearanceRenewal
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch::RepoWatchStaleReviewClearanceRenewal
where
    T: ?marker::Sized,
{
}
```

## RepoWatchPlannedStaleReviewClearance

```rust
pub struct RepoWatchPlannedStaleReviewClearance {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch::RepoWatchPlannedStaleReviewClearance
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch::RepoWatchPlannedStaleReviewClearance
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch::RepoWatchPlannedStaleReviewClearance {}
impl<T> parse_display::IntoResult<T> for repo_watch::RepoWatchPlannedStaleReviewClearance {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch::RepoWatchPlannedStaleReviewClearance
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch::RepoWatchPlannedStaleReviewClearance {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch::RepoWatchPlannedStaleReviewClearance {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch::RepoWatchPlannedStaleReviewClearance
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch::RepoWatchPlannedStaleReviewClearance
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch::RepoWatchPlannedStaleReviewClearance
where
    T: ?marker::Sized,
{
}
impl repo_watch::RepoWatchPlannedStaleReviewClearance {
    pub const fn current_head_sha(&self) -> &repo_watch::CommitSha;
    pub const fn base_branch(&self) -> &repo_watch::BranchName;
    pub const fn base_revision(&self) -> &repo_watch::CommitSha;
    pub const fn reviewer(&self) -> &repo_watch::RepoWatchAuthorLogin;
    pub const fn reviewed_head_sha(&self) -> &repo_watch::CommitSha;
}
impl repo_watch::RepoWatchPlannedStaleReviewClearance {
    pub fn from_fixture(fixture: repo_watch::RepoWatchPlannedStaleReviewClearanceFixture) -> Self;
    pub const fn clearance_id(&self) -> repo_watch::RepoWatchStaleReviewClearanceId;
    pub const fn claim_token(&self) -> repo_watch::RepoWatchStaleReviewClearanceClaimToken;
    pub const fn number(&self) -> repo_watch::PullRequestNumber;
    pub const fn review_node_id(&self) -> &str;
    pub const fn dismissal_message(&self) -> &str;
}
```

## RepoWatchPlannedStaleReviewClearanceFixture

```rust
pub struct RepoWatchPlannedStaleReviewClearanceFixture {
    pub clearance_id: repo_watch::RepoWatchStaleReviewClearanceId,
    pub claim_token: repo_watch::RepoWatchStaleReviewClearanceClaimToken,
    pub number: repo_watch::PullRequestNumber,
    pub current_head_sha: repo_watch::CommitSha,
    pub base_branch: repo_watch::BranchName,
    pub base_revision: repo_watch::CommitSha,
    pub review_node_id: string::String,
    pub reviewer: repo_watch::RepoWatchAuthorLogin,
    pub reviewed_head_sha: repo_watch::CommitSha,
    pub dismissal_message: string::String,
}
// derives: clone::Clone, fmt::Debug
impl<T> from_ref::FromRef<T> for repo_watch::RepoWatchPlannedStaleReviewClearanceFixture
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch::RepoWatchPlannedStaleReviewClearanceFixture
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch::RepoWatchPlannedStaleReviewClearanceFixture {}
impl<T> parse_display::IntoResult<T> for repo_watch::RepoWatchPlannedStaleReviewClearanceFixture {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch::RepoWatchPlannedStaleReviewClearanceFixture
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch::RepoWatchPlannedStaleReviewClearanceFixture {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch::RepoWatchPlannedStaleReviewClearanceFixture {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch::RepoWatchPlannedStaleReviewClearanceFixture
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch::RepoWatchPlannedStaleReviewClearanceFixture
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch::RepoWatchPlannedStaleReviewClearanceFixture
where
    T: ?marker::Sized,
{
}
```

## RepoWatchStaleReviewClearanceOutcome

```rust
pub enum RepoWatchStaleReviewClearanceOutcome {
    Dismissed,
    AlreadyDismissed,
    ClearedElsewhere,
    Superseded,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch::RepoWatchStaleReviewClearanceOutcome
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch::RepoWatchStaleReviewClearanceOutcome
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch::RepoWatchStaleReviewClearanceOutcome {}
impl<T> parse_display::IntoResult<T> for repo_watch::RepoWatchStaleReviewClearanceOutcome {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch::RepoWatchStaleReviewClearanceOutcome
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch::RepoWatchStaleReviewClearanceOutcome {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch::RepoWatchStaleReviewClearanceOutcome {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch::RepoWatchStaleReviewClearanceOutcome
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch::RepoWatchStaleReviewClearanceOutcome
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch::RepoWatchStaleReviewClearanceOutcome
where
    T: ?marker::Sized,
{
}
```

## RepoWatchObservedReviewState

```rust
pub enum RepoWatchObservedReviewState {
    Approved,
    ChangesRequested,
    Commented,
    Dismissed,
    Pending,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch::RepoWatchObservedReviewState
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch::RepoWatchObservedReviewState
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch::RepoWatchObservedReviewState {}
impl<T> parse_display::IntoResult<T> for repo_watch::RepoWatchObservedReviewState {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch::RepoWatchObservedReviewState
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch::RepoWatchObservedReviewState {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch::RepoWatchObservedReviewState {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch::RepoWatchObservedReviewState
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch::RepoWatchObservedReviewState
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch::RepoWatchObservedReviewState
where
    T: ?marker::Sized,
{
}
```

## PostgresRepoWatchStore

```rust
pub struct PostgresRepoWatchStore {/* private */}
// derives: clone::Clone, fmt::Debug
impl<T> from_ref::FromRef<T> for repo_watch::PostgresRepoWatchStore
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch::PostgresRepoWatchStore
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch::PostgresRepoWatchStore {}
impl<T> parse_display::IntoResult<T> for repo_watch::PostgresRepoWatchStore {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch::PostgresRepoWatchStore
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch::PostgresRepoWatchStore {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch::PostgresRepoWatchStore {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch::PostgresRepoWatchStore
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch::PostgresRepoWatchStore
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch::PostgresRepoWatchStore
where
    T: ?marker::Sized,
{
}
impl repo_watch::PostgresRepoWatchStore {
    pub const fn new(pool: sqlx_postgres::PgPool) -> Self;
    pub async fn load_cursor(
        &self,
        repository: &repo_watch::RepositorySlug,
    ) -> result::Result<option::Option<repo_watch::RepoWatchCursor>, repo_watch::RepoWatchStoreError>;
    pub async fn load_cursor_payload_bytes(
        &self,
        repository: &repo_watch::RepositorySlug,
    ) -> result::Result<option::Option<u64>, repo_watch::RepoWatchStoreError>;
    pub async fn load_complete_poll_age(
        &self,
        repository: &repo_watch::RepositorySlug,
    ) -> result::Result<option::Option<time::Duration>, repo_watch::RepoWatchStoreError>;
    pub async fn commit(
        &self,
        repository: &repo_watch::RepositorySlug,
        request: repo_watch::RepoWatchCommitRequest,
    ) -> result::Result<repo_watch::RepoWatchCommitOutcome, repo_watch::RepoWatchStoreError>;
    pub async fn commit_with_convergence(
        &self,
        repository: &repo_watch::RepositorySlug,
        request: repo_watch::RepoWatchCommitRequest,
        assessments: &[repo_watch::RepoWatchConvergenceAssessment],
    ) -> result::Result<repo_watch::RepoWatchCommitOutcome, repo_watch::RepoWatchStoreError>;
    pub async fn record_convergence_assessments(
        &self,
        repository: &repo_watch::RepositorySlug,
        cursor_generation: repo_watch::RepoWatchCursorGeneration,
        assessments: &[repo_watch::RepoWatchConvergenceAssessment],
    ) -> result::Result<(), repo_watch::RepoWatchStoreError>;
    pub async fn plan_stale_review_clearances(
        &self,
        repository: &repo_watch::RepositorySlug,
        cursor_generation: repo_watch::RepoWatchCursorGeneration,
        candidates: &[repo_watch::RepoWatchStaleReviewClearanceCandidate],
    ) -> result::Result<
        vec::Vec<repo_watch::RepoWatchPlannedStaleReviewClearance>,
        repo_watch::RepoWatchStoreError,
    >;
    pub async fn claim_pending_stale_review_clearances(
        &self,
        repository: &repo_watch::RepositorySlug,
    ) -> result::Result<
        vec::Vec<repo_watch::RepoWatchPlannedStaleReviewClearance>,
        repo_watch::RepoWatchStoreError,
    >;
    pub async fn renew_stale_review_clearance_claim(
        &self,
        clearance_id: repo_watch::RepoWatchStaleReviewClearanceId,
        claim_token: repo_watch::RepoWatchStaleReviewClearanceClaimToken,
    ) -> result::Result<
        repo_watch::RepoWatchStaleReviewClearanceRenewal,
        repo_watch::RepoWatchStoreError,
    >;
    pub async fn record_stale_review_clearance_outcome(
        &self,
        clearance_id: repo_watch::RepoWatchStaleReviewClearanceId,
        claim_token: repo_watch::RepoWatchStaleReviewClearanceClaimToken,
        outcome: repo_watch::RepoWatchStaleReviewClearanceOutcome,
        provider_state: repo_watch::RepoWatchObservedReviewState,
    ) -> result::Result<(), repo_watch::RepoWatchStoreError>;
    pub async fn release_stale_review_clearance_claim(
        &self,
        clearance_id: repo_watch::RepoWatchStaleReviewClearanceId,
        claim_token: repo_watch::RepoWatchStaleReviewClearanceClaimToken,
    ) -> result::Result<(), repo_watch::RepoWatchStoreError>;
    pub async fn load_event_page(
        &self,
        repository: &repo_watch::RepositorySlug,
        after: option::Option<repo_watch::RepoWatchEventPosition>,
        page_size: repo_watch::RepoWatchEventPageSize,
    ) -> result::Result<repo_watch::RepoWatchEventPage, repo_watch::RepoWatchStoreError>;
    pub async fn load_event(
        &self,
        repository: &repo_watch::RepositorySlug,
        event: signalbox_domain::RepoWatchEventId,
    ) -> result::Result<option::Option<repo_watch::RepoWatchEvent>, repo_watch::RepoWatchStoreError>;
}
```
