<!-- generated by scripts/render_domain_spine.py, do not edit -->

# repo_watch_webhook

## MAX_PENDING_PAGE_BYTES

```rust
pub const MAX_PENDING_PAGE_BYTES: usize;
```

## RepoWatchWebhookDeliveryKey

```rust
pub struct RepoWatchWebhookDeliveryKey {/* private */}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch_webhook::RepoWatchWebhookDeliveryKey
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch_webhook::RepoWatchWebhookDeliveryKey
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch_webhook::RepoWatchWebhookDeliveryKey {}
impl<T> parse_display::IntoResult<T> for repo_watch_webhook::RepoWatchWebhookDeliveryKey {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch_webhook::RepoWatchWebhookDeliveryKey
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch_webhook::RepoWatchWebhookDeliveryKey {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch_webhook::RepoWatchWebhookDeliveryKey {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch_webhook::RepoWatchWebhookDeliveryKey
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch_webhook::RepoWatchWebhookDeliveryKey
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch_webhook::RepoWatchWebhookDeliveryKey
where
    T: ?marker::Sized,
{
}
impl repo_watch_webhook::RepoWatchWebhookDeliveryKey {
    pub const fn new(hook_id: nonzero::NonZeroU64, delivery_id: uuid::Uuid) -> Self;
    pub const fn hook_id(self) -> nonzero::NonZeroU64;
    pub const fn delivery_id(self) -> uuid::Uuid;
}
```

## RepoWatchWebhookAdmission

```rust
pub struct RepoWatchWebhookAdmission {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch_webhook::RepoWatchWebhookAdmission
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch_webhook::RepoWatchWebhookAdmission
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch_webhook::RepoWatchWebhookAdmission {}
impl<T> parse_display::IntoResult<T> for repo_watch_webhook::RepoWatchWebhookAdmission {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch_webhook::RepoWatchWebhookAdmission
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch_webhook::RepoWatchWebhookAdmission {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch_webhook::RepoWatchWebhookAdmission {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch_webhook::RepoWatchWebhookAdmission
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch_webhook::RepoWatchWebhookAdmission
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch_webhook::RepoWatchWebhookAdmission
where
    T: ?marker::Sized,
{
}
impl repo_watch_webhook::RepoWatchWebhookAdmission {
    pub fn try_new(
        key: repo_watch_webhook::RepoWatchWebhookDeliveryKey,
        repository: repo_watch::RepositorySlug,
        event_name: string::String,
        action_name: option::Option<string::String>,
        body_digest: [u8; 32],
        body: vec::Vec<u8>,
    ) -> result::Result<Self, repo_watch_webhook::RepoWatchWebhookRequestError>;
    pub const fn key(&self) -> repo_watch_webhook::RepoWatchWebhookDeliveryKey;
    pub const fn repository(&self) -> &repo_watch::RepositorySlug;
    pub fn event_name(&self) -> &str;
    pub fn action_name(&self) -> option::Option<&str>;
    pub const fn body_digest(&self) -> &[u8; 32];
    pub fn body(&self) -> &[u8];
}
```

## RepoWatchWebhookRequestError

```rust
pub enum RepoWatchWebhookRequestError {
    InvalidEventName,
    InvalidActionName,
    InvalidOutcomeCode,
    EmptyBody,
    EmptyOccurrenceKey,
    TooManyProjections,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch_webhook::RepoWatchWebhookRequestError
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch_webhook::RepoWatchWebhookRequestError
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch_webhook::RepoWatchWebhookRequestError {}
impl<T> parse_display::IntoResult<T> for repo_watch_webhook::RepoWatchWebhookRequestError {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch_webhook::RepoWatchWebhookRequestError
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch_webhook::RepoWatchWebhookRequestError {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch_webhook::RepoWatchWebhookRequestError {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch_webhook::RepoWatchWebhookRequestError
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch_webhook::RepoWatchWebhookRequestError
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch_webhook::RepoWatchWebhookRequestError
where
    T: ?marker::Sized,
{
}
impl fmt::Display for repo_watch_webhook::RepoWatchWebhookRequestError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for repo_watch_webhook::RepoWatchWebhookRequestError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## RepoWatchWebhookReceipt

```rust
pub struct RepoWatchWebhookReceipt {/* private */}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch_webhook::RepoWatchWebhookReceipt
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch_webhook::RepoWatchWebhookReceipt
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch_webhook::RepoWatchWebhookReceipt {}
impl<T> parse_display::IntoResult<T> for repo_watch_webhook::RepoWatchWebhookReceipt {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch_webhook::RepoWatchWebhookReceipt
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch_webhook::RepoWatchWebhookReceipt {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch_webhook::RepoWatchWebhookReceipt {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch_webhook::RepoWatchWebhookReceipt
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch_webhook::RepoWatchWebhookReceipt
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch_webhook::RepoWatchWebhookReceipt
where
    T: ?marker::Sized,
{
}
impl repo_watch_webhook::RepoWatchWebhookReceipt {
    pub const fn sequence(self) -> nonzero::NonZeroU64;
    pub const fn received_at(self) -> time::SystemTime;
}
```

## RepoWatchWebhookAdmissionOutcome

```rust
pub enum RepoWatchWebhookAdmissionOutcome {
    Admitted(repo_watch_webhook::RepoWatchWebhookReceipt),
    EqualDuplicate(repo_watch_webhook::RepoWatchWebhookReceipt),
    Conflict,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch_webhook::RepoWatchWebhookAdmissionOutcome
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch_webhook::RepoWatchWebhookAdmissionOutcome
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch_webhook::RepoWatchWebhookAdmissionOutcome {}
impl<T> parse_display::IntoResult<T> for repo_watch_webhook::RepoWatchWebhookAdmissionOutcome {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch_webhook::RepoWatchWebhookAdmissionOutcome
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch_webhook::RepoWatchWebhookAdmissionOutcome {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch_webhook::RepoWatchWebhookAdmissionOutcome {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch_webhook::RepoWatchWebhookAdmissionOutcome
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch_webhook::RepoWatchWebhookAdmissionOutcome
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch_webhook::RepoWatchWebhookAdmissionOutcome
where
    T: ?marker::Sized,
{
}
```

## RecordedRepoWatchWebhookDisposition

```rust
pub struct RecordedRepoWatchWebhookDisposition {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch_webhook::RecordedRepoWatchWebhookDisposition
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch_webhook::RecordedRepoWatchWebhookDisposition
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch_webhook::RecordedRepoWatchWebhookDisposition {}
impl<T> parse_display::IntoResult<T> for repo_watch_webhook::RecordedRepoWatchWebhookDisposition {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch_webhook::RecordedRepoWatchWebhookDisposition
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch_webhook::RecordedRepoWatchWebhookDisposition {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch_webhook::RecordedRepoWatchWebhookDisposition {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch_webhook::RecordedRepoWatchWebhookDisposition
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch_webhook::RecordedRepoWatchWebhookDisposition
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch_webhook::RecordedRepoWatchWebhookDisposition
where
    T: ?marker::Sized,
{
}
impl repo_watch_webhook::RecordedRepoWatchWebhookDisposition {
    pub const fn disposition(&self) -> repo_watch_webhook::RepoWatchWebhookDisposition;
    pub fn outcome_code(&self) -> option::Option<&str>;
}
```

## RepoWatchWebhookProjectionFault

```rust
pub struct RepoWatchWebhookProjectionFault {/* private */}
impl<T> into_either::IntoEither for repo_watch_webhook::RepoWatchWebhookProjectionFault {}
impl<T> parse_display::IntoResult<T> for repo_watch_webhook::RepoWatchWebhookProjectionFault {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch_webhook::RepoWatchWebhookProjectionFault
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch_webhook::RepoWatchWebhookProjectionFault {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch_webhook::RepoWatchWebhookProjectionFault {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch_webhook::RepoWatchWebhookProjectionFault
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch_webhook::RepoWatchWebhookProjectionFault
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch_webhook::RepoWatchWebhookProjectionFault
where
    T: ?marker::Sized,
{
}
impl repo_watch_webhook::RepoWatchWebhookProjectionFault {
    pub async fn restore(
        self,
    ) -> result::Result<(), repo_watch_webhook::RepoWatchWebhookStoreError>;
}
```

## RepoWatchWebhookPendingPageSize

```rust
pub struct RepoWatchWebhookPendingPageSize(/* private */);
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch_webhook::RepoWatchWebhookPendingPageSize
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch_webhook::RepoWatchWebhookPendingPageSize
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch_webhook::RepoWatchWebhookPendingPageSize {}
impl<T> parse_display::IntoResult<T> for repo_watch_webhook::RepoWatchWebhookPendingPageSize {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch_webhook::RepoWatchWebhookPendingPageSize
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch_webhook::RepoWatchWebhookPendingPageSize {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch_webhook::RepoWatchWebhookPendingPageSize {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch_webhook::RepoWatchWebhookPendingPageSize
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch_webhook::RepoWatchWebhookPendingPageSize
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch_webhook::RepoWatchWebhookPendingPageSize
where
    T: ?marker::Sized,
{
}
impl repo_watch_webhook::RepoWatchWebhookPendingPageSize {
    pub fn try_new(
        value: nonzero::NonZeroU16,
    ) -> result::Result<Self, repo_watch_webhook::RepoWatchWebhookPageSizeError>;
    pub const fn get(self) -> u16;
}
```

## RepoWatchWebhookPageSizeError

```rust
pub struct RepoWatchWebhookPageSizeError;
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch_webhook::RepoWatchWebhookPageSizeError
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch_webhook::RepoWatchWebhookPageSizeError
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch_webhook::RepoWatchWebhookPageSizeError {}
impl<T> parse_display::IntoResult<T> for repo_watch_webhook::RepoWatchWebhookPageSizeError {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch_webhook::RepoWatchWebhookPageSizeError
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch_webhook::RepoWatchWebhookPageSizeError {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch_webhook::RepoWatchWebhookPageSizeError {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch_webhook::RepoWatchWebhookPageSizeError
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch_webhook::RepoWatchWebhookPageSizeError
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch_webhook::RepoWatchWebhookPageSizeError
where
    T: ?marker::Sized,
{
}
impl fmt::Display for repo_watch_webhook::RepoWatchWebhookPageSizeError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for repo_watch_webhook::RepoWatchWebhookPageSizeError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## PendingRepoWatchWebhookDelivery

```rust
pub struct PendingRepoWatchWebhookDelivery {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch_webhook::PendingRepoWatchWebhookDelivery
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch_webhook::PendingRepoWatchWebhookDelivery
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch_webhook::PendingRepoWatchWebhookDelivery {}
impl<T> parse_display::IntoResult<T> for repo_watch_webhook::PendingRepoWatchWebhookDelivery {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch_webhook::PendingRepoWatchWebhookDelivery
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch_webhook::PendingRepoWatchWebhookDelivery {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch_webhook::PendingRepoWatchWebhookDelivery {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch_webhook::PendingRepoWatchWebhookDelivery
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch_webhook::PendingRepoWatchWebhookDelivery
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch_webhook::PendingRepoWatchWebhookDelivery
where
    T: ?marker::Sized,
{
}
impl repo_watch_webhook::PendingRepoWatchWebhookDelivery {
    pub const fn key(&self) -> repo_watch_webhook::RepoWatchWebhookDeliveryKey;
    pub const fn repository(&self) -> &repo_watch::RepositorySlug;
    pub fn event_name(&self) -> &str;
    pub fn action_name(&self) -> option::Option<&str>;
    pub const fn body_digest(&self) -> &[u8; 32];
    pub const fn receipt(&self) -> repo_watch_webhook::RepoWatchWebhookReceipt;
    pub fn body(&self) -> &[u8];
}
```

## PendingRepoWatchWebhookReceipt

```rust
pub struct PendingRepoWatchWebhookReceipt {/* private */}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch_webhook::PendingRepoWatchWebhookReceipt
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch_webhook::PendingRepoWatchWebhookReceipt
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch_webhook::PendingRepoWatchWebhookReceipt {}
impl<T> parse_display::IntoResult<T> for repo_watch_webhook::PendingRepoWatchWebhookReceipt {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch_webhook::PendingRepoWatchWebhookReceipt
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch_webhook::PendingRepoWatchWebhookReceipt {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch_webhook::PendingRepoWatchWebhookReceipt {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch_webhook::PendingRepoWatchWebhookReceipt
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch_webhook::PendingRepoWatchWebhookReceipt
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch_webhook::PendingRepoWatchWebhookReceipt
where
    T: ?marker::Sized,
{
}
impl repo_watch_webhook::PendingRepoWatchWebhookReceipt {
    pub const fn key(&self) -> repo_watch_webhook::RepoWatchWebhookDeliveryKey;
    pub const fn receipt(&self) -> repo_watch_webhook::RepoWatchWebhookReceipt;
    pub const fn pending_for(&self) -> time::Duration;
}
```

## RepoWatchWebhookTargetedQuery

```rust
pub enum RepoWatchWebhookTargetedQuery {
    PullRequestHydration(repo_watch::PullRequestNumber),
    Mergeability(repo_watch::PullRequestNumber),
    CheckRollup(repo_watch::CommitSha),
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch_webhook::RepoWatchWebhookTargetedQuery
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch_webhook::RepoWatchWebhookTargetedQuery
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch_webhook::RepoWatchWebhookTargetedQuery {}
impl<T> parse_display::IntoResult<T> for repo_watch_webhook::RepoWatchWebhookTargetedQuery {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch_webhook::RepoWatchWebhookTargetedQuery
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch_webhook::RepoWatchWebhookTargetedQuery {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch_webhook::RepoWatchWebhookTargetedQuery {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch_webhook::RepoWatchWebhookTargetedQuery
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch_webhook::RepoWatchWebhookTargetedQuery
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch_webhook::RepoWatchWebhookTargetedQuery
where
    T: ?marker::Sized,
{
}
```

## RepoWatchWebhookParityCauseV1

```rust
pub enum RepoWatchWebhookParityCauseV1 {
    CompressedTransition,
    ContextDrift,
    PollOnlyFamily,
    CrossDrainShadowGap,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch_webhook::RepoWatchWebhookParityCauseV1
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch_webhook::RepoWatchWebhookParityCauseV1
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch_webhook::RepoWatchWebhookParityCauseV1 {}
impl<T> parse_display::IntoResult<T> for repo_watch_webhook::RepoWatchWebhookParityCauseV1 {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch_webhook::RepoWatchWebhookParityCauseV1
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch_webhook::RepoWatchWebhookParityCauseV1 {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch_webhook::RepoWatchWebhookParityCauseV1 {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch_webhook::RepoWatchWebhookParityCauseV1
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch_webhook::RepoWatchWebhookParityCauseV1
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch_webhook::RepoWatchWebhookParityCauseV1
where
    T: ?marker::Sized,
{
}
impl repo_watch_webhook::RepoWatchWebhookParityCauseV1 {
    pub const fn code(self) -> &'static str;
}
```

## RepoWatchWebhookProjection

```rust
pub enum RepoWatchWebhookProjection {
    Event {
        content_identity: repo_watch::RepoWatchEventContentIdentityV1,
        event_kind: repo_watch::RepoWatchEventKindNameV1,
        occurrence_key: boxed::Box<[u8]>,
        cause: option::Option<repo_watch_webhook::RepoWatchWebhookParityCauseV1>,
    },
    TargetedQuery(repo_watch_webhook::RepoWatchWebhookTargetedQuery),
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch_webhook::RepoWatchWebhookProjection
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch_webhook::RepoWatchWebhookProjection
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch_webhook::RepoWatchWebhookProjection {}
impl<T> parse_display::IntoResult<T> for repo_watch_webhook::RepoWatchWebhookProjection {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch_webhook::RepoWatchWebhookProjection
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch_webhook::RepoWatchWebhookProjection {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch_webhook::RepoWatchWebhookProjection {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch_webhook::RepoWatchWebhookProjection
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch_webhook::RepoWatchWebhookProjection
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch_webhook::RepoWatchWebhookProjection
where
    T: ?marker::Sized,
{
}
impl repo_watch_webhook::RepoWatchWebhookProjection {
    pub fn event(
        content_identity: repo_watch::RepoWatchEventContentIdentityV1,
        event_kind: repo_watch::RepoWatchEventKindNameV1,
        occurrence_key: vec::Vec<u8>,
        cause: option::Option<repo_watch_webhook::RepoWatchWebhookParityCauseV1>,
    ) -> result::Result<Self, repo_watch_webhook::RepoWatchWebhookRequestError>;
}
```

## RepoWatchWebhookDisposition

```rust
pub enum RepoWatchWebhookDisposition {
    Projected,
    Committed,
    DuplicateState,
    Superseded,
    Ignored,
    Quarantined,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch_webhook::RepoWatchWebhookDisposition
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch_webhook::RepoWatchWebhookDisposition
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch_webhook::RepoWatchWebhookDisposition {}
impl<T> parse_display::IntoResult<T> for repo_watch_webhook::RepoWatchWebhookDisposition {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch_webhook::RepoWatchWebhookDisposition
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch_webhook::RepoWatchWebhookDisposition {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch_webhook::RepoWatchWebhookDisposition {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch_webhook::RepoWatchWebhookDisposition
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch_webhook::RepoWatchWebhookDisposition
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch_webhook::RepoWatchWebhookDisposition
where
    T: ?marker::Sized,
{
}
```

## RepoWatchWebhookTerminalRequest

```rust
pub struct RepoWatchWebhookTerminalRequest {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch_webhook::RepoWatchWebhookTerminalRequest
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch_webhook::RepoWatchWebhookTerminalRequest
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch_webhook::RepoWatchWebhookTerminalRequest {}
impl<T> parse_display::IntoResult<T> for repo_watch_webhook::RepoWatchWebhookTerminalRequest {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch_webhook::RepoWatchWebhookTerminalRequest
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch_webhook::RepoWatchWebhookTerminalRequest {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch_webhook::RepoWatchWebhookTerminalRequest {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch_webhook::RepoWatchWebhookTerminalRequest
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch_webhook::RepoWatchWebhookTerminalRequest
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch_webhook::RepoWatchWebhookTerminalRequest
where
    T: ?marker::Sized,
{
}
impl repo_watch_webhook::RepoWatchWebhookTerminalRequest {
    pub fn try_new(
        projections: vec::Vec<repo_watch_webhook::RepoWatchWebhookProjection>,
        disposition: repo_watch_webhook::RepoWatchWebhookDisposition,
        outcome_code: option::Option<string::String>,
    ) -> result::Result<Self, repo_watch_webhook::RepoWatchWebhookRequestError>;
    pub fn projections(&self) -> &[repo_watch_webhook::RepoWatchWebhookProjection];
    pub const fn disposition(&self) -> repo_watch_webhook::RepoWatchWebhookDisposition;
    pub fn outcome_code(&self) -> option::Option<&str>;
}
```

## RepoWatchWebhookTerminalOutcome

```rust
pub enum RepoWatchWebhookTerminalOutcome {
    Recorded,
    AlreadyTerminal,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch_webhook::RepoWatchWebhookTerminalOutcome
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch_webhook::RepoWatchWebhookTerminalOutcome
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch_webhook::RepoWatchWebhookTerminalOutcome {}
impl<T> parse_display::IntoResult<T> for repo_watch_webhook::RepoWatchWebhookTerminalOutcome {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch_webhook::RepoWatchWebhookTerminalOutcome
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch_webhook::RepoWatchWebhookTerminalOutcome {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch_webhook::RepoWatchWebhookTerminalOutcome {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch_webhook::RepoWatchWebhookTerminalOutcome
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch_webhook::RepoWatchWebhookTerminalOutcome
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch_webhook::RepoWatchWebhookTerminalOutcome
where
    T: ?marker::Sized,
{
}
```

## RepoWatchWebhookStorageCorruption

```rust
pub enum RepoWatchWebhookStorageCorruption {
    InvalidHookId,
    InvalidReceiptSequence,
    InvalidRepository,
    InvalidBodyDigest,
    InvalidDisposition,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for repo_watch_webhook::RepoWatchWebhookStorageCorruption
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch_webhook::RepoWatchWebhookStorageCorruption
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch_webhook::RepoWatchWebhookStorageCorruption {}
impl<T> parse_display::IntoResult<T> for repo_watch_webhook::RepoWatchWebhookStorageCorruption {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch_webhook::RepoWatchWebhookStorageCorruption
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch_webhook::RepoWatchWebhookStorageCorruption {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch_webhook::RepoWatchWebhookStorageCorruption {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch_webhook::RepoWatchWebhookStorageCorruption
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch_webhook::RepoWatchWebhookStorageCorruption
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch_webhook::RepoWatchWebhookStorageCorruption
where
    T: ?marker::Sized,
{
}
impl fmt::Display for repo_watch_webhook::RepoWatchWebhookStorageCorruption {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for repo_watch_webhook::RepoWatchWebhookStorageCorruption {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## RepoWatchWebhookStoreError

```rust
pub enum RepoWatchWebhookStoreError {
    Database(error::Error),
    CommitAmbiguous(error::Error),
    Corruption(repo_watch_webhook::RepoWatchWebhookStorageCorruption),
    MissingDelivery,
}
// derives: fmt::Debug
impl<T> into_either::IntoEither for repo_watch_webhook::RepoWatchWebhookStoreError {}
impl<T> parse_display::IntoResult<T> for repo_watch_webhook::RepoWatchWebhookStoreError {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch_webhook::RepoWatchWebhookStoreError
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch_webhook::RepoWatchWebhookStoreError {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch_webhook::RepoWatchWebhookStoreError {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch_webhook::RepoWatchWebhookStoreError
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch_webhook::RepoWatchWebhookStoreError
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch_webhook::RepoWatchWebhookStoreError
where
    T: ?marker::Sized,
{
}
impl fmt::Display for repo_watch_webhook::RepoWatchWebhookStoreError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for repo_watch_webhook::RepoWatchWebhookStoreError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
impl convert::From<error::Error> for repo_watch_webhook::RepoWatchWebhookStoreError {
    fn from(error: error::Error) -> Self;
}
impl convert::From<repo_watch_webhook::RepoWatchWebhookStorageCorruption>
    for repo_watch_webhook::RepoWatchWebhookStoreError
{
    fn from(error: repo_watch_webhook::RepoWatchWebhookStorageCorruption) -> Self;
}
```

## PostgresRepoWatchWebhookStore

```rust
pub struct PostgresRepoWatchWebhookStore {/* private */}
// derives: clone::Clone, fmt::Debug
impl<T> from_ref::FromRef<T> for repo_watch_webhook::PostgresRepoWatchWebhookStore
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for repo_watch_webhook::PostgresRepoWatchWebhookStore
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for repo_watch_webhook::PostgresRepoWatchWebhookStore {}
impl<T> parse_display::IntoResult<T> for repo_watch_webhook::PostgresRepoWatchWebhookStore {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for repo_watch_webhook::PostgresRepoWatchWebhookStore
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for repo_watch_webhook::PostgresRepoWatchWebhookStore {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for repo_watch_webhook::PostgresRepoWatchWebhookStore {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for repo_watch_webhook::PostgresRepoWatchWebhookStore
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for repo_watch_webhook::PostgresRepoWatchWebhookStore
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for repo_watch_webhook::PostgresRepoWatchWebhookStore
where
    T: ?marker::Sized,
{
}
impl repo_watch_webhook::PostgresRepoWatchWebhookStore {
    pub const fn new(pool: sqlx_postgres::PgPool) -> Self;
    pub async fn admit(
        &self,
        request: &repo_watch_webhook::RepoWatchWebhookAdmission,
    ) -> result::Result<
        repo_watch_webhook::RepoWatchWebhookAdmissionOutcome,
        repo_watch_webhook::RepoWatchWebhookStoreError,
    >;
    pub async fn load_pending(
        &self,
        repository: &repo_watch::RepositorySlug,
        page_size: repo_watch_webhook::RepoWatchWebhookPendingPageSize,
        after_receipt: option::Option<nonzero::NonZeroU64>,
    ) -> result::Result<
        vec::Vec<repo_watch_webhook::PendingRepoWatchWebhookDelivery>,
        repo_watch_webhook::RepoWatchWebhookStoreError,
    >;
    pub async fn load_oldest_pending_receipt(
        &self,
        repository: &repo_watch::RepositorySlug,
    ) -> result::Result<
        option::Option<repo_watch_webhook::PendingRepoWatchWebhookReceipt>,
        repo_watch_webhook::RepoWatchWebhookStoreError,
    >;
    pub async fn load_disposition(
        &self,
        key: repo_watch_webhook::RepoWatchWebhookDeliveryKey,
    ) -> result::Result<
        option::Option<repo_watch_webhook::RecordedRepoWatchWebhookDisposition>,
        repo_watch_webhook::RepoWatchWebhookStoreError,
    >;
    pub async fn inject_projection_rejection(
        &self,
        delivery: repo_watch_webhook::RepoWatchWebhookDeliveryKey,
    ) -> result::Result<
        repo_watch_webhook::RepoWatchWebhookProjectionFault,
        repo_watch_webhook::RepoWatchWebhookStoreError,
    >;
    pub async fn inject_projection_wedge(
        &self,
        delivery: repo_watch_webhook::RepoWatchWebhookDeliveryKey,
        advisory_lock: i64,
    ) -> result::Result<(), repo_watch_webhook::RepoWatchWebhookStoreError>;
    pub async fn projection_wedge_is_reached(
        &self,
    ) -> result::Result<bool, repo_watch_webhook::RepoWatchWebhookStoreError>;
    pub async fn recorded_event_projection_count(
        &self,
        key: repo_watch_webhook::RepoWatchWebhookDeliveryKey,
    ) -> result::Result<u64, repo_watch_webhook::RepoWatchWebhookStoreError>;
    pub async fn record_terminal(
        &self,
        key: repo_watch_webhook::RepoWatchWebhookDeliveryKey,
        request: &repo_watch_webhook::RepoWatchWebhookTerminalRequest,
    ) -> result::Result<
        repo_watch_webhook::RepoWatchWebhookTerminalOutcome,
        repo_watch_webhook::RepoWatchWebhookStoreError,
    >;
    pub async fn terminal_disposition_exists(
        &self,
        key: repo_watch_webhook::RepoWatchWebhookDeliveryKey,
    ) -> result::Result<bool, repo_watch_webhook::RepoWatchWebhookStoreError>;
    pub async fn purge_expired_payloads(
        &self,
    ) -> result::Result<u64, repo_watch_webhook::RepoWatchWebhookStoreError>;
}
```
