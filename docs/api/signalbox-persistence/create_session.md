<!-- generated by scripts/render_domain_spine.py, do not edit -->

# create_session

## CreateSessionHandlingOutcome

```rust
pub enum CreateSessionHandlingOutcome {
    Applied(session::CreateSessionAppliedResult),
    ConflictingReuse {
        command_id: signalbox_domain::DurableCommandId,
    },
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for create_session::CreateSessionHandlingOutcome
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for create_session::CreateSessionHandlingOutcome
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for create_session::CreateSessionHandlingOutcome {}
impl<T> parse_display::IntoResult<T> for create_session::CreateSessionHandlingOutcome {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for create_session::CreateSessionHandlingOutcome
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for create_session::CreateSessionHandlingOutcome {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for create_session::CreateSessionHandlingOutcome {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for create_session::CreateSessionHandlingOutcome
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for create_session::CreateSessionHandlingOutcome
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for create_session::CreateSessionHandlingOutcome
where
    T: ?marker::Sized,
{
}
```

## CreateSessionCorruption

```rust
pub enum CreateSessionCorruption {
    Missing(&'static str),
    Unsupported {
        field: &'static str,
        value: string::String,
    },
    Inconsistent(&'static str),
    InvalidOrdinal {
        field: &'static str,
        reason: mapping::PositiveOrdinalMappingError,
    },
    Domain(session::CreateSessionReconstitutionFailure),
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for create_session::CreateSessionCorruption
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for create_session::CreateSessionCorruption
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for create_session::CreateSessionCorruption {}
impl<T> parse_display::IntoResult<T> for create_session::CreateSessionCorruption {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for create_session::CreateSessionCorruption
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for create_session::CreateSessionCorruption {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for create_session::CreateSessionCorruption {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for create_session::CreateSessionCorruption
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for create_session::CreateSessionCorruption
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for create_session::CreateSessionCorruption
where
    T: ?marker::Sized,
{
}
impl fmt::Display for create_session::CreateSessionCorruption {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for create_session::CreateSessionCorruption {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## CreateSessionRepositoryError

```rust
pub enum CreateSessionRepositoryError {
    Database(error::Error),
    CommitAmbiguous(error::Error),
    DifferentCommandKind {
        command_id: signalbox_domain::DurableCommandId,
    },
    Corruption(create_session::CreateSessionCorruption),
}
// derives: fmt::Debug
impl<T> into_either::IntoEither for create_session::CreateSessionRepositoryError {}
impl<T> parse_display::IntoResult<T> for create_session::CreateSessionRepositoryError {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for create_session::CreateSessionRepositoryError
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for create_session::CreateSessionRepositoryError {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for create_session::CreateSessionRepositoryError {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for create_session::CreateSessionRepositoryError
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for create_session::CreateSessionRepositoryError
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for create_session::CreateSessionRepositoryError
where
    T: ?marker::Sized,
{
}
impl fmt::Display for create_session::CreateSessionRepositoryError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for create_session::CreateSessionRepositoryError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
impl convert::From<error::Error> for create_session::CreateSessionRepositoryError {
    fn from(error: error::Error) -> Self;
}
impl convert::From<create_session::CreateSessionCorruption>
    for create_session::CreateSessionRepositoryError
{
    fn from(error: create_session::CreateSessionCorruption) -> Self;
}
```

## CreateSessionRepository

```rust
pub struct CreateSessionRepository {/* private */}
// derives: clone::Clone, fmt::Debug
impl<T> from_ref::FromRef<T> for create_session::CreateSessionRepository
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for create_session::CreateSessionRepository
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for create_session::CreateSessionRepository {}
impl<T> parse_display::IntoResult<T> for create_session::CreateSessionRepository {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for create_session::CreateSessionRepository
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for create_session::CreateSessionRepository {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for create_session::CreateSessionRepository {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for create_session::CreateSessionRepository
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for create_session::CreateSessionRepository
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for create_session::CreateSessionRepository
where
    T: ?marker::Sized,
{
}
impl create_session::CreateSessionRepository {
    pub fn new(pool: sqlx_postgres::PgPool, credential_pin: SessionCredentialPin) -> Self;
    pub async fn handle(
        &self,
        prepared: session::PreparedCreateSession,
    ) -> result::Result<
        create_session::CreateSessionHandlingOutcome,
        create_session::CreateSessionRepositoryError,
    >;
    pub async fn load(
        &self,
        command_id: signalbox_domain::DurableCommandId,
    ) -> result::Result<
        option::Option<session::ReconstitutedSessionCreation>,
        create_session::CreateSessionRepositoryError,
    >;
}
impl create_session::CreateSessionTransaction for create_session::CreateSessionRepository {
    type Error = create_session::CreateSessionRepositoryError;
    async fn handle(
        &mut self,
        prepared: session::PreparedCreateSession,
    ) -> result::Result<
        create_session::CreateSessionOutcome,
        <Self as create_session::CreateSessionTransaction>::Error,
    >;
}
```
