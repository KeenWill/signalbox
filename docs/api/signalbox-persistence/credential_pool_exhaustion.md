<!-- generated by scripts/render_domain_spine.py, do not edit -->

# credential_pool_exhaustion

## CredentialPoolExclusion

```rust
pub enum CredentialPoolExclusion {
    ProfileQuarantine {
        record_generation: option::Option<u64>,
    },
    MembershipExclusion {
        record_generation: option::Option<u64>,
    },
    SessionDisplacement {
        record_generation: option::Option<u64>,
    },
    ChainExclusion {
        predecessor_model_call_id: uuid::Uuid,
    },
    TransientExclusion {
        observation_model_call_id: uuid::Uuid,
    },
    HeadroomReserve {
        observed_headroom_percent: i64,
        reserve_percent: u8,
    },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize, de::Deserialize<'de>
```

## CredentialPoolMemberEvidence

```rust
pub struct CredentialPoolMemberEvidence {
    pub profile: string::String,
    pub reset_at_unix_ms: option::Option<i64>,
    pub exclusion: credential_pool_exhaustion::CredentialPoolExclusion,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize, de::Deserialize<'de>
```

## CredentialPoolExhaustion

```rust
pub struct CredentialPoolExhaustion {
    pub turn_id: uuid::Uuid,
    pub terminal_attempt_id: uuid::Uuid,
    pub terminal_frontier_id: uuid::Uuid,
    pub failure_entry_id: uuid::Uuid,
    pub pool_policy_id: uuid::Uuid,
    pub policy_members: vec::Vec<string::String>,
    pub members: vec::Vec<credential_pool_exhaustion::CredentialPoolMemberEvidence>,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## CredentialPoolEvidenceError

```rust
pub enum CredentialPoolEvidenceError {
    Database(error::Error),
    Corruption,
}
// derives: fmt::Debug
impl convert::From<error::Error> for credential_pool_exhaustion::CredentialPoolEvidenceError {
    fn from(error: error::Error) -> Self;
}
impl fmt::Display for credential_pool_exhaustion::CredentialPoolEvidenceError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for credential_pool_exhaustion::CredentialPoolEvidenceError {}
```

## load_policy

```rust
pub async fn load_policy(
    connection: &mut connection::PgConnection,
    policy_id: uuid::Uuid,
) -> result::Result<
    option::Option<vec::Vec<string::String>>,
    credential_pool_exhaustion::CredentialPoolEvidenceError,
>;
```

## read_policy

```rust
pub async fn read_policy(
    connection: &mut connection::PgConnection,
    session: uuid::Uuid,
    turn: uuid::Uuid,
    policy: uuid::Uuid,
) -> result::Result<
    option::Option<vec::Vec<string::String>>,
    credential_pool_exhaustion::CredentialPoolEvidenceError,
>;
```

## load

```rust
pub async fn load(
    connection: &mut connection::PgConnection,
    session: uuid::Uuid,
    turn: uuid::Uuid,
) -> result::Result<
    option::Option<credential_pool_exhaustion::CredentialPoolExhaustion>,
    credential_pool_exhaustion::CredentialPoolEvidenceError,
>;
```
