<!-- generated by scripts/render_domain_spine.py, do not edit -->

# root

## MIGRATOR

```rust
pub static MIGRATOR: migrator::Migrator;
```

## migrate

```rust
pub async fn migrate(pool: &sqlx_postgres::PgPool) -> result::Result<(), error::MigrateError>;
```

## connect_production

```rust
pub async fn connect_production(
    database_url: &str,
) -> result::Result<sqlx_postgres::PgPool, error::Error>;
```

## production_connection_environment_variables

```rust
pub fn production_connection_environment_variables() -> impl iterator::Iterator<Item = &'static str>;
```

## production_connection_options

```rust
pub fn production_connection_options(
    database_url: &str,
) -> result::Result<options::PgConnectOptions, error::Error>;
```

## production_connection_ambient_warnings

```rust
pub fn production_connection_ambient_warnings() -> vec::Vec<&'static str>;
```

## local_test_connection_options

```rust
pub fn local_test_connection_options(
    database_url: &str,
) -> result::Result<options::PgConnectOptions, error::Error>;
```

## DISPOSABLE_TEST_CONTAINER_LABEL_KEY

```rust
pub const DISPOSABLE_TEST_CONTAINER_LABEL_KEY: &str;
```

## DISPOSABLE_TEST_CONTAINER_LABEL_VALUE

```rust
pub const DISPOSABLE_TEST_CONTAINER_LABEL_VALUE: &str;
```

## DISPOSABLE_TEST_CONTAINER_LIFETIME_HOURS

```rust
pub const DISPOSABLE_TEST_CONTAINER_LIFETIME_HOURS: u64;
```

## outlives_the_disposable_container_sweep

```rust
pub fn outlives_the_disposable_container_sweep(lifetime: time::Duration) -> bool;
```

## disposable_test_container_labels

```rust
pub fn disposable_test_container_labels() -> vec::Vec<(&'static str, &'static str)>;
```

## disposable_test_container_labels_for_command

```rust
pub fn disposable_test_container_labels_for_command(
    command: option::Option<&str>,
) -> vec::Vec<(&'static str, &'static str)>;
```

## disposable_postgres_server_args

```rust
#[cfg(feature = "postgres-integration")]
pub fn disposable_postgres_server_args() -> [&'static str; 6];
```

## disposable_postgres_state_tmpfs

```rust
#[cfg(feature = "postgres-integration")]
pub fn disposable_postgres_state_tmpfs(ceiling_bytes: option::Option<i64>) -> mounts::Mount;
```

## disposable_postgres_state_tmpfs_from_example

```rust
#[cfg(feature = "postgres-integration")]
pub fn disposable_postgres_state_tmpfs_from_example() -> error::Result<mounts::Mount>;
```
