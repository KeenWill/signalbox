<!-- generated by scripts/render_domain_spine.py, do not edit -->

# mapping

## instruction_root_kind_from_str

```rust
pub fn instruction_root_kind_from_str(
    value: &str,
) -> option::Option<signalbox_domain::InstructionDiscoveryRootKind>;
```

## instruction_bundle_kind_from_str

```rust
pub fn instruction_bundle_kind_from_str(
    value: &str,
) -> option::Option<signalbox_domain::InstructionBundleKind>;
```

## instruction_finding_kind_from_str

```rust
pub fn instruction_finding_kind_from_str(
    value: &str,
) -> option::Option<signalbox_application::InstructionDiscoveryFindingKind>;
```

## DelegationPolicyStorageKind

```rust
pub enum DelegationPolicyStorageKind {
    Background,
    Bound,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## delegation_policy_kind_to_str

```rust
pub const fn delegation_policy_kind_to_str(
    value: mapping::DelegationPolicyStorageKind,
) -> &'static str;
```

## delegation_policy_kind_from_str

```rust
pub fn delegation_policy_kind_from_str(
    value: &str,
) -> option::Option<mapping::DelegationPolicyStorageKind>;
```

## turn_terminal_cause_to_str

```rust
pub const fn turn_terminal_cause_to_str(value: signalbox_domain::TurnTerminalCause)
    -> &'static str;
```

## turn_terminal_cause_from_str

```rust
pub fn turn_terminal_cause_from_str(
    value: &str,
) -> option::Option<signalbox_domain::TurnTerminalCause>;
```

## DelegationUpdateStorageKind

```rust
pub enum DelegationUpdateStorageKind {
    ChildSpawned,
    ChildWaiting,
    ChildLifecycleDisposition,
    ChildResult,
    SessionMessage,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## delegation_update_kind_to_str

```rust
pub const fn delegation_update_kind_to_str(
    value: mapping::DelegationUpdateStorageKind,
) -> &'static str;
```

## delegation_update_kind_from_str

```rust
pub fn delegation_update_kind_from_str(
    value: &str,
) -> option::Option<mapping::DelegationUpdateStorageKind>;
```

## DelegationWakeStorageKind

```rust
pub enum DelegationWakeStorageKind {
    Result,
    Message,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## delegation_wake_subject_to_str

```rust
pub const fn delegation_wake_subject_to_str(
    value: mapping::DelegationWakeStorageKind,
) -> &'static str;
```

## delegation_wake_subject_from_str

```rust
pub fn delegation_wake_subject_from_str(
    value: &str,
) -> option::Option<mapping::DelegationWakeStorageKind>;
```

## GoalEventDiscriminator

```rust
pub enum GoalEventDiscriminator {
    Commissioned,
    Blocked,
    Resumed,
    Achieved,
    UserStopped,
    Superseded,
    SessionClosed,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## PositiveOrdinalMappingError

```rust
pub enum PositiveOrdinalMappingError {
    NonPositive,
    Fractional,
    OutOfRange,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for mapping::PositiveOrdinalMappingError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for mapping::PositiveOrdinalMappingError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## defaults_version_to_numeric

```rust
pub fn defaults_version_to_numeric(
    value: signalbox_domain::SessionConfigurationDefaultsVersion,
) -> decimal::Decimal;
```

## defaults_version_from_numeric

```rust
pub fn defaults_version_from_numeric(
    value: decimal::Decimal,
) -> result::Result<
    signalbox_domain::SessionConfigurationDefaultsVersion,
    mapping::PositiveOrdinalMappingError,
>;
```

## input_position_to_numeric

```rust
pub fn input_position_to_numeric(value: signalbox_domain::SessionInputPosition)
    -> decimal::Decimal;
```

## input_position_from_numeric

```rust
pub fn input_position_from_numeric(
    value: decimal::Decimal,
) -> result::Result<signalbox_domain::SessionInputPosition, mapping::PositiveOrdinalMappingError>;
```

## dangerous_tool_auto_approval_to_str

```rust
pub fn dangerous_tool_auto_approval_to_str(
    value: signalbox_domain::DangerousToolAutoApproval,
) -> &'static str;
```

## dangerous_tool_auto_approval_from_str

```rust
pub fn dangerous_tool_auto_approval_from_str(
    value: &str,
) -> option::Option<signalbox_domain::DangerousToolAutoApproval>;
```

## session_id_to_uuid

```rust
pub fn session_id_to_uuid(value: signalbox_domain::SessionId) -> uuid::Uuid;
```

## session_id_from_uuid

```rust
pub fn session_id_from_uuid(value: uuid::Uuid) -> signalbox_domain::SessionId;
```

## accepted_input_id_to_uuid

```rust
pub fn accepted_input_id_to_uuid(value: signalbox_domain::AcceptedInputId) -> uuid::Uuid;
```

## accepted_input_id_from_uuid

```rust
pub fn accepted_input_id_from_uuid(value: uuid::Uuid) -> signalbox_domain::AcceptedInputId;
```

## turn_id_to_uuid

```rust
pub fn turn_id_to_uuid(value: signalbox_domain::TurnId) -> uuid::Uuid;
```

## turn_id_from_uuid

```rust
pub fn turn_id_from_uuid(value: uuid::Uuid) -> signalbox_domain::TurnId;
```

## tool_request_id_to_uuid

```rust
pub fn tool_request_id_to_uuid(value: signalbox_domain::ToolRequestId) -> uuid::Uuid;
```

## tool_request_id_from_uuid

```rust
pub fn tool_request_id_from_uuid(value: uuid::Uuid) -> signalbox_domain::ToolRequestId;
```

## tool_attempt_id_to_uuid

```rust
pub fn tool_attempt_id_to_uuid(value: signalbox_domain::ToolAttemptId) -> uuid::Uuid;
```

## tool_attempt_id_from_uuid

```rust
pub fn tool_attempt_id_from_uuid(value: uuid::Uuid) -> signalbox_domain::ToolAttemptId;
```

## DurableCommandIdMappingError

```rust
pub enum DurableCommandIdMappingError {
    SentinelUuid,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for mapping::DurableCommandIdMappingError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for mapping::DurableCommandIdMappingError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## durable_command_id_to_uuid

```rust
pub fn durable_command_id_to_uuid(value: signalbox_domain::DurableCommandId) -> uuid::Uuid;
```

## durable_command_id_from_uuid

```rust
pub fn durable_command_id_from_uuid(
    value: uuid::Uuid,
) -> result::Result<signalbox_domain::DurableCommandId, mapping::DurableCommandIdMappingError>;
```
