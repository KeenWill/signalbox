<!-- generated by scripts/render_domain_spine.py, do not edit -->

# credential_exclusions

## CredentialExclusionTarget

```rust
pub enum CredentialExclusionTarget {
    ProfileQuarantine {
        profile: string::String,
        record_generation: u64,
    },
    MembershipExclusion {
        pool_policy_id: uuid::Uuid,
        profile: string::String,
        record_generation: u64,
    },
    SessionDisplacement {
        session_id: uuid::Uuid,
        pool_policy_id: uuid::Uuid,
        profile: string::String,
        record_generation: u64,
    },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, cmp::Ord, cmp::PartialOrd, ser::Serialize, de::Deserialize<'de>
impl credential_exclusions::CredentialExclusionTarget {
    pub fn profile(&self) -> &str;
    pub const fn generation(&self) -> u64;
}
```

## ClearCredentialExclusion

```rust
pub struct ClearCredentialExclusion {
    pub command_id: signalbox_domain::DurableCommandId,
    pub target: credential_exclusions::CredentialExclusionTarget,
}
// derives: clone::Clone, fmt::Debug
impl cmp::PartialEq for credential_exclusions::ClearCredentialExclusion {
    fn eq(&self, other: &Self) -> bool;
}
impl cmp::Eq for credential_exclusions::ClearCredentialExclusion {}
```

## ClearCredentialExclusionOutcome

```rust
pub enum ClearCredentialExclusionOutcome {
    Cleared,
    AlreadyCleared,
    StaleGeneration,
    UnknownCredentialExclusion,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize, de::Deserialize<'de>
```

## ClearCredentialExclusionResult

```rust
pub enum ClearCredentialExclusionResult {
    Recorded(credential_exclusions::ClearCredentialExclusionOutcome),
    ConflictingReuse,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## CredentialExclusionPage

```rust
pub struct CredentialExclusionPage {
    pub exclusions: vec::Vec<credential_exclusions::CredentialExclusionTarget>,
    pub next_after: option::Option<credential_exclusions::CredentialExclusionTarget>,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## CredentialExclusionError

```rust
pub enum CredentialExclusionError {
    Database(error::Error),
    CommitAmbiguous(error::Error),
    Corruption,
    InvalidRequest,
}
// derives: fmt::Debug
impl fmt::Display for credential_exclusions::CredentialExclusionError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for credential_exclusions::CredentialExclusionError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
impl convert::From<error::Error> for credential_exclusions::CredentialExclusionError {
    fn from(error: error::Error) -> Self;
}
```

## list

```rust
pub async fn list(
    pool: &sqlx_postgres::PgPool,
    page_size: u32,
    after: option::Option<&credential_exclusions::CredentialExclusionTarget>,
) -> result::Result<
    credential_exclusions::CredentialExclusionPage,
    credential_exclusions::CredentialExclusionError,
>;
```

## clear

```rust
pub async fn clear(
    pool: &sqlx_postgres::PgPool,
    command: credential_exclusions::ClearCredentialExclusion,
) -> result::Result<
    credential_exclusions::ClearCredentialExclusionResult,
    credential_exclusions::CredentialExclusionError,
>;
```
