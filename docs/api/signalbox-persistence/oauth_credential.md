<!-- generated by scripts/render_domain_spine.py, do not edit -->

# oauth_credential

## OauthCredentialOperation

```rust
pub enum OauthCredentialOperation {
    Provision,
    Reprovision,
    Delete,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## OauthCredentialCommand

```rust
pub struct OauthCredentialCommand {
    pub command_id: signalbox_domain::DurableCommandId,
    pub operation: oauth_credential::OauthCredentialOperation,
    pub profile: string::String,
}
// derives: clone::Clone, fmt::Debug
impl oauth_credential::OauthCredentialCommand {
    pub fn same_payload(&self, other: &Self) -> bool;
}
```

## OauthCredentialOutcome

```rust
pub enum OauthCredentialOutcome {
    Provisioned,
    AlreadyProvisioned,
    Reprovisioned,
    Deleted,
    AlreadyDeleted,
    NotProvisioned,
    Abandoned,
    Superseded,
    Failed(oauth_credential::OauthCredentialFailure),
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## OauthCredentialFailure

```rust
pub enum OauthCredentialFailure {
    UnknownProfile,
    NonOauthProfile,
    RegistrationChanged,
    DeviceEndpointRejected,
    DeviceEndpointFailed,
    AccessDenied,
    PollingExpired,
    TokenEndpointFailed,
    TokenResponseWithoutIdentity,
    AccountIndependenceFailed,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## OauthCredentialHandlingOutcome

```rust
pub enum OauthCredentialHandlingOutcome {
    Recorded(oauth_credential::OauthCredentialOutcome),
    Pending,
    ConflictingReuse,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## OauthCredentialRepositoryError

```rust
pub enum OauthCredentialRepositoryError {
    Database(error::Error),
    CommitAmbiguous(error::Error),
    Corruption,
    InvalidProfile,
}
// derives: fmt::Debug
impl fmt::Display for oauth_credential::OauthCredentialRepositoryError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for oauth_credential::OauthCredentialRepositoryError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
impl convert::From<error::Error> for oauth_credential::OauthCredentialRepositoryError {
    fn from(error: error::Error) -> Self;
}
```

## OauthCredentialRepository

```rust
pub struct OauthCredentialRepository {/* private */}
// derives: clone::Clone, fmt::Debug
impl oauth_credential::OauthCredentialRepository {
    pub const fn new(pool: sqlx_postgres::PgPool) -> Self;
    pub async fn record(
        &self,
        command: &oauth_credential::OauthCredentialCommand,
        evaluate: impl function::FnOnce() -> oauth_credential::OauthCredentialOutcome,
    ) -> result::Result<
        oauth_credential::OauthCredentialHandlingOutcome,
        oauth_credential::OauthCredentialRepositoryError,
    >;
}
```
