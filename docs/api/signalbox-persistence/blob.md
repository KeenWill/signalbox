<!-- generated by scripts/render_domain_spine.py, do not edit -->

# blob

## BlobStoreBindingRecord

```rust
pub struct BlobStoreBindingRecord {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl blob::BlobStoreBindingRecord {
    pub const fn store(&self) -> &signalbox_blob_store::BlobStoreName;
}
impl blob::BlobStoreBindingRecord {
    pub const fn new(store: signalbox_blob_store::BlobStoreName, namespace_id: uuid::Uuid) -> Self;
    pub const fn namespace_id(&self) -> uuid::Uuid;
}
```

## BlobReplicaRecord

```rust
pub struct BlobReplicaRecord {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl blob::BlobReplicaRecord {
    pub const fn store(&self) -> &signalbox_blob_store::BlobStoreName;
    pub const fn object_key(&self) -> &signalbox_blob_store::BlobObjectKey;
}
impl blob::BlobReplicaRecord {
    pub const fn new(
        store: signalbox_blob_store::BlobStoreName,
        object_key: signalbox_blob_store::BlobObjectKey,
    ) -> Self;
}
```

## BlobCatalogEntry

```rust
pub struct BlobCatalogEntry {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl blob::BlobCatalogEntry {
    pub fn replicas(&self) -> &[blob::BlobReplicaRecord];
}
impl blob::BlobCatalogEntry {
    pub const fn expected(&self) -> signalbox_blob_store::ExpectedBlob;
    pub fn replica_in_store(
        &self,
        store: &signalbox_blob_store::BlobStoreName,
    ) -> option::Option<&blob::BlobReplicaRecord>;
}
```

## BlobCatalogCorruption

```rust
pub enum BlobCatalogCorruption {
    InvalidDigest,
    InvalidByteLength,
    InvalidStoreName,
    StoreNamespaceMismatch,
    NamespaceStoreMismatch,
    ReplicaBindingMismatch,
    InvalidObjectKey,
    BlobLengthMismatch,
    ReplicaKeyMismatch,
    ObjectKeyCollision,
    BlobWithoutReplica,
    PartialReplica,
    MissingRegisteredReplica,
    StoreLimitExceeded,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for blob::BlobCatalogCorruption {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for blob::BlobCatalogCorruption {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## BlobCatalogRepositoryError

```rust
pub enum BlobCatalogRepositoryError {
    Database(error::Error),
    CommitAmbiguous(error::Error),
    Corruption(blob::BlobCatalogCorruption),
}
// derives: fmt::Debug
impl fmt::Display for blob::BlobCatalogRepositoryError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for blob::BlobCatalogRepositoryError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
impl blob::BlobCatalogRepositoryError {
    pub const fn corruption(&self) -> option::Option<blob::BlobCatalogCorruption>;
}
impl convert::From<error::Error> for blob::BlobCatalogRepositoryError {
    fn from(error: error::Error) -> Self;
}
impl convert::From<blob::BlobCatalogCorruption> for blob::BlobCatalogRepositoryError {
    fn from(error: blob::BlobCatalogCorruption) -> Self;
}
```

## BlobCatalogRepository

```rust
pub struct BlobCatalogRepository {/* private */}
// derives: clone::Clone, fmt::Debug
impl blob::BlobCatalogRepository {
    pub const fn new(pool: sqlx_postgres::PgPool) -> Self;
    #[cfg(feature = "test-support")]
    pub async fn inject_registration_fault(
        &self,
    ) -> result::Result<blob::BlobCatalogRegistrationFault, blob::BlobCatalogRepositoryError>;
    pub async fn is_empty(&self) -> result::Result<bool, blob::BlobCatalogRepositoryError>;
    pub async fn recorded_store_bindings(
        &self,
    ) -> result::Result<boxed::Box<[blob::BlobStoreBindingRecord]>, blob::BlobCatalogRepositoryError>;
    pub async fn register_store_binding(
        &self,
        binding: blob::BlobStoreBindingRecord,
    ) -> result::Result<blob::BlobStoreBindingRecord, blob::BlobCatalogRepositoryError>;
    pub async fn find(
        &self,
        digest: signalbox_domain::BlobDigest,
    ) -> result::Result<option::Option<blob::BlobCatalogEntry>, blob::BlobCatalogRepositoryError>;
    pub async fn register_verified_replica(
        &self,
        expected: signalbox_blob_store::ExpectedBlob,
        binding: blob::BlobStoreBindingRecord,
        replica: blob::BlobReplicaRecord,
    ) -> result::Result<blob::BlobCatalogEntry, blob::BlobCatalogRepositoryError>;
}
```

## BlobCatalogRegistrationFault

```rust
#[cfg(feature = "test-support")]
pub struct BlobCatalogRegistrationFault {/* private */}
#[cfg(feature = "test-support")]
impl blob::BlobCatalogRegistrationFault {
    pub async fn restore(self) -> result::Result<(), blob::BlobCatalogRepositoryError>;
}
```
