<!-- generated by scripts/render_domain_spine.py, do not edit -->

# lifecycle_metrics

## LifecycleMetricsCorruption

```rust
pub enum LifecycleMetricsCorruption {
    Missing(&'static str),
    Invalid(&'static str),
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for lifecycle_metrics::LifecycleMetricsCorruption {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for lifecycle_metrics::LifecycleMetricsCorruption {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## LifecycleMetricsError

```rust
pub enum LifecycleMetricsError {
    Database(error::Error),
    Corruption(lifecycle_metrics::LifecycleMetricsCorruption),
}
// derives: fmt::Debug
impl fmt::Display for lifecycle_metrics::LifecycleMetricsError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for lifecycle_metrics::LifecycleMetricsError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
impl convert::From<error::Error> for lifecycle_metrics::LifecycleMetricsError {
    fn from(error: error::Error) -> Self;
}
impl convert::From<lifecycle_metrics::LifecycleMetricsCorruption>
    for lifecycle_metrics::LifecycleMetricsError
{
    fn from(error: lifecycle_metrics::LifecycleMetricsCorruption) -> Self;
}
```

## LifecycleRate

```rust
pub struct LifecycleRate {/* private */}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl lifecycle_metrics::LifecycleRate {
    pub const fn numerator(self) -> u64;
    pub const fn denominator(self) -> u64;
}
impl lifecycle_metrics::LifecycleRate {
    pub const fn parts_per_million(self) -> option::Option<u64>;
}
```

## LifecycleWeeklyMetrics

```rust
pub struct LifecycleWeeklyMetrics {/* private */}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl lifecycle_metrics::LifecycleWeeklyMetrics {
    pub const fn week_start(&self) -> primitive_date_time::PrimitiveDateTime;
    pub fn week_start_date(&self) -> string::String;
    pub const fn completion_failure(&self) -> lifecycle_metrics::LifecycleRate;
    pub const fn failed_unknown_share(&self) -> lifecycle_metrics::LifecycleRate;
    pub const fn overflow_incidence(&self) -> lifecycle_metrics::LifecycleRate;
    pub const fn finish_given_overflow(&self) -> lifecycle_metrics::LifecycleRate;
    pub const fn wall_rate(&self) -> lifecycle_metrics::LifecycleRate;
    pub const fn wall_occurrences(&self) -> u64;
    pub const fn turn_cause_completeness(&self) -> lifecycle_metrics::LifecycleRate;
    pub const fn model_call_cause_completeness(&self) -> lifecycle_metrics::LifecycleRate;
}
```

## LifecycleNonTerminalState

```rust
pub enum LifecycleNonTerminalState {
    Created,
    Dispatched,
    Active,
    Waiting,
    Recovering,
    Blocked,
    Parked,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## LifecycleDeadlineViolation

```rust
pub struct LifecycleDeadlineViolation {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl lifecycle_metrics::LifecycleDeadlineViolation {
    pub const fn session(&self) -> signalbox_domain::SessionId;
    pub const fn state(&self) -> lifecycle_metrics::LifecycleNonTerminalState;
    pub fn deadline_kind(&self) -> option::Option<&str>;
    pub const fn expired_for_seconds(&self) -> option::Option<u64>;
}
```

## LifecycleMetricsReport

```rust
pub struct LifecycleMetricsReport {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl lifecycle_metrics::LifecycleMetricsReport {
    pub fn weeks(&self) -> &[lifecycle_metrics::LifecycleWeeklyMetrics];
}
impl lifecycle_metrics::LifecycleMetricsReport {
    pub const fn nonterminal_past_deadline(&self) -> u64;
    pub fn latest_measured<Select>(
        &self,
        select: Select,
    ) -> option::Option<lifecycle_metrics::LifecycleRate>
    where
        Select: function::Fn(
            &lifecycle_metrics::LifecycleWeeklyMetrics,
        ) -> lifecycle_metrics::LifecycleRate;
}
```

## LifecycleMetricsRepository

```rust
pub struct LifecycleMetricsRepository {/* private */}
// derives: clone::Clone, fmt::Debug
impl lifecycle_metrics::LifecycleMetricsRepository {
    pub const fn new(pool: sqlx_postgres::PgPool) -> Self;
    pub async fn read(
        &self,
    ) -> result::Result<
        lifecycle_metrics::LifecycleMetricsReport,
        lifecycle_metrics::LifecycleMetricsError,
    >;
}
```
