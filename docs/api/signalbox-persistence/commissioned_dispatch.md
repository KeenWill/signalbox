<!-- generated by scripts/render_domain_spine.py, do not edit -->

# commissioned_dispatch

## CommissionDispatchOutcome

```rust
pub enum CommissionDispatchOutcome {
    Dispatched {
        dispatch: signalbox_domain::CommissionedDispatchId,
        session: signalbox_domain::SessionId,
    },
    Replayed {
        dispatch: signalbox_domain::CommissionedDispatchId,
        session: signalbox_domain::SessionId,
    },
    ConflictingReuse,
    TargetBusy {
        session: signalbox_domain::SessionId,
    },
    TargetCoolingOff {
        session: signalbox_domain::SessionId,
    },
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## CommissionedDispatchRepositoryError

```rust
pub enum CommissionedDispatchRepositoryError {
    Database {
        source: error::Error,
        commit_ambiguous: bool,
    },
    SessionCreation(create_session::CreateSessionRepositoryError),
    InitialInput(submit_input::SubmitInputRepositoryError),
    GoalCommission(goal::GoalRepositoryError),
    Corruption(&'static str),
}
// derives: fmt::Debug
impl fmt::Display for commissioned_dispatch::CommissionedDispatchRepositoryError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for commissioned_dispatch::CommissionedDispatchRepositoryError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
impl commissioned_dispatch::CommissionedDispatchRepositoryError {
    pub const fn commit_ambiguous(&self) -> bool;
}
impl convert::From<error::Error> for commissioned_dispatch::CommissionedDispatchRepositoryError {
    fn from(source: error::Error) -> Self;
}
```

## PostgresCommissionedDispatchStore

```rust
pub struct PostgresCommissionedDispatchStore {/* private */}
// derives: clone::Clone, fmt::Debug
impl commissioned_dispatch::PostgresCommissionedDispatchStore {
    pub fn new(pool: sqlx_postgres::PgPool, credential_pin: SessionCredentialPin) -> Self;
    pub async fn load(
        &self,
        command: signalbox_domain::DurableCommandId,
    ) -> result::Result<
        option::Option<commissioned_dispatch::RecordedCommissionedDispatch>,
        commissioned_dispatch::CommissionedDispatchRepositoryError,
    >;
    pub async fn identity_claimed(
        &self,
        command: signalbox_domain::DurableCommandId,
    ) -> result::Result<bool, commissioned_dispatch::CommissionedDispatchRepositoryError>;
    pub async fn commission<SelectDefinition>(
        &self,
        prepared: signalbox_application::PreparedCommissionedDispatch,
        ids: &mut impl signalbox_application::SubmitInputIdGenerator,
        select_definition: SelectDefinition,
    ) -> result::Result<
        commissioned_dispatch::CommissionDispatchOutcome,
        commissioned_dispatch::CommissionedDispatchRepositoryError,
    >
    where
        SelectDefinition: function::Fn(
                signalbox_domain::ModelAlias,
            ) -> option::Option<signalbox_domain::FrozenAliasDefinition>
            + marker::Copy
            + marker::Send;
    pub async fn commission_after_cool_off<SelectDefinition>(
        &self,
        prepared: signalbox_application::PreparedCommissionedDispatch,
        ids: &mut impl signalbox_application::SubmitInputIdGenerator,
        cool_off: time::Duration,
        select_definition: SelectDefinition,
    ) -> result::Result<
        commissioned_dispatch::CommissionDispatchOutcome,
        commissioned_dispatch::CommissionedDispatchRepositoryError,
    >
    where
        SelectDefinition: function::Fn(
                signalbox_domain::ModelAlias,
            ) -> option::Option<signalbox_domain::FrozenAliasDefinition>
            + marker::Copy
            + marker::Send;
}
```

## RecordedCommissionedDispatch

```rust
pub struct RecordedCommissionedDispatch {/* private */}
// derives: fmt::Debug
impl commissioned_dispatch::RecordedCommissionedDispatch {
    #[must_use]
    pub const fn dispatch(&self) -> signalbox_domain::CommissionedDispatchId;
    #[must_use]
    pub const fn session(&self) -> signalbox_domain::SessionId;
    #[must_use]
    pub fn matches(&self, request: &signalbox_application::CommissionDispatchRequest) -> bool;
}
```
