<!-- generated by scripts/render_domain_spine.py, do not edit -->

# program_cancellation

## CancelProgramRun

```rust
pub struct CancelProgramRun {
    pub command_id: signalbox_domain::DurableCommandId,
    pub run_id: signalbox_domain::ProgramRunId,
}
// derives: clone::Clone, fmt::Debug
impl cmp::PartialEq for program_cancellation::CancelProgramRun {
    fn eq(&self, other: &Self) -> bool;
}
impl cmp::Eq for program_cancellation::CancelProgramRun {}
```

## ProgramTerminalState

```rust
pub enum ProgramTerminalState {
    Cancelled,
    Faulted,
    Succeeded(signalbox_domain::InlineFramePayload),
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ProgramCancellationOutcome

```rust
pub enum ProgramCancellationOutcome {
    Applied,
    NotFound,
    AlreadyTerminal(program_cancellation::ProgramTerminalState),
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ProgramCancellationResult

```rust
pub enum ProgramCancellationResult {
    Recorded(program_cancellation::ProgramCancellationOutcome),
    ConflictingReuse,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ProgramCancellationError

```rust
pub enum ProgramCancellationError {
    Database(error::Error),
    CommitAmbiguous(error::Error),
    Journal(program_journal::ProgramJournalRepositoryError),
    Corruption,
}
// derives: fmt::Debug
impl fmt::Display for program_cancellation::ProgramCancellationError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for program_cancellation::ProgramCancellationError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
impl convert::From<error::Error> for program_cancellation::ProgramCancellationError {
    fn from(error: error::Error) -> Self;
}
impl convert::From<program_journal::ProgramJournalRepositoryError>
    for program_cancellation::ProgramCancellationError
{
    fn from(error: program_journal::ProgramJournalRepositoryError) -> Self;
}
```

## cancel

```rust
pub async fn cancel(
    pool: &sqlx_postgres::PgPool,
    command: program_cancellation::CancelProgramRun,
) -> result::Result<
    program_cancellation::ProgramCancellationResult,
    program_cancellation::ProgramCancellationError,
>;
```
