<!-- generated by scripts/render_domain_spine.py, do not edit -->

# reload_configuration

## ReloadConfiguration

```rust
pub struct ReloadConfiguration {
    pub command_id: signalbox_domain::DurableCommandId,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq
impl cmp::PartialEq for reload_configuration::ReloadConfiguration {
    fn eq(&self, other: &Self) -> bool;
}
```

## ReloadIntent

```rust
pub struct ReloadIntent {
    pub replacement_snapshot: string::String,
    pub prior_snapshot: string::String,
    pub rule_set_digest: [u8; 32],
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ReloadPhase

```rust
pub enum ReloadPhase {
    Read,
    Validate,
    Activate,
    Reconcile,
    Install,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ReloadResult

```rust
pub enum ReloadResult {
    Reloaded,
    Failed {
        phase: reload_configuration::ReloadPhase,
        reason: string::String,
    },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ReloadLookup

```rust
pub enum ReloadLookup {
    Unclaimed,
    Pending,
    Recorded(reload_configuration::ReloadResult),
    ConflictingReuse,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ReloadClaim

```rust
pub enum ReloadClaim {
    Retained,
    Settled(reload_configuration::ReloadLookup),
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ReloadRepositoryError

```rust
pub enum ReloadRepositoryError {
    Database(error::Error),
    Corruption(&'static str),
    InvalidCommandId,
}
// derives: fmt::Debug
impl fmt::Display for reload_configuration::ReloadRepositoryError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for reload_configuration::ReloadRepositoryError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
impl convert::From<error::Error> for reload_configuration::ReloadRepositoryError {
    fn from(value: error::Error) -> Self;
}
```

## ReloadConfigurationRepository

```rust
pub struct ReloadConfigurationRepository {/* private */}
// derives: clone::Clone, fmt::Debug
impl reload_configuration::ReloadConfigurationRepository {
    pub fn new(pool: sqlx_postgres::PgPool) -> Self;
    pub async fn lookup(
        &self,
        request: reload_configuration::ReloadConfiguration,
    ) -> result::Result<
        reload_configuration::ReloadLookup,
        reload_configuration::ReloadRepositoryError,
    >;
    pub async fn claim(
        &self,
        request: reload_configuration::ReloadConfiguration,
        intent: result::Result<
            &reload_configuration::ReloadIntent,
            &reload_configuration::ReloadResult,
        >,
    ) -> result::Result<
        reload_configuration::ReloadClaim,
        reload_configuration::ReloadRepositoryError,
    >;
    pub async fn finish(
        &self,
        request: reload_configuration::ReloadConfiguration,
        result: &reload_configuration::ReloadResult,
    ) -> result::Result<(), reload_configuration::ReloadRepositoryError>;
    pub async fn pending(
        &self,
    ) -> result::Result<
        vec::Vec<(
            reload_configuration::ReloadConfiguration,
            reload_configuration::ReloadIntent,
        )>,
        reload_configuration::ReloadRepositoryError,
    >;
}
```
