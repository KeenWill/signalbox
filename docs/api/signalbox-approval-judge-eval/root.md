<!-- generated by scripts/render_domain_spine.py, do not edit -->

# root

## CORPUS_FORMAT_VERSION

```rust
pub const CORPUS_FORMAT_VERSION: u32;
```

## ApprovalJudgeCorpus

```rust
pub struct ApprovalJudgeCorpus {
    pub format_version: u32,
    pub cases: vec::Vec<ApprovalJudgeCase>,
}
// derives: clone::Clone, fmt::Debug, de::Deserialize<'de>, cmp::Eq, cmp::PartialEq, ser::Serialize
impl<T> from_ref::FromRef<T> for ApprovalJudgeCorpus
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for ApprovalJudgeCorpus
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for ApprovalJudgeCorpus {}
impl<T> future_ext::FutureExt for ApprovalJudgeCorpus {}
impl<T> parse_display::IntoResult<T> for ApprovalJudgeCorpus {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for ApprovalJudgeCorpus
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> de::DeserializeOwned for ApprovalJudgeCorpus where T: for<'de> de::Deserialize<'de> {}
impl<T> request::IntoRequest<T> for ApprovalJudgeCorpus {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for ApprovalJudgeCorpus {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for ApprovalJudgeCorpus
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for ApprovalJudgeCorpus where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for ApprovalJudgeCorpus
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for ApprovalJudgeCorpus
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive> for ApprovalJudgeCorpus where
    T: ?marker::Sized
{
}
```

## ApprovalJudgeCase

```rust
pub struct ApprovalJudgeCase {
    pub id: string::String,
    pub request: ApprovalJudgeRequestContext,
    pub expected: ApprovalDisposition,
    pub label_provenance: string::String,
}
// derives: clone::Clone, fmt::Debug, de::Deserialize<'de>, cmp::Eq, cmp::PartialEq, ser::Serialize
impl<T> from_ref::FromRef<T> for ApprovalJudgeCase
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for ApprovalJudgeCase
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for ApprovalJudgeCase {}
impl<T> future_ext::FutureExt for ApprovalJudgeCase {}
impl<T> parse_display::IntoResult<T> for ApprovalJudgeCase {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for ApprovalJudgeCase
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> de::DeserializeOwned for ApprovalJudgeCase where T: for<'de> de::Deserialize<'de> {}
impl<T> request::IntoRequest<T> for ApprovalJudgeCase {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for ApprovalJudgeCase {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for ApprovalJudgeCase
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for ApprovalJudgeCase where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit> for ApprovalJudgeCase
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for ApprovalJudgeCase
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive> for ApprovalJudgeCase where
    T: ?marker::Sized
{
}
```

## ApprovalJudgeRequestContext

```rust
pub struct ApprovalJudgeRequestContext {
    pub tool: string::String,
    pub arguments: string::String,
    pub commissioned_goal: option::Option<string::String>,
    pub session_template: option::Option<string::String>,
    pub frozen_system_prompt: option::Option<string::String>,
}
// derives: clone::Clone, fmt::Debug, de::Deserialize<'de>, cmp::Eq, cmp::PartialEq, ser::Serialize
impl<T> from_ref::FromRef<T> for ApprovalJudgeRequestContext
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for ApprovalJudgeRequestContext
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for ApprovalJudgeRequestContext {}
impl<T> future_ext::FutureExt for ApprovalJudgeRequestContext {}
impl<T> parse_display::IntoResult<T> for ApprovalJudgeRequestContext {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for ApprovalJudgeRequestContext
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> de::DeserializeOwned for ApprovalJudgeRequestContext where T: for<'de> de::Deserialize<'de> {}
impl<T> request::IntoRequest<T> for ApprovalJudgeRequestContext {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for ApprovalJudgeRequestContext {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for ApprovalJudgeRequestContext
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for ApprovalJudgeRequestContext where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for ApprovalJudgeRequestContext
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for ApprovalJudgeRequestContext
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for ApprovalJudgeRequestContext
where
    T: ?marker::Sized,
{
}
```

## ApprovalDisposition

```rust
pub enum ApprovalDisposition {
    Approve,
    Deny,
    EscalateToHuman,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, de::Deserialize<'de>, cmp::Eq, cmp::PartialEq, ser::Serialize
impl<T> from_ref::FromRef<T> for ApprovalDisposition
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for ApprovalDisposition
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for ApprovalDisposition {}
impl<T> future_ext::FutureExt for ApprovalDisposition {}
impl<T> parse_display::IntoResult<T> for ApprovalDisposition {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for ApprovalDisposition
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> de::DeserializeOwned for ApprovalDisposition where T: for<'de> de::Deserialize<'de> {}
impl<T> request::IntoRequest<T> for ApprovalDisposition {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for ApprovalDisposition {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for ApprovalDisposition
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for ApprovalDisposition where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for ApprovalDisposition
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for ApprovalDisposition
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive> for ApprovalDisposition where
    T: ?marker::Sized
{
}
impl ApprovalDisposition {
    #[must_use]
    pub const fn as_str(self) -> &'static str;
}
impl convert::From<tool::DelegateApprovalRecommendation> for ApprovalDisposition {
    fn from(recommendation: tool::DelegateApprovalRecommendation) -> Self;
}
```

## load_corpus

```rust
pub fn load_corpus(
    path: impl convert::AsRef<path::Path>,
) -> result::Result<ApprovalJudgeCorpus, CorpusLoadError>;
```

## request_fingerprint

```rust
#[must_use]
pub fn request_fingerprint(case: &ApprovalJudgeCase) -> string::String;
```

## decode_corpus

```rust
pub fn decode_corpus(bytes: &[u8]) -> result::Result<ApprovalJudgeCorpus, CorpusLoadError>;
```

## CorpusLoadError

```rust
pub enum CorpusLoadError {
    Read {
        path: path::PathBuf,
        source: error::Error,
    },
    Json(error::Error),
    JsonInFile {
        path: path::PathBuf,
        source: error::Error,
    },
    UnsupportedFormatVersion {
        observed: u32,
    },
    EmptyCorpus,
    DuplicateCaseId {
        id: string::String,
    },
    BlankCaseId,
    InvalidCaseId {
        id: string::String,
    },
    NulCaseString {
        id: string::String,
        field: &'static str,
    },
    MissingLabelProvenance {
        id: string::String,
    },
}
// derives: fmt::Debug
impl<T> into_either::IntoEither for CorpusLoadError {}
impl<T> future_ext::FutureExt for CorpusLoadError {}
impl<T> parse_display::IntoResult<T> for CorpusLoadError {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for CorpusLoadError
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for CorpusLoadError {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for CorpusLoadError {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for CorpusLoadError
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for CorpusLoadError where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit> for CorpusLoadError
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for CorpusLoadError
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive> for CorpusLoadError where
    T: ?marker::Sized
{
}
impl fmt::Display for CorpusLoadError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for CorpusLoadError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## score_corpus

```rust
pub async fn score_corpus(
    model: &dyn approval_judge::ApprovalJudgeModel,
    binding: &approval_judge_eval::ApprovalJudgeEvalBinding,
    corpus: &ApprovalJudgeCorpus,
) -> result::Result<ApprovalJudgeScorecard, ScoreError>;
```

## ApprovalJudgeCaseVerdict

```rust
pub struct ApprovalJudgeCaseVerdict {
    pub case_id: string::String,
    pub expected: ApprovalDisposition,
    pub actual: ApprovalDisposition,
    pub correct: bool,
    pub rationale: string::String,
    pub label_provenance: string::String,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
impl<T> from_ref::FromRef<T> for ApprovalJudgeCaseVerdict
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for ApprovalJudgeCaseVerdict
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for ApprovalJudgeCaseVerdict {}
impl<T> future_ext::FutureExt for ApprovalJudgeCaseVerdict {}
impl<T> parse_display::IntoResult<T> for ApprovalJudgeCaseVerdict {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for ApprovalJudgeCaseVerdict
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for ApprovalJudgeCaseVerdict {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for ApprovalJudgeCaseVerdict {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for ApprovalJudgeCaseVerdict
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for ApprovalJudgeCaseVerdict where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for ApprovalJudgeCaseVerdict
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for ApprovalJudgeCaseVerdict
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for ApprovalJudgeCaseVerdict
where
    T: ?marker::Sized,
{
}
```

## ApprovalJudgeScorecard

```rust
pub struct ApprovalJudgeScorecard {
    pub accuracy: MetricRate,
    pub dispositions: vec::Vec<DispositionMetrics>,
    pub verdicts: vec::Vec<ApprovalJudgeCaseVerdict>,
}
// derives: clone::Clone, fmt::Debug, cmp::PartialEq, ser::Serialize
impl<T> from_ref::FromRef<T> for ApprovalJudgeScorecard
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for ApprovalJudgeScorecard
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for ApprovalJudgeScorecard {}
impl<T> future_ext::FutureExt for ApprovalJudgeScorecard {}
impl<T> parse_display::IntoResult<T> for ApprovalJudgeScorecard {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for ApprovalJudgeScorecard
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for ApprovalJudgeScorecard {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for ApprovalJudgeScorecard {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for ApprovalJudgeScorecard
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for ApprovalJudgeScorecard where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for ApprovalJudgeScorecard
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for ApprovalJudgeScorecard
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for ApprovalJudgeScorecard
where
    T: ?marker::Sized,
{
}
```

## MetricRate

```rust
pub struct MetricRate {
    pub numerator: usize,
    pub denominator: usize,
    pub value: option::Option<f64>,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::PartialEq, ser::Serialize
impl<T> from_ref::FromRef<T> for MetricRate
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for MetricRate
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for MetricRate {}
impl<T> future_ext::FutureExt for MetricRate {}
impl<T> parse_display::IntoResult<T> for MetricRate {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for MetricRate
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for MetricRate {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for MetricRate {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for MetricRate
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for MetricRate where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit> for MetricRate
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for MetricRate
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive> for MetricRate where
    T: ?marker::Sized
{
}
```

## DispositionMetrics

```rust
pub struct DispositionMetrics {
    pub disposition: ApprovalDisposition,
    pub true_positives: usize,
    pub false_positives: usize,
    pub false_negatives: usize,
    pub precision: MetricRate,
    pub recall: MetricRate,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::PartialEq, ser::Serialize
impl<T> from_ref::FromRef<T> for DispositionMetrics
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for DispositionMetrics
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for DispositionMetrics {}
impl<T> future_ext::FutureExt for DispositionMetrics {}
impl<T> parse_display::IntoResult<T> for DispositionMetrics {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for DispositionMetrics
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for DispositionMetrics {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for DispositionMetrics {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for DispositionMetrics
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for DispositionMetrics where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for DispositionMetrics
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for DispositionMetrics
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive> for DispositionMetrics where
    T: ?marker::Sized
{
}
```

## ScoreError

```rust
pub enum ScoreError {
    UnsupportedFormatVersion {
        observed: u32,
    },
    EmptyCorpus,
    DuplicateCaseId {
        id: string::String,
    },
    BlankCaseId,
    InvalidCaseId {
        id: string::String,
    },
    NulCaseString {
        id: string::String,
        field: &'static str,
    },
    MissingLabelProvenance {
        id: string::String,
    },
    Case {
        case_id: string::String,
        source: boxed::Box<dyn error::Error + marker::Send + marker::Sync>,
    },
}
// derives: fmt::Debug
impl<T> into_either::IntoEither for ScoreError {}
impl<T> future_ext::FutureExt for ScoreError {}
impl<T> parse_display::IntoResult<T> for ScoreError {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for ScoreError
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for ScoreError {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for ScoreError {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for ScoreError
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for ScoreError where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit> for ScoreError
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for ScoreError
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive> for ScoreError where
    T: ?marker::Sized
{
}
impl ScoreError {
    #[must_use]
    pub fn case_id(&self) -> option::Option<&str>;
}
impl fmt::Display for ScoreError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for ScoreError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```
