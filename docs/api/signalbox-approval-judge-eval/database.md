<!-- generated by scripts/render_domain_spine.py, do not edit -->

# database

## DatabaseCorpusStore

```rust
pub struct DatabaseCorpusStore {/* private */}
// derives: clone::Clone, fmt::Debug
impl<T> from_ref::FromRef<T> for DatabaseCorpusStore
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for DatabaseCorpusStore
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for DatabaseCorpusStore {}
impl<T> future_ext::FutureExt for DatabaseCorpusStore {}
impl<T> parse_display::IntoResult<T> for DatabaseCorpusStore {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for DatabaseCorpusStore
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for DatabaseCorpusStore {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for DatabaseCorpusStore {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for DatabaseCorpusStore
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for DatabaseCorpusStore where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for DatabaseCorpusStore
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for DatabaseCorpusStore
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive> for DatabaseCorpusStore where
    T: ?marker::Sized
{
}
impl DatabaseCorpusStore {
    #[must_use]
    pub const fn new(pool: sqlx_postgres::PgPool) -> Self;
    pub async fn import_manifest(
        &self,
        manifest_path: impl convert::AsRef<path::Path>,
    ) -> result::Result<CorpusRegistration, CorpusStoreError>;
    pub async fn put(
        &self,
        registration: CorpusRegistration,
        corpus: &ApprovalJudgeCorpus,
    ) -> result::Result<CorpusRegistration, CorpusStoreError>;
    pub async fn registration(
        &self,
        key: &CorpusKey,
    ) -> result::Result<CorpusRegistration, CorpusStoreError>;
}
impl CorpusStore for DatabaseCorpusStore {
    fn enumerate(&self) -> CorpusStoreFuture<'_, vec::Vec<CorpusRegistration>>;
    fn load<'a>(&'a self, key: &'a CorpusKey) -> CorpusStoreFuture<'a, ApprovalJudgeCorpus>;
}
```
