<!-- generated by scripts/render_domain_spine.py, do not edit -->

# manifest

## CORPUS_MANIFEST_VERSION

```rust
pub const CORPUS_MANIFEST_VERSION: u32;
```

## CorpusManifest

```rust
pub struct CorpusManifest {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
impl<T> from_ref::FromRef<T> for manifest::CorpusManifest
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for manifest::CorpusManifest
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for manifest::CorpusManifest {}
impl<T> future_ext::FutureExt for manifest::CorpusManifest {}
impl<T> parse_display::IntoResult<T> for manifest::CorpusManifest {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for manifest::CorpusManifest
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> de::DeserializeOwned for manifest::CorpusManifest where T: for<'de> de::Deserialize<'de> {}
impl<T> request::IntoRequest<T> for manifest::CorpusManifest {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for manifest::CorpusManifest {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for manifest::CorpusManifest
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for manifest::CorpusManifest where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for manifest::CorpusManifest
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for manifest::CorpusManifest
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for manifest::CorpusManifest
where
    T: ?marker::Sized,
{
}
impl<'de> de::Deserialize<'de> for manifest::CorpusManifest {
    fn deserialize<DeserializerType>(
        deserializer: DeserializerType,
    ) -> result::Result<Self, <DeserializerType as de::Deserializer>::Error>
    where
        DeserializerType: de::Deserializer<'de>;
}
impl manifest::CorpusManifest {
    #[must_use]
    pub const fn manifest_version(&self) -> u32;
    #[must_use]
    pub fn name(&self) -> &str;
    #[must_use]
    pub fn version(&self) -> &str;
    #[must_use]
    pub const fn corpus_format_version(&self) -> u32;
    #[must_use]
    pub const fn case_source(&self) -> &manifest::ManifestCaseSource;
    #[must_use]
    pub const fn integrity(&self) -> &manifest::CorpusIntegrity;
}
```

## ManifestCaseSource

```rust
pub enum ManifestCaseSource {
    Repository {
        repository: string::String,
        path: string::String,
    },
    DatabaseNative {
        cases: vec::Vec<ApprovalJudgeCase>,
    },
    BlobReference {
        store: option::Option<string::String>,
        digest: Sha256Digest,
        byte_length: u64,
    },
}
// derives: clone::Clone, fmt::Debug, de::Deserialize<'de>, cmp::Eq, cmp::PartialEq, ser::Serialize
impl<T> from_ref::FromRef<T> for manifest::ManifestCaseSource
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for manifest::ManifestCaseSource
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for manifest::ManifestCaseSource {}
impl<T> future_ext::FutureExt for manifest::ManifestCaseSource {}
impl<T> parse_display::IntoResult<T> for manifest::ManifestCaseSource {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for manifest::ManifestCaseSource
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> de::DeserializeOwned for manifest::ManifestCaseSource where T: for<'de> de::Deserialize<'de> {}
impl<T> request::IntoRequest<T> for manifest::ManifestCaseSource {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for manifest::ManifestCaseSource {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for manifest::ManifestCaseSource
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for manifest::ManifestCaseSource where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for manifest::ManifestCaseSource
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for manifest::ManifestCaseSource
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for manifest::ManifestCaseSource
where
    T: ?marker::Sized,
{
}
```

## CorpusIntegrity

```rust
pub struct CorpusIntegrity {
    pub corpus_sha256: Sha256Digest,
    pub source_sha256: option::Option<Sha256Digest>,
    pub cases: vec::Vec<manifest::CaseIntegrity>,
}
// derives: clone::Clone, fmt::Debug, de::Deserialize<'de>, cmp::Eq, cmp::PartialEq, ser::Serialize
impl<T> from_ref::FromRef<T> for manifest::CorpusIntegrity
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for manifest::CorpusIntegrity
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for manifest::CorpusIntegrity {}
impl<T> future_ext::FutureExt for manifest::CorpusIntegrity {}
impl<T> parse_display::IntoResult<T> for manifest::CorpusIntegrity {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for manifest::CorpusIntegrity
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> de::DeserializeOwned for manifest::CorpusIntegrity where T: for<'de> de::Deserialize<'de> {}
impl<T> request::IntoRequest<T> for manifest::CorpusIntegrity {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for manifest::CorpusIntegrity {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for manifest::CorpusIntegrity
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for manifest::CorpusIntegrity where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for manifest::CorpusIntegrity
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for manifest::CorpusIntegrity
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for manifest::CorpusIntegrity
where
    T: ?marker::Sized,
{
}
```

## CaseIntegrity

```rust
pub struct CaseIntegrity {
    pub id: string::String,
    pub sha256: Sha256Digest,
}
// derives: clone::Clone, fmt::Debug, de::Deserialize<'de>, cmp::Eq, cmp::PartialEq, ser::Serialize
impl<T> from_ref::FromRef<T> for manifest::CaseIntegrity
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for manifest::CaseIntegrity
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for manifest::CaseIntegrity {}
impl<T> future_ext::FutureExt for manifest::CaseIntegrity {}
impl<T> parse_display::IntoResult<T> for manifest::CaseIntegrity {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for manifest::CaseIntegrity
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> de::DeserializeOwned for manifest::CaseIntegrity where T: for<'de> de::Deserialize<'de> {}
impl<T> request::IntoRequest<T> for manifest::CaseIntegrity {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for manifest::CaseIntegrity {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for manifest::CaseIntegrity
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for manifest::CaseIntegrity where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for manifest::CaseIntegrity
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for manifest::CaseIntegrity
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for manifest::CaseIntegrity
where
    T: ?marker::Sized,
{
}
```

## LoadedManifestCorpus

```rust
pub struct LoadedManifestCorpus {
    pub manifest: manifest::CorpusManifest,
    pub registration: CorpusRegistration,
    pub corpus: ApprovalJudgeCorpus,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for manifest::LoadedManifestCorpus
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for manifest::LoadedManifestCorpus
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for manifest::LoadedManifestCorpus {}
impl<T> future_ext::FutureExt for manifest::LoadedManifestCorpus {}
impl<T> parse_display::IntoResult<T> for manifest::LoadedManifestCorpus {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for manifest::LoadedManifestCorpus
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for manifest::LoadedManifestCorpus {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for manifest::LoadedManifestCorpus {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for manifest::LoadedManifestCorpus
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for manifest::LoadedManifestCorpus where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for manifest::LoadedManifestCorpus
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for manifest::LoadedManifestCorpus
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for manifest::LoadedManifestCorpus
where
    T: ?marker::Sized,
{
}
```

## decode_manifest

```rust
pub fn decode_manifest(
    bytes: &[u8],
) -> result::Result<manifest::CorpusManifest, manifest::ManifestError>;
```

## load_manifest_corpus

```rust
pub fn load_manifest_corpus(
    path: impl convert::AsRef<path::Path>,
) -> result::Result<manifest::LoadedManifestCorpus, manifest::ManifestError>;
```

## corpus_digest

```rust
pub fn corpus_digest(
    corpus: &ApprovalJudgeCorpus,
) -> result::Result<Sha256Digest, manifest::ManifestError>;
```

## case_integrity

```rust
pub fn case_integrity(
    corpus: &ApprovalJudgeCorpus,
) -> result::Result<vec::Vec<manifest::CaseIntegrity>, manifest::ManifestError>;
```

## digest_bytes

```rust
#[must_use]
pub fn digest_bytes(bytes: &[u8]) -> Sha256Digest;
```

## ManifestError

```rust
pub enum ManifestError {
    Read {
        path: path::PathBuf,
        source: error::Error,
    },
    Json(error::Error),
    UnsupportedManifestVersion {
        observed: u32,
    },
    UnsupportedCorpusVersion {
        observed: u32,
    },
    InvalidIdentity(&'static str),
    NonPortablePath(string::String),
    RepositoryRootUnavailable(path::PathBuf),
    RepositorySourceOutsideCheckout(path::PathBuf),
    MissingSourceDigest,
    UnexpectedSourceDigest,
    InvalidBlobByteLength,
    InvalidBlobStore,
    SourceDigestMismatch {
        expected: Sha256Digest,
        observed: Sha256Digest,
    },
    Corpus(CorpusLoadError),
    CorpusVersionMismatch {
        manifest: u32,
        corpus: u32,
    },
    DuplicateCaseId(string::String),
    CaseIntegrityMismatch,
    MissingCaseIntegrity,
    CorpusDigestMismatch {
        expected: Sha256Digest,
        observed: Sha256Digest,
    },
    BlobBackendUnavailable,
    LengthOverflow,
}
// derives: fmt::Debug
impl<T> into_either::IntoEither for manifest::ManifestError {}
impl<T> future_ext::FutureExt for manifest::ManifestError {}
impl<T> parse_display::IntoResult<T> for manifest::ManifestError {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for manifest::ManifestError
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for manifest::ManifestError {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for manifest::ManifestError {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for manifest::ManifestError
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for manifest::ManifestError where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for manifest::ManifestError
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for manifest::ManifestError
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for manifest::ManifestError
where
    T: ?marker::Sized,
{
}
impl fmt::Display for manifest::ManifestError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for manifest::ManifestError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```
