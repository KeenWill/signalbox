<!-- generated by scripts/render_domain_spine.py, do not edit -->

# store

## CorpusStoreFuture

```rust
pub type CorpusStoreFuture<'a, Value> = pin::Pin<
    boxed::Box<
        dyn future::Future<Output = result::Result<Value, CorpusStoreError>> + marker::Send + 'a,
    >,
>;
```

## CorpusKey

```rust
pub struct CorpusKey {
    pub name: string::String,
    pub version: string::String,
}
// derives: clone::Clone, fmt::Debug, de::Deserialize<'de>, cmp::Eq, cmp::Ord, cmp::PartialEq, cmp::PartialOrd, ser::Serialize
impl<T> from_ref::FromRef<T> for CorpusKey
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for CorpusKey
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for CorpusKey {}
impl<T> future_ext::FutureExt for CorpusKey {}
impl<T> parse_display::IntoResult<T> for CorpusKey {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for CorpusKey
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> de::DeserializeOwned for CorpusKey where T: for<'de> de::Deserialize<'de> {}
impl<T> request::IntoRequest<T> for CorpusKey {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for CorpusKey {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for CorpusKey
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for CorpusKey where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit> for CorpusKey
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for CorpusKey
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive> for CorpusKey where
    T: ?marker::Sized
{
}
```

## Sha256Digest

```rust
pub struct Sha256Digest(/* private */);
// derives: clone::Clone, marker::Copy, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl<T> from_ref::FromRef<T> for Sha256Digest
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for Sha256Digest
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for Sha256Digest {}
impl<T> future_ext::FutureExt for Sha256Digest {}
impl<T> parse_display::IntoResult<T> for Sha256Digest {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for Sha256Digest
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> de::DeserializeOwned for Sha256Digest where T: for<'de> de::Deserialize<'de> {}
impl<T> request::IntoRequest<T> for Sha256Digest {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for Sha256Digest {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for Sha256Digest
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for Sha256Digest where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit> for Sha256Digest
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for Sha256Digest
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive> for Sha256Digest where
    T: ?marker::Sized
{
}
impl Sha256Digest {
    #[must_use]
    pub const fn from_bytes(bytes: [u8; 32]) -> Self;
    #[must_use]
    pub const fn as_bytes(&self) -> &[u8; 32];
    #[must_use]
    pub fn to_hex(self) -> string::String;
}
impl fmt::Debug for Sha256Digest {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl fmt::Display for Sha256Digest {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl traits::FromStr for Sha256Digest {
    type Err = DigestParseError;
    fn from_str(value: &str) -> result::Result<Self, <Self as traits::FromStr>::Err>;
}
impl ser::Serialize for Sha256Digest {
    fn serialize<SerializerType>(
        &self,
        serializer: SerializerType,
    ) -> result::Result<
        <SerializerType as ser::Serializer>::Ok,
        <SerializerType as ser::Serializer>::Error,
    >
    where
        SerializerType: ser::Serializer;
}
impl<'de> de::Deserialize<'de> for Sha256Digest {
    fn deserialize<DeserializerType>(
        deserializer: DeserializerType,
    ) -> result::Result<Self, <DeserializerType as de::Deserializer>::Error>
    where
        DeserializerType: de::Deserializer<'de>;
}
```

## DigestParseError

```rust
pub struct DigestParseError;
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for DigestParseError
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for DigestParseError
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for DigestParseError {}
impl<T> future_ext::FutureExt for DigestParseError {}
impl<T> parse_display::IntoResult<T> for DigestParseError {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for DigestParseError
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for DigestParseError {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for DigestParseError {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for DigestParseError
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for DigestParseError where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit> for DigestParseError
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for DigestParseError
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive> for DigestParseError where
    T: ?marker::Sized
{
}
impl fmt::Display for DigestParseError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for DigestParseError {}
```

## CorpusSourceDescriptor

```rust
pub enum CorpusSourceDescriptor {
    Repository {
        repository: string::String,
        path: string::String,
    },
    DatabaseNative,
    BlobReference {
        store: option::Option<string::String>,
        digest: Sha256Digest,
        byte_length: u64,
    },
}
// derives: clone::Clone, fmt::Debug, de::Deserialize<'de>, cmp::Eq, cmp::PartialEq, ser::Serialize
impl<T> from_ref::FromRef<T> for CorpusSourceDescriptor
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for CorpusSourceDescriptor
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for CorpusSourceDescriptor {}
impl<T> future_ext::FutureExt for CorpusSourceDescriptor {}
impl<T> parse_display::IntoResult<T> for CorpusSourceDescriptor {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for CorpusSourceDescriptor
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> de::DeserializeOwned for CorpusSourceDescriptor where T: for<'de> de::Deserialize<'de> {}
impl<T> request::IntoRequest<T> for CorpusSourceDescriptor {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for CorpusSourceDescriptor {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for CorpusSourceDescriptor
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for CorpusSourceDescriptor where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for CorpusSourceDescriptor
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for CorpusSourceDescriptor
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive>
    for CorpusSourceDescriptor
where
    T: ?marker::Sized,
{
}
```

## CorpusRegistration

```rust
pub struct CorpusRegistration {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
impl<T> from_ref::FromRef<T> for CorpusRegistration
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for CorpusRegistration
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for CorpusRegistration {}
impl<T> future_ext::FutureExt for CorpusRegistration {}
impl<T> parse_display::IntoResult<T> for CorpusRegistration {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for CorpusRegistration
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for CorpusRegistration {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for CorpusRegistration {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for CorpusRegistration
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for CorpusRegistration where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for CorpusRegistration
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for CorpusRegistration
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive> for CorpusRegistration where
    T: ?marker::Sized
{
}
impl CorpusRegistration {
    pub fn new(
        key: CorpusKey,
        source: CorpusSourceDescriptor,
        corpus: &ApprovalJudgeCorpus,
    ) -> result::Result<Self, manifest::ManifestError>;
    #[must_use]
    pub const fn key(&self) -> &CorpusKey;
    #[must_use]
    pub const fn format_version(&self) -> u32;
    #[must_use]
    pub const fn corpus_sha256(&self) -> Sha256Digest;
    #[must_use]
    pub const fn case_count(&self) -> u64;
    #[must_use]
    pub const fn source(&self) -> &CorpusSourceDescriptor;
}
```

## CorpusStore

```rust
pub trait CorpusStore: marker::Send + marker::Sync {
    fn enumerate(&self) -> CorpusStoreFuture<'_, vec::Vec<CorpusRegistration>>;
    fn load<'a>(&'a self, key: &'a CorpusKey) -> CorpusStoreFuture<'a, ApprovalJudgeCorpus>;
}
```

## DiskCorpusStore

```rust
pub struct DiskCorpusStore {/* private */}
// derives: clone::Clone, fmt::Debug
impl<T> from_ref::FromRef<T> for DiskCorpusStore
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for DiskCorpusStore
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for DiskCorpusStore {}
impl<T> future_ext::FutureExt for DiskCorpusStore {}
impl<T> parse_display::IntoResult<T> for DiskCorpusStore {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for DiskCorpusStore
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for DiskCorpusStore {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for DiskCorpusStore {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for DiskCorpusStore
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for DiskCorpusStore where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit> for DiskCorpusStore
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for DiskCorpusStore
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive> for DiskCorpusStore where
    T: ?marker::Sized
{
}
impl DiskCorpusStore {
    pub fn open(
        manifest_path: impl convert::AsRef<path::Path>,
    ) -> result::Result<Self, CorpusStoreError>;
    #[must_use]
    pub fn manifest_path(&self) -> &path::Path;
}
impl CorpusStore for DiskCorpusStore {
    fn enumerate(&self) -> CorpusStoreFuture<'_, vec::Vec<CorpusRegistration>>;
    fn load<'a>(&'a self, key: &'a CorpusKey) -> CorpusStoreFuture<'a, ApprovalJudgeCorpus>;
}
```

## CorpusStoreError

```rust
pub enum CorpusStoreError {
    NotFound(CorpusKey),
    Manifest(manifest::ManifestError),
    Database(error::Error),
    StoredCaseJson(error::Error),
    CorruptRegistration(CorpusStoreCorruption),
    CorruptStoredAdmission(manifest::ManifestError),
    BlobBackendUnavailable,
    RepositorySourceRequiresManifestImport,
    DatabaseNativeRequiresDatabaseImport,
}
// derives: fmt::Debug
impl<T> into_either::IntoEither for CorpusStoreError {}
impl<T> future_ext::FutureExt for CorpusStoreError {}
impl<T> parse_display::IntoResult<T> for CorpusStoreError {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for CorpusStoreError
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for CorpusStoreError {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for CorpusStoreError {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for CorpusStoreError
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for CorpusStoreError where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit> for CorpusStoreError
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for CorpusStoreError
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive> for CorpusStoreError where
    T: ?marker::Sized
{
}
impl fmt::Display for CorpusStoreError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for CorpusStoreError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
impl convert::From<error::Error> for CorpusStoreError {
    fn from(source: error::Error) -> Self;
}
```

## CorpusStoreCorruption

```rust
pub enum CorpusStoreCorruption {
    CaseCountOutOfRange,
    CasePositionOutOfRange,
    FormatVersionOutOfRange,
    NegativeCaseCount,
    NegativeFormatVersion,
    FormatVersionMismatch,
    CaseCountMismatch,
    CorpusDigestMismatch,
    ReplayDigestMismatch,
    CaseIdMismatch,
    RegistrationConflict,
    UnknownSourceKind(string::String),
    MissingSourceField(&'static str),
    InvalidBlobByteLength,
    InvalidDigestLength,
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl<T> from_ref::FromRef<T> for CorpusStoreCorruption
where
    T: clone::Clone,
{
    fn from_ref(input: &T) -> T;
}
impl<T> dyn_clone::DynClone for CorpusStoreCorruption
where
    T: clone::Clone,
{
    fn __clone_box(&self, _: sealed::Private) -> *mut ();
}
impl<T> into_either::IntoEither for CorpusStoreCorruption {}
impl<T> future_ext::FutureExt for CorpusStoreCorruption {}
impl<T> parse_display::IntoResult<T> for CorpusStoreCorruption {
    type Err = never;
    fn into_result(self) -> result::Result<T, <T as parse_display::IntoResult<T>>::Err>;
}
impl<V, T> types::VZip<V> for CorpusStoreCorruption
where
    V: types::MultiLane<T>,
{
    fn vzip(self) -> V;
}
impl<T> request::IntoRequest<T> for CorpusStoreCorruption {
    fn into_request(self) -> request::Request<T>;
}
impl<L> layered::LayerExt<L> for CorpusStoreCorruption {
    fn named_layer<S>(
        &self,
        service: S,
    ) -> layered::Layered<<L as tower_layer::Layer<S>>::Service, S>
    where
        L: tower_layer::Layer<S>;
}
impl<T> policy::PolicyExt for CorpusStoreCorruption
where
    T: ?marker::Sized,
{
    fn and<P, B, E>(self, other: P) -> and::And<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
    fn or<P, B, E>(self, other: P) -> or::Or<T, P>
    where
        T: marker::Sized + policy::Policy<B, E>,
        P: policy::Policy<B, E>;
}
impl<T> erased::ErasedDestructor for CorpusStoreCorruption where T: 'static {}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Uninit, invariant::Uninit>
    for CorpusStoreCorruption
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<ST, DT> invariant::CastableFrom<ST, invariant::Initialized, invariant::Initialized>
    for CorpusStoreCorruption
where
    ST: ?marker::Sized,
    DT: ?marker::Sized,
{
}
impl<T> invariant::Read<invariant::Exclusive, invariant::BecauseExclusive> for CorpusStoreCorruption where
    T: ?marker::Sized
{
}
impl fmt::Display for CorpusStoreCorruption {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
```
