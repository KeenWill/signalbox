<!-- generated by scripts/render_domain_spine.py, do not edit -->

# live

## CaseCategory

```rust
pub enum CaseCategory {
    GitPush,
    ThreadOps,
    NetworkEgress,
    CredentialAccess,
    Destructive,
    WorkspaceBenign,
    InjectionResistance,
    ContextAbsent,
    UndecodableArguments,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, de::Deserialize<'de>, cmp::Eq, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl live::CaseCategory {
    pub fn as_str(self) -> &'static str;
}
```

## ExpectedVerdict

```rust
pub enum ExpectedVerdict {
    Approve,
    Deny,
    EscalateToHuman,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, de::Deserialize<'de>, cmp::Eq, cmp::PartialEq
impl live::ExpectedVerdict {
    pub const fn as_str(self) -> &'static str;
}
```

## CorpusCase

```rust
pub struct CorpusCase {
    pub name: string::String,
    pub category: live::CaseCategory,
    pub tool: string::String,
    pub arguments: string::String,
    pub expected: live::ExpectedVerdict,
    pub goal: option::Option<string::String>,
    pub template: option::Option<string::String>,
    pub system_prompt: option::Option<string::String>,
    pub dispatch: option::Option<live::CorpusDispatchFence>,
    pub notes: option::Option<string::String>,
}
// derives: fmt::Debug, de::Deserialize<'de>
```

## CorpusDispatchFence

```rust
pub struct CorpusDispatchFence {
    pub repository: string::String,
    pub pull_request: u64,
    pub head_sha: string::String,
    pub head_repository: string::String,
    pub head_branch: string::String,
    pub base_branch: string::String,
}
// derives: fmt::Debug, de::Deserialize<'de>
```

## CategoryScore

```rust
pub struct CategoryScore {/* private */}
// derives: default::Default
```

## ScorecardMetadata

```rust
pub struct ScorecardMetadata {
    pub scoring_semantics_version: u32,
    pub judge_selection: string::String,
    pub provider_model: string::String,
    pub corpus_digest: string::String,
    pub contract_digest: string::String,
    pub rendered_digest: string::String,
    pub repeats: usize,
    pub speculative_tools: vec::Vec<string::String>,
}
```

## render_scorecard

```rust
pub fn render_scorecard(
    metadata: live::ScorecardMetadata,
    scores: &map::BTreeMap<live::CaseCategory, live::CategoryScore>,
    case_reports: vec::Vec<value::Value>,
) -> result::Result<string::String, string::String>;
```

## ScoredVerdict

```rust
pub struct ScoredVerdict {
    pub usage: signalbox_domain::ProviderReportedTokenUsage,
    pub recommendation: signalbox_domain::DelegateApprovalRecommendation,
    pub rationale: string::String,
    pub provider_reported_model: option::Option<string::String>,
}
```

## score_case

```rust
pub fn score_case(
    case: &live::CorpusCase,
    repeats: usize,
    verdicts: &[live::ScoredVerdict],
    failure_causes: vec::Vec<string::String>,
    configured_posture: option::Option<&str>,
    scores: &mut map::BTreeMap<live::CaseCategory, live::CategoryScore>,
) -> value::Value;
```
