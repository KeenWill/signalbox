<!-- generated by scripts/render_domain_spine.py, do not edit -->

# root

## Error

```rust
pub enum Error {
    Evidence(string::String),
    Io(error::Error),
    Json(error::Error),
    Toml(error::Error),
    Grammar(error::Error),
}
// derives: fmt::Debug, error::Error, fmt::Display, convert::From<error::Error>
```

## ConvergencePolicy

```rust
pub struct ConvergencePolicy {
    pub repository: string::String,
    pub fixed_in_commit_pattern: string::String,
    pub declined_prefix: string::String,
    pub informational_classes: vec::Vec<string::String>,
    pub acknowledgement_words: vec::Vec<string::String>,
    pub scratchpad_marker_line: string::String,
    pub description_word_limit: option::Option<usize>,
    pub reject_drafts: bool,
    pub reviewers: vec::Vec<ReviewerPolicy>,
    pub non_gating_check_patterns: vec::Vec<string::String>,
    pub exempt_smoke_workflow_names: vec::Vec<string::String>,
    pub thread_limit: usize,
    pub page_limit: usize,
    pub wave_cap: usize,
    pub extended_wave_cap: usize,
}
// derives: clone::Clone, fmt::Debug, de::Deserialize<'de>, ser::Serialize
impl ConvergencePolicy {
    pub fn read(path: &path::Path) -> result::Result<Self, Error>;
    pub fn validate(&self) -> result::Result<(), Error>;
    pub fn is_non_gating(&self, name: &str) -> bool;
}
```

## ReviewerPolicy

```rust
pub struct ReviewerPolicy {
    pub login: string::String,
    pub bot: bool,
    pub request_pattern: string::String,
    pub verdict_marker: string::String,
    pub verdict_pattern: string::String,
    pub escalation_marker: string::String,
    pub trusted_requests: bool,
    pub post_green_requests: bool,
    pub completion_reaction: string::String,
}
// derives: clone::Clone, fmt::Debug, de::Deserialize<'de>, ser::Serialize
```

## Response

```rust
pub struct Response {
    pub query: string::String,
    pub variables: value::Value,
    pub response: value::Value,
}
// derives: clone::Clone, fmt::Debug, de::Deserialize<'de>, ser::Serialize
```

## Recording

```rust
pub struct Recording {
    pub repository: string::String,
    pub number: u64,
    pub observations: vec::Vec<vec::Vec<Response>>,
    pub comparisons: map::BTreeMap<string::String, value::Value>,
    pub blobs: map::BTreeMap<string::String, value::Value>,
    pub previous: value::Value,
    pub observed_at: option::Option<string::String>,
}
// derives: clone::Clone, fmt::Debug, de::Deserialize<'de>, ser::Serialize
impl Recording {
    pub fn snapshot(&self, policy: &ConvergencePolicy) -> result::Result<Snapshot, Error>;
}
impl Recording {
    pub fn read(path: &path::Path) -> result::Result<Self, Error>;
    pub fn write(&self, path: &path::Path) -> result::Result<(), Error>;
}
```

## Snapshot

```rust
pub struct Snapshot {
    pub initial: value::Value,
    pub current: value::Value,
    pub comparisons: map::BTreeMap<string::String, value::Value>,
    pub blobs: map::BTreeMap<string::String, value::Value>,
    pub previous: value::Value,
    pub observed_at: option::Option<string::String>,
}
// derives: clone::Clone, fmt::Debug
```
