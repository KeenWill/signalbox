<!-- generated by scripts/render_domain_spine.py, do not edit -->

# fetch

## GitHubRequest

```rust
pub enum GitHubRequest {
    GraphQl {
        query: string::String,
        variables: value::Value,
    },
    Rest {
        path: string::String,
    },
}
```

## RequestFuture

```rust
pub type RequestFuture<'a> = pin::Pin<
    boxed::Box<
        dyn future::Future<Output = result::Result<value::Value, Error>> + marker::Send + 'a,
    >,
>;
```

## complete_connection

```rust
pub fn complete_connection(connection: &value::Value) -> result::Result<(), Error>;
```

## record_with

```rust
pub async fn record_with(
    send: &mut (dyn function::FnMut(fetch::GitHubRequest) -> fetch::RequestFuture<'_>
              + marker::Send
              + '_),
    previous: value::Value,
    repository: &str,
    number: u64,
    policy: &ConvergencePolicy,
) -> result::Result<Recording, Error>;
```

## record

```rust
pub fn record(
    repository: &str,
    number: u64,
    policy: &ConvergencePolicy,
    previous: value::Value,
) -> result::Result<Recording, Error>;
```
