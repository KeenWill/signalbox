<!-- generated by scripts/render_domain_spine.py, do not edit -->

# root

## LIST_CONVERSATIONS_NAME

```rust
pub const LIST_CONVERSATIONS_NAME: &str;
```

## READ_OWN_CONVERSATION_NAME

```rust
pub const READ_OWN_CONVERSATION_NAME: &str;
```

## READ_CONVERSATION_NAME

```rust
pub const READ_CONVERSATION_NAME: &str;
```

## READ_IMPORTED_CONVERSATION_NAME

```rust
pub const READ_IMPORTED_CONVERSATION_NAME: &str;
```

## CONVERSATION_TOOL_NAMES

```rust
pub const CONVERSATION_TOOL_NAMES: [&str; 4];
```

## MAX_CONVERSATION_LIST_RESULTS

```rust
pub const MAX_CONVERSATION_LIST_RESULTS: usize;
```

## MAX_TRANSCRIPT_ENTRIES

```rust
pub const MAX_TRANSCRIPT_ENTRIES: usize;
```

## MAX_TRANSCRIPT_CONTENT_BYTES

```rust
pub const MAX_TRANSCRIPT_CONTENT_BYTES: usize;
```

## MAX_LIST_TITLE_BYTES

```rust
pub const MAX_LIST_TITLE_BYTES: usize;
```

## ConversationCursor

```rust
pub enum ConversationCursor {
    Native(signalbox_domain::SessionId),
    Imported(signalbox_domain::ImportedConversationId),
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ConversationListRequest

```rust
pub struct ConversationListRequest {/* private */}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl ConversationListRequest {
    pub const fn new(after: option::Option<ConversationCursor>, max_results: usize) -> Self;
    pub const fn after(&self) -> option::Option<ConversationCursor>;
    pub const fn max_results(&self) -> usize;
}
```

## ConversationListItem

```rust
pub enum ConversationListItem {
    Native {
        session: signalbox_domain::SessionId,
        title: option::Option<string::String>,
        archived: bool,
    },
    Imported {
        conversation: signalbox_domain::ImportedConversationId,
        title: option::Option<string::String>,
        entry_count: u64,
    },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl ConversationListItem {
    pub const fn cursor(&self) -> ConversationCursor;
    pub fn title(&self) -> option::Option<&str>;
}
```

## ConversationListPage

```rust
pub struct ConversationListPage {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl ConversationListPage {
    pub fn new(items: vec::Vec<ConversationListItem>, has_more: bool) -> Self;
    pub fn items(&self) -> &[ConversationListItem];
    pub const fn has_more(&self) -> bool;
    pub fn into_parts(self) -> (vec::Vec<ConversationListItem>, bool);
}
```

## ConversationTranscriptRequest

```rust
pub struct ConversationTranscriptRequest {/* private */}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl ConversationTranscriptRequest {
    pub const fn new(
        requesting_session: signalbox_domain::SessionId,
        target_session: signalbox_domain::SessionId,
        after_position: option::Option<nonzero::NonZeroU64>,
        max_entries: usize,
        max_bytes: usize,
    ) -> Self;
    pub const fn requesting_session(&self) -> signalbox_domain::SessionId;
    pub const fn target_session(&self) -> signalbox_domain::SessionId;
    pub const fn after_position(&self) -> option::Option<nonzero::NonZeroU64>;
    pub const fn max_entries(&self) -> usize;
    pub const fn max_bytes(&self) -> usize;
}
```

## ConversationTranscriptRead

```rust
pub enum ConversationTranscriptRead {
    Read(TranscriptPage),
    NotFound,
    Refused(signalbox_domain::SessionReadScopeRefusal),
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## ImportedTranscriptRequest

```rust
pub struct ImportedTranscriptRequest {/* private */}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl ImportedTranscriptRequest {
    pub const fn new(
        conversation: signalbox_domain::ImportedConversationId,
        after_position: option::Option<nonzero::NonZeroU64>,
        max_entries: usize,
        max_bytes: usize,
    ) -> Self;
    pub const fn conversation(&self) -> signalbox_domain::ImportedConversationId;
    pub const fn after_position(&self) -> option::Option<nonzero::NonZeroU64>;
    pub const fn max_entries(&self) -> usize;
    pub const fn max_bytes(&self) -> usize;
}
```

## TranscriptEntryKind

```rust
pub enum TranscriptEntryKind {
    User,
    Assistant,
    ToolUse,
    ToolResult,
    System,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
```

## TranscriptEntry

```rust
pub struct TranscriptEntry {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl TranscriptEntry {
    pub fn new(
        position: nonzero::NonZeroU64,
        kind: TranscriptEntryKind,
        content: string::String,
        content_truncated: bool,
    ) -> Self;
    pub const fn position(&self) -> nonzero::NonZeroU64;
    pub const fn kind(&self) -> TranscriptEntryKind;
    pub fn content(&self) -> &str;
    pub const fn content_truncated(&self) -> bool;
}
```

## TranscriptPage

```rust
pub struct TranscriptPage {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl TranscriptPage {
    pub fn new(entries: vec::Vec<TranscriptEntry>, has_more: bool) -> Self;
    pub fn entries(&self) -> &[TranscriptEntry];
    pub const fn has_more(&self) -> bool;
    pub fn into_parts(self) -> (vec::Vec<TranscriptEntry>, bool);
}
```

## ConversationIntrospectionPort

```rust
pub trait ConversationIntrospectionPort: marker::Send {
    type Error: signalbox_application::ClassifyOperatorFailure;
    fn list_conversations(
        &mut self,
        request: ConversationListRequest,
    ) -> impl future::Future<
        Output = result::Result<
            ConversationListPage,
            <Self as ConversationIntrospectionPort>::Error,
        >,
    > + marker::Send;
    fn read_conversation(
        &mut self,
        request: ConversationTranscriptRequest,
    ) -> impl future::Future<
        Output = result::Result<
            ConversationTranscriptRead,
            <Self as ConversationIntrospectionPort>::Error,
        >,
    > + marker::Send;
    fn read_imported_conversation(
        &mut self,
        request: ImportedTranscriptRequest,
    ) -> impl future::Future<
        Output = result::Result<
            option::Option<TranscriptPage>,
            <Self as ConversationIntrospectionPort>::Error,
        >,
    > + marker::Send;
}
```

## ListConversationsArguments

```rust
pub struct ListConversationsArguments {
    pub after: option::Option<ConversationCursorArguments>,
    pub max_results: usize,
}
// derives: fmt::Debug, de::Deserialize<'de>, schemars::JsonSchema
```

## ConversationCursorArguments

```rust
pub struct ConversationCursorArguments {
    pub kind: ConversationCursorKind,
    pub id: string::String,
}
// derives: fmt::Debug, de::Deserialize<'de>, schemars::JsonSchema
```

## ConversationCursorKind

```rust
pub enum ConversationCursorKind {
    Native,
    Imported,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, de::Deserialize<'de>, schemars::JsonSchema
```

## ReadOwnConversationArguments

```rust
pub struct ReadOwnConversationArguments {
    pub after_position: option::Option<u64>,
    pub max_entries: usize,
    pub max_bytes: usize,
}
// derives: fmt::Debug, de::Deserialize<'de>, schemars::JsonSchema
```

## ReadConversationArguments

```rust
pub struct ReadConversationArguments {
    pub session_id: string::String,
    pub after_position: option::Option<u64>,
    pub max_entries: usize,
    pub max_bytes: usize,
}
// derives: fmt::Debug, de::Deserialize<'de>, schemars::JsonSchema
```

## ReadImportedConversationArguments

```rust
pub struct ReadImportedConversationArguments {
    pub imported_conversation_id: string::String,
    pub after_position: option::Option<u64>,
    pub max_entries: usize,
    pub max_bytes: usize,
}
// derives: fmt::Debug, de::Deserialize<'de>, schemars::JsonSchema
```

## ConversationToolConstructionError

```rust
pub enum ConversationToolConstructionError {
    Name,
    Schema,
    ErrorDetail,
    Duplicate,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for ConversationToolConstructionError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for ConversationToolConstructionError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## ConversationTools

```rust
pub struct ConversationTools<Port> {/* private */}
// derives: clone::Clone, fmt::Debug
impl<Port> ConversationTools<Port> {
    pub fn try_new(port: Port) -> result::Result<Self, ConversationToolConstructionError>;
    pub fn into_parts(
        self,
    ) -> (
        signalbox_application::CompiledToolCatalog,
        ConversationExecutor<Port>,
    );
}
```

## ConversationExecutor

```rust
pub struct ConversationExecutor<Port> {/* private */}
// derives: clone::Clone, fmt::Debug
impl<Port> ConversationExecutor<Port> {
    pub fn into_port(self) -> Port;
}
impl<Port> signalbox_application::ToolExecutor for ConversationExecutor<Port>
where
    Port: ConversationIntrospectionPort,
{
    type Error = ConversationExecutorError<<Port as ConversationIntrospectionPort>::Error>;
    async fn execute(
        &mut self,
        invocation: signalbox_application::ToolExecutionInvocation,
    ) -> result::Result<
        signalbox_application::CorrelatedToolExecutorEvidence,
        <Self as signalbox_application::ToolExecutor>::Error,
    >;
}
```

## ConversationExecutorError

```rust
pub enum ConversationExecutorError<PortError> {
    ArgumentValidationDrift,
    Port(PortError),
    PortContract,
    ResultEncoding,
}
// derives: fmt::Debug
impl<PortError> fmt::Display for ConversationExecutorError<PortError>
where
    PortError: fmt::Display,
{
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl<PortError> error::Error for ConversationExecutorError<PortError>
where
    PortError: error::Error + 'static,
{
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
impl<PortError> signalbox_application::ClassifyOperatorFailure
    for ConversationExecutorError<PortError>
where
    PortError: signalbox_application::ClassifyOperatorFailure,
{
    fn operator_failure_class(&self) -> signalbox_application::OperatorFailureClass;
}
```
