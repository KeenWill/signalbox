<!-- generated by scripts/render_domain_spine.py, do not edit -->

# oauth

## OauthCredentialMaterial

```rust
pub struct OauthCredentialMaterial {
    pub access_token: signalbox_model_runtime::CredentialValue,
    pub identity_token: signalbox_model_runtime::CredentialValue,
    pub account_id: option::Option<string::String>,
}
// derives: fmt::Debug
```

## OauthCredentialInstaller

```rust
pub trait OauthCredentialInstaller: marker::Send {
    fn install(
        &mut self,
        material: OauthCredentialMaterial,
    ) -> result::Result<(), signalbox_model_runtime::CredentialAccessFailure>;
}
```

## OauthDeliveryOutcome

```rust
pub enum OauthDeliveryOutcome {
    Delivered,
    Cancelled,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## OauthDeliveryFuture

```rust
pub type OauthDeliveryFuture<'a> = pin::Pin<
    boxed::Box<
        dyn future::Future<
                Output = result::Result<
                    OauthDeliveryOutcome,
                    signalbox_model_runtime::CredentialAccessFailure,
                >,
            > + marker::Send
            + 'a,
    >,
>;
```

## OauthCredentialProvider

```rust
pub trait OauthCredentialProvider: marker::Send + marker::Sync + fmt::Debug {
    fn deliver<'a>(
        &'a self,
        reference: &'a signalbox_model_runtime::CredentialReference,
        installer: &'a mut dyn OauthCredentialInstaller,
        cancellation: signalbox_model_runtime::CancellationSignal,
    ) -> OauthDeliveryFuture<'a>;
}
```

## OauthCredentialRoot

```rust
pub struct OauthCredentialRoot {/* private */}
impl fmt::Debug for OauthCredentialRoot {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl OauthCredentialRoot {
    pub fn open(path: &path::Path) -> error::Result<sync::Arc<Self>>;
}
```
