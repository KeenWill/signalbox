<!-- generated by scripts/render_domain_spine.py, do not edit -->

# root

## PLAN_WRITE_NAME

```rust
pub const PLAN_WRITE_NAME: &str;
```

## PLAN_READ_NAME

```rust
pub const PLAN_READ_NAME: &str;
```

## PLAN_TOOL_NAMES

```rust
pub const PLAN_TOOL_NAMES: [&str; 2];
```

## MAX_PLAN_TEXT_CHARS

```rust
pub const MAX_PLAN_TEXT_CHARS: usize;
```

## MAX_PLAN_READ_ENTRIES

```rust
pub const MAX_PLAN_READ_ENTRIES: usize;
```

## MAX_PLAN_DEPENDENCIES_PER_ENTRY

```rust
pub const MAX_PLAN_DEPENDENCIES_PER_ENTRY: usize;
```

## MIN_PLAN_HISTORY_EVENTS

```rust
pub const MIN_PLAN_HISTORY_EVENTS: usize;
```

## MAX_PLAN_HISTORY_EVENTS

```rust
pub const MAX_PLAN_HISTORY_EVENTS: usize;
```

## PlanEventOrdinal

```rust
pub struct PlanEventOrdinal(/* private */);
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl PlanEventOrdinal {
    pub const MIN: Self;
    pub const fn new(value: nonzero::NonZeroU64) -> Self;
    pub const fn try_from_u64(value: u64) -> option::Option<Self>;
    pub const fn get(self) -> u64;
    pub const fn checked_next(self) -> option::Option<Self>;
}
```

## PlanEntryId

```rust
pub struct PlanEntryId(/* private */);
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::Ord, cmp::PartialEq, cmp::PartialOrd
impl PlanEntryId {
    pub const fn creation_ordinal(self) -> PlanEventOrdinal;
}
impl PlanEntryId {
    pub const fn from_creation_ordinal(ordinal: PlanEventOrdinal) -> Self;
    pub const fn try_from_u64(value: u64) -> option::Option<Self>;
    pub const fn as_u64(self) -> u64;
}
```

## PlanStatus

```rust
pub enum PlanStatus {
    Pending,
    InProgress,
    Completed,
    Abandoned,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq, de::Deserialize<'de>, ser::Serialize, schemars::JsonSchema
```

## PlanText

```rust
pub struct PlanText(/* private */);
// derives: clone::Clone, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
impl PlanText {
    pub fn as_str(&self) -> &str;
}
impl PlanText {
    pub fn try_new(value: string::String) -> result::Result<Self, PlanTextError>;
    pub fn into_string(self) -> string::String;
}
```

## PlanTextError

```rust
pub enum PlanTextError {
    Empty,
    ContainsNull,
    TooLong { characters: usize },
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for PlanTextError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for PlanTextError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## PlanEventDraft

```rust
pub enum PlanEventDraft {
    Create {
        text: PlanText,
    },
    Revise {
        entry: PlanEntryId,
        text: PlanText,
    },
    SetStatus {
        entry: PlanEntryId,
        status: PlanStatus,
    },
    DependsOn {
        entry: PlanEntryId,
        dependency: PlanEntryId,
    },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## PlanEventProvenance

```rust
pub struct PlanEventProvenance(/* private */);
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, hash::Hash, cmp::PartialEq
impl PlanEventProvenance {
    pub const fn correlation(self) -> signalbox_domain::ToolAttemptDispatchCorrelation;
}
impl PlanEventProvenance {
    pub const fn from_invocation(
        correlation: signalbox_domain::ToolAttemptDispatchCorrelation,
    ) -> Self;
    pub const fn session(self) -> signalbox_domain::SessionId;
}
```

## PlanEventKind

```rust
pub enum PlanEventKind {
    Created {
        text: PlanText,
    },
    TextRevised {
        entry: PlanEntryId,
        text: PlanText,
    },
    StatusChanged {
        entry: PlanEntryId,
        status: PlanStatus,
    },
    DependsOn {
        entry: PlanEntryId,
        dependency: PlanEntryId,
    },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## PlanDependencyCycle

```rust
pub struct PlanDependencyCycle {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl PlanDependencyCycle {
    pub fn path(&self) -> &[PlanEntryId];
}
impl PlanDependencyCycle {
    pub fn try_new(
        entry: PlanEntryId,
        dependency: PlanEntryId,
        path: vec::Vec<PlanEntryId>,
    ) -> option::Option<Self>;
    pub const fn entry(&self) -> PlanEntryId;
    pub const fn dependency(&self) -> PlanEntryId;
}
```

## PlanEvent

```rust
pub struct PlanEvent {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl PlanEvent {
    pub const fn kind(&self) -> &PlanEventKind;
}
impl PlanEvent {
    pub const fn new(
        ordinal: PlanEventOrdinal,
        provenance: PlanEventProvenance,
        kind: PlanEventKind,
    ) -> Self;
    pub const fn ordinal(&self) -> PlanEventOrdinal;
    pub const fn provenance(&self) -> PlanEventProvenance;
}
```

## PlanEntry

```rust
pub struct PlanEntry {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl PlanEntry {
    pub const fn text(&self) -> &PlanText;
    pub fn dependencies(&self) -> &[PlanEntryId];
}
impl PlanEntry {
    pub const fn new(id: PlanEntryId, text: PlanText, status: PlanStatus) -> Self;
    pub fn with_dependencies(
        id: PlanEntryId,
        text: PlanText,
        status: PlanStatus,
        dependencies: vec::Vec<PlanEntryId>,
        readiness: PlanReadiness,
    ) -> Self;
    pub const fn id(&self) -> PlanEntryId;
    pub const fn status(&self) -> PlanStatus;
    pub const fn readiness(&self) -> PlanReadiness;
}
```

## PlanReadiness

```rust
pub enum PlanReadiness {
    Ready,
    Waiting,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, ser::Serialize
```

## FoldedPlan

```rust
pub struct FoldedPlan {/* private */}
// derives: clone::Clone, fmt::Debug, default::Default, cmp::Eq, cmp::PartialEq
impl FoldedPlan {
    pub fn entries(&self) -> &[PlanEntry];
}
impl FoldedPlan {
    pub fn into_entries(self) -> vec::Vec<PlanEntry>;
}
```

## PlanFoldError

```rust
pub enum PlanFoldError {
    NoncontiguousOrdinal { expected: PlanEventOrdinal },
    MixedSessions,
    UnknownEntry { entry: PlanEntryId },
    DependencyCycle(PlanDependencyCycle),
    DependencyLimitExceeded { entry: PlanEntryId },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for PlanFoldError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for PlanFoldError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## fold_plan_events

```rust
pub fn fold_plan_events(events: &[PlanEvent]) -> result::Result<FoldedPlan, PlanFoldError>;
```

## PlanAppendRequest

```rust
pub struct PlanAppendRequest {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl PlanAppendRequest {
    pub const fn draft(&self) -> &PlanEventDraft;
}
impl PlanAppendRequest {
    pub const fn new(provenance: PlanEventProvenance, draft: PlanEventDraft) -> Self;
    pub const fn session(&self) -> signalbox_domain::SessionId;
    pub const fn provenance(&self) -> PlanEventProvenance;
}
```

## PlanAppendRejection

```rust
pub enum PlanAppendRejection {
    UnknownEntry { entry: PlanEntryId },
    DependencyCycle(PlanDependencyCycle),
    DependencyLimitReached { entry: PlanEntryId },
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## PlanAppendOutcome

```rust
pub enum PlanAppendOutcome {
    Appended(PlanEvent),
    Rejected(PlanAppendRejection),
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
```

## PlanReadRequest

```rust
pub struct PlanReadRequest {/* private */}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl PlanReadRequest {
    pub const fn new(
        session: signalbox_domain::SessionId,
        after_entry: option::Option<PlanEntryId>,
        history_limit: option::Option<usize>,
    ) -> Self;
    pub const fn session(&self) -> signalbox_domain::SessionId;
    pub const fn after_entry(&self) -> option::Option<PlanEntryId>;
    pub const fn max_entries(&self) -> usize;
    pub const fn history_limit(&self) -> option::Option<usize>;
}
```

## PlanPageCompleteness

```rust
pub enum PlanPageCompleteness {
    Complete,
    Truncated,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl PlanPageCompleteness {
    pub const fn is_truncated(self) -> bool;
}
```

## PlanHistoryPage

```rust
pub struct PlanHistoryPage {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl PlanHistoryPage {
    pub fn events(&self) -> &[PlanEvent];
}
impl PlanHistoryPage {
    pub fn new(events: vec::Vec<PlanEvent>, completeness: PlanPageCompleteness) -> Self;
    pub const fn completeness(&self) -> PlanPageCompleteness;
}
```

## PlanReadPage

```rust
pub struct PlanReadPage {/* private */}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq
impl PlanReadPage {
    pub fn entries(&self) -> &[PlanEntry];
}
impl PlanReadPage {
    pub fn new(
        session: signalbox_domain::SessionId,
        entries: vec::Vec<PlanEntry>,
        completeness: PlanPageCompleteness,
        history: option::Option<PlanHistoryPage>,
    ) -> Self;
    pub const fn session(&self) -> signalbox_domain::SessionId;
    pub const fn completeness(&self) -> PlanPageCompleteness;
    pub const fn history(&self) -> option::Option<&PlanHistoryPage>;
}
```

## SessionPlanPort

```rust
pub trait SessionPlanPort: marker::Send {
    type Error: signalbox_application::ClassifyOperatorFailure + error::Error + 'static;
    fn append_plan_event(
        &mut self,
        request: PlanAppendRequest,
    ) -> impl future::Future<
        Output = result::Result<PlanAppendOutcome, <Self as SessionPlanPort>::Error>,
    > + marker::Send;
    fn read_plan(
        &mut self,
        request: PlanReadRequest,
    ) -> impl future::Future<Output = result::Result<PlanReadPage, <Self as SessionPlanPort>::Error>>
           + marker::Send;
}
```

## PlanWriteArguments

```rust
pub enum PlanWriteArguments {
    Create { text: string::String },
    Revise { entry_id: u64, text: string::String },
    SetStatus { entry_id: u64, status: PlanStatus },
    DependsOn { entry_id: u64, dependency_id: u64 },
}
// derives: fmt::Debug, de::Deserialize<'de>, schemars::JsonSchema
```

## PlanReadArguments

```rust
pub struct PlanReadArguments {
    pub after_entry_id: option::Option<u64>,
    pub include_history: bool,
}
// derives: fmt::Debug, default::Default, de::Deserialize<'de>, schemars::JsonSchema
```

## PlanToolConstructionError

```rust
pub enum PlanToolConstructionError {
    Name,
    Schema,
    ErrorDetail,
    Duplicate,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq
impl fmt::Display for PlanToolConstructionError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for PlanToolConstructionError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## PlanTools

```rust
pub struct PlanTools<Port> {/* private */}
// derives: clone::Clone, fmt::Debug
impl<Port> PlanTools<Port> {
    pub fn try_new(port: Port) -> result::Result<Self, PlanToolConstructionError>;
    pub fn into_parts(
        self,
    ) -> (
        signalbox_application::CompiledToolCatalog,
        PlanExecutor<Port>,
    );
}
```

## PlanExecutor

```rust
pub struct PlanExecutor<Port> {/* private */}
// derives: clone::Clone, fmt::Debug
impl<Port> PlanExecutor<Port> {
    pub fn into_port(self) -> Port;
}
impl<Port: SessionPlanPort> signalbox_application::ToolExecutor for PlanExecutor<Port> {
    type Error = PlanExecutorError<<Port as SessionPlanPort>::Error>;
    async fn execute(
        &mut self,
        invocation: signalbox_application::ToolExecutionInvocation,
    ) -> result::Result<
        signalbox_application::CorrelatedToolExecutorEvidence,
        <Self as signalbox_application::ToolExecutor>::Error,
    >;
}
```

## PlanExecutorError

```rust
pub enum PlanExecutorError<PortError> {
    ArgumentValidationDrift,
    Port(PortError),
    PortContract,
    ResultEncoding,
}
// derives: fmt::Debug
impl<PortError> fmt::Display for PlanExecutorError<PortError>
where
    PortError: fmt::Display,
{
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl<PortError> error::Error for PlanExecutorError<PortError>
where
    PortError: error::Error + 'static,
{
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
impl<PortError: signalbox_application::ClassifyOperatorFailure>
    signalbox_application::ClassifyOperatorFailure for PlanExecutorError<PortError>
{
    fn operator_failure_class(&self) -> signalbox_application::OperatorFailureClass;
}
```
