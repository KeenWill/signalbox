<!-- generated by scripts/render_domain_spine.py, do not edit -->

# root

## WORKFLOW_TOOL_NAMES

```rust
pub const WORKFLOW_TOOL_NAMES: [&str; 6];
```

## Operation

```rust
pub enum Operation {
    List,
    Read,
    Start,
    Stop,
    Replay,
    Register,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, cmp::Ord, cmp::PartialOrd, de::Deserialize<'de>, ser::Serialize
impl Operation {
    pub const fn name(self) -> &'static str;
    pub fn from_name(name: &str) -> option::Option<Self>;
    pub const fn default_posture(self) -> signalbox_domain::ToolApprovalPosture;
    pub const fn targets_registration(self) -> bool;
}
```

## RegistrationNames

```rust
pub enum RegistrationNames {
    All(string::String),
    Names(vec::Vec<string::String>),
}
// derives: clone::Clone, fmt::Debug, cmp::Eq, cmp::PartialEq, de::Deserialize<'de>, ser::Serialize
```

## OperationPolicy

```rust
pub struct OperationPolicy {
    pub enabled: option::Option<bool>,
    pub names: option::Option<RegistrationNames>,
    pub posture: option::Option<ApprovalPosture>,
}
// derives: clone::Clone, fmt::Debug, default::Default, cmp::Eq, cmp::PartialEq, de::Deserialize<'de>, ser::Serialize
```

## ApprovalPosture

```rust
pub enum ApprovalPosture {
    Auto,
    Delegated,
    Human,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, cmp::Eq, cmp::PartialEq, de::Deserialize<'de>, ser::Serialize
impl convert::From<ApprovalPosture> for signalbox_domain::ToolApprovalPosture {
    fn from(value: ApprovalPosture) -> Self;
}
```

## WorkflowPolicy

```rust
pub struct WorkflowPolicy(pub map::BTreeMap<Operation, OperationPolicy>);
// derives: clone::Clone, fmt::Debug, default::Default, cmp::Eq, cmp::PartialEq, de::Deserialize<'de>, ser::Serialize
impl WorkflowPolicy {
    pub fn valid(&self) -> bool;
    pub fn posture(&self, operation: Operation) -> signalbox_domain::ToolApprovalPosture;
    pub fn permits(&self, operation: Operation, name: option::Option<&str>) -> bool;
}
```

## ListArguments

```rust
pub struct ListArguments {
    pub after: option::Option<string::String>,
}
// derives: clone::Clone, fmt::Debug, de::Deserialize<'de>, schemars::JsonSchema
```

## RunArguments

```rust
pub struct RunArguments {
    pub run_id: string::String,
}
// derives: clone::Clone, fmt::Debug, de::Deserialize<'de>, schemars::JsonSchema
```

## StartArguments

```rust
pub struct StartArguments {
    pub name: string::String,
    pub revision: string::String,
    pub input: vec::Vec<u8>,
}
// derives: clone::Clone, fmt::Debug, de::Deserialize<'de>, schemars::JsonSchema
```

## RegisterArguments

```rust
pub struct RegisterArguments {
    pub name: string::String,
    pub revision: string::String,
    pub source_path: string::String,
    pub artifact_path: string::String,
    pub grants: vec::Vec<ProgramGrant>,
}
// derives: clone::Clone, fmt::Debug, de::Deserialize<'de>, schemars::JsonSchema
```

## ProgramGrant

```rust
pub enum ProgramGrant {
    Time,
    Random,
    Sleep,
    Subscribe,
    Session,
    Judge,
    ExecStage,
    Corpus,
    EvalRecord,
    Blob,
    Register,
    RepoWatch,
}
// derives: clone::Clone, marker::Copy, fmt::Debug, de::Deserialize<'de>, ser::Serialize, schemars::JsonSchema
```

## WorkflowRequest

```rust
pub enum WorkflowRequest {
    List { after: option::Option<uuid::Uuid> },
    Read { run: uuid::Uuid },
    Start(StartArguments),
    Stop { run: uuid::Uuid },
    Replay { run: uuid::Uuid },
    Register(RegisterArguments),
}
// derives: clone::Clone, fmt::Debug
impl WorkflowRequest {
    pub const fn operation(&self) -> Operation;
}
```

## ConstructionError

```rust
pub struct ConstructionError;
// derives: fmt::Debug
impl fmt::Display for ConstructionError {
    fn fmt(&self, __signalbox_formatter: &mut fmt::Formatter<'_>) -> fmt::Result;
}
impl error::Error for ConstructionError {
    fn source(&self) -> option::Option<&(dyn error::Error + 'static)>;
}
```

## catalog

```rust
pub fn catalog() -> result::Result<signalbox_application::CompiledToolCatalog, ConstructionError>;
```

## WorkflowPort

```rust
pub trait WorkflowPort: marker::Send {
    type Error: signalbox_application::ClassifyOperatorFailure;
    fn execute(
        &mut self,
        caller: &signalbox_domain::ToolRequest,
        request: WorkflowRequest,
    ) -> impl future::Future<
        Output = result::Result<
            signalbox_application::ToolExecutorEvidence,
            <Self as WorkflowPort>::Error,
        >,
    > + marker::Send;
}
```

## WorkflowExecutor

```rust
pub struct WorkflowExecutor<Port>(pub Port);
// derives: clone::Clone, fmt::Debug
impl<Port: WorkflowPort> signalbox_application::ToolExecutor for WorkflowExecutor<Port> {
    type Error = <Port as WorkflowPort>::Error;
    async fn execute(
        &mut self,
        invocation: signalbox_application::ToolExecutionInvocation,
    ) -> result::Result<
        signalbox_application::CorrelatedToolExecutorEvidence,
        <Self as signalbox_application::ToolExecutor>::Error,
    >;
}
```
