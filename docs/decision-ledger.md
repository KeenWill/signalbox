# Decision ledger

This is the authoritative inventory of unresolved foundational questions. A “leaning” guides exploration but is not a decision. Closing a row requires an accepted ADR or an explicit later-scope decision. A linked proposed ADR leaves its rows open until repository-owner acceptance. Scenario identifiers refer to [scenarios.md](scenarios.md).

Unless a row says otherwise, **owner/reviewer status** is “repository owner decision pending; specialist reviewer unassigned.” This status applies individually to every row below.

## Proposed foundation set under review

The records remain independent review units for finding and correction, but their normative definitions are coupled. In their current form the five-record foundation set requires atomic owner acceptance; accepting one separately first requires removing or making conditional every dependency on a still-proposed record.

| Record | Questions grouped for independent review | Status |
| --- | --- | --- |
| [ADR-0001](decisions/0001-domain-terminology-and-identity.md) | Core names, durable identity boundaries, and accepted input versus transcript/turn identity | Proposed; all linked rows remain open |
| [ADR-0003](decisions/0003-session-creation-and-transcript-ancestry.md) | Immutable creation cause and ancestry, owner-initiated-only baseline, typed cause extension, and separation from versioned session defaults | Proposed; all linked rows remain open |
| [ADR-0004](decisions/0004-turn-and-attempt-lifecycle.md) | Logical/physical lifecycle, aggregate current-attempt ownership, progressing-slot states, state-specific cancellation, startup recovery scan, terminal guards, regeneration boundary, and exact-set ambiguity decisions | Proposed; all linked rows remain open; regeneration command/context and delegated waits remain later ADR scope |
| [ADR-0005](decisions/0005-model-call-retry-semantics.md) | Target-before-call identity, reported-target mismatch failure, no automatic known-failure retry, ambiguous-call recovery, continuation calls, changed configuration identity, and refusal disposition | Proposed; all linked rows remain open |
| [ADR-0027](decisions/0027-input-delivery-lifecycle.md) | Input delivery, versioned session defaults, constructible steering/configuration provenance, command deduplication, durable queue ordering, eligibility-fixed starting lineage, steering disposition, and context frontiers | Proposed; all linked rows remain open |

## Terminology

| Question | Why it matters | Plausible choices | Current leaning | Timing | Scenarios affected | Proposed ADR | Owner/reviewer status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Final name for a durable conversation? | It anchors APIs, storage, and UI without implying a login connection. | Session; conversation; thread | Proposed: session, with an explicit durable-conversation boundary | Blocking before public protocol | All | [ADR-0001](decisions/0001-domain-terminology-and-identity.md) | Owner decision pending; domain reviewer unassigned |
| What durable identity does accepted user input require? | Steering can be durable without creating a turn, and command retries must not duplicate semantic input. | Transcript message identity; turn identity; distinct accepted-input identity | Proposed: distinct accepted-input identity with an explicit use/disposition | Blocking before input domain types | S01, S03, S07–S09, S24 | [ADR-0001](decisions/0001-domain-terminology-and-identity.md), [ADR-0027](decisions/0027-input-delivery-lifecycle.md) | Owner decision pending; domain/lifecycle reviewers unassigned |
| Final name and boundary for logical accepted work? | Retry, regeneration, and transcript semantics depend on this identity. | Turn; operation; work item | Proposed: one turn is one typed-origin request for a conversational outcome under frozen effective configuration; typed commands, never semantic intent comparison, determine identity | Blocking before domain schema | S03–S09, S18–S23, S26 | [ADR-0001](decisions/0001-domain-terminology-and-identity.md), [ADR-0004](decisions/0004-turn-and-attempt-lifecycle.md) | Owner decision pending; domain reviewer unassigned |
| Final name for a physical orchestration effort? | It must be distinct from runners and provider/tool calls. | Turn attempt; orchestration attempt; execution | Proposed: turn attempt, bounded physical orchestration tenure | Blocking before domain schema | S03–S08, S10, S18 | [ADR-0001](decisions/0001-domain-terminology-and-identity.md), [ADR-0004](decisions/0004-turn-and-attempt-lifecycle.md) | Owner decision pending; domain reviewer unassigned |
| Final name for one provider interaction? | Provenance and provider retries require a stable unit, including attempts that fail before send. | Model call; provider call; inference attempt | Proposed: every hub authorization to attempt a provider interaction is a distinct model call, whether or not it reaches the provider | Blocking before provider slice | S02, S04, S20–S23 | [ADR-0001](decisions/0001-domain-terminology-and-identity.md), [ADR-0005](decisions/0005-model-call-retry-semantics.md) | Owner decision pending; provider reviewer unassigned |
| Final names for logical and physical tool work? | Approval must bind to logical intent while retries identify effects. | Tool request/tool attempt; invocation/execution | Proposed: tool request and tool attempt remain distinct | Blocking before tool slice | S05–S16 | [ADR-0001](decisions/0001-domain-terminology-and-identity.md) | Owner decision pending; tool-safety reviewer unassigned |

## Session semantics

| Question | Why it matters | Plausible choices | Current leaning | Timing | Scenarios affected | Proposed ADR | Owner/reviewer status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| How are session creation cause and initial transcript context represented? | Why a session exists and where its initial context came from can vary independently and must not be reconstructed from relationship links. | One parent field; cause-derived context; independent immutable cause and ancestry | Proposed: required independent immutable facts; the first implementable cause is owner-initiated, future feature ADRs add causes with exact initiator identities, and ancestry is none or one exact source frontier | Blocking before session domain types | S01, S17, S18 | [ADR-0003](decisions/0003-session-creation-and-transcript-ancestry.md) | Owner decision pending; domain/lifecycle reviewers unassigned |
| How do session-level configuration defaults relate to accepted turn configuration? | Users need persistent per-session choices, but changing them must not rewrite queued, active, or recovering work. | No session defaults; mutable defaults read at execution; versioned defaults frozen per accepted origin | Proposed: creation establishes defaults version one; explicit updates install immutable later versions for future origins only. Each explicitly configured origin records its request, exact defaults version, and effective value; pending steering records only its source turn, and a reclassified origin derives that turn's canonical immutable effective value. | Blocking before session and input configuration types | S01, S03, S08, S09 | [ADR-0003](decisions/0003-session-creation-and-transcript-ancestry.md), [ADR-0027](decisions/0027-input-delivery-lifecycle.md) | Owner decision pending; domain/configuration reviewers unassigned |
| How does parent cancellation propagate to active delegated children? | Over-cancellation loses independent work; under-cancellation wastes effects. | Always cascade; never cascade; relationship policy; prompt owner | Explicit relationship policy with visible child outcome | Blocking before delegation | S18, S19 | ADR-0002 Delegation lifecycle | Owner decision pending; lifecycle reviewer unassigned |
| Is detached delegated work in version one? | It changes cancellation, archival, and result delivery. | Exclude; include from start; allow detach after creation | Exclude unless a core scenario proves need | Deferrable, but decide before delegation scope | S18, S19 | ADR-0002 Delegation lifecycle | Owner decision pending; product reviewer unassigned |
| How are child results represented in the parent conversation? | Model visibility and audit history must not depend on UI-only links. | Structured result message; tool result; reference plus import; summary | Structured durable reference plus explicit delivered content | Blocking before delegation | S18, S19 | ADR-0002 Delegation lifecycle | Owner decision pending; domain reviewer unassigned |
| May transcript ancestry later have multiple sources or merge? | Present identities must not prevent extension or imply merge semantics now. | Permanently single source; multi-source DAG; explicit merge operation | Proposed: none or one immutable source frontier initially; merging remains an explicit non-decision with an extension boundary | Deferrable | S17 | [ADR-0003](decisions/0003-session-creation-and-transcript-ancestry.md) | Owner decision pending; domain reviewer unassigned |

## Input and archival lifecycle

| Question | Why it matters | Plausible choices | Current leaning | Timing | Scenarios affected | Proposed ADR | Owner/reviewer status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| How is input submitted when no turn is active represented and made eligible? | The three accepted delivery-policy intents describe input during active work; reusing one for the first or other no-active-turn input would otherwise invent predecessor semantics and an implicit protocol contract. | A distinct immediate treatment; normalize an allowed active-work policy to immediate eligibility; one command whose treatment depends explicitly on session state; reject active-work-only policies while no turn is active | Proposed: explicit `start when no active turn`; it joins any existing queued FIFO tail, and stale or active-work-only requests fail before acceptance rather than normalize silently | Blocking before input protocol and first-turn implementation | S01, S03 | [ADR-0027](decisions/0027-input-delivery-lifecycle.md) | Owner decision pending; lifecycle/protocol reviewers unassigned |
| What is the precedence and mismatch behavior for durable command deduplication? | A network retry may arrive after the session has advanced; validating current state first could reject a command that was already accepted or apply an identifier to different intent. | Validate then deduplicate; deduplicate then validate; allow identifier replacement | Proposed: look up command identity first; the same typed payload by structural domain equality returns the stored result, a different payload with the same identity is rejected, and only an unseen command validates current state | Blocking before durable command transitions | S01, S04, S07, S10–S12 | [ADR-0004](decisions/0004-turn-and-attempt-lifecycle.md), [ADR-0027](decisions/0027-input-delivery-lifecycle.md) | Owner decision pending; domain/reliability reviewers unassigned |
| What qualifies as a safe point, what may consume pending steering, and how is steering handled if the turn becomes terminal first? | Context ordering, tool behavior, restart recovery, and visible unconsumed input must not depend on an implicit orchestration boundary. | Provider-call boundaries only; selected tool/orchestration boundaries; an explicit typed set; expose, reclassify, or cancel unconsumed steering | Proposed: only the boundary before a new model call after every earlier issued physical operation is classified and every earlier logical tool/approval dependency has a durable outcome; an authorized-but-undispatched request remains blocking. Every prepared call, including a duplicate-risk replacement, consumes eligible steering into history and records consumption by call identity without duplicating its turn/frontier. Terminal-before-consumption visibly reclassifies it behind any interrupt-created immediate successor, then among ordinary queued work by original acceptance order | Blocking before turn-state implementation | S08, S10 | [ADR-0027](decisions/0027-input-delivery-lifecycle.md) | Owner decision pending; lifecycle/domain reviewers unassigned |
| When are session defaults, queued logical work, and effective configuration fixed? | Session-level user preferences must remain useful without allowing later changes to rewrite accepted intent, configuration provenance, recovery, or identity. | Resolve at execution; freeze at acceptance; remove session defaults; version defaults and freeze each origin | Proposed: session creation establishes versioned defaults and explicit updates affect only future origin acceptance. Each explicitly configured origin atomically records its request, exact defaults version, and complete effective value. A reclassified steering origin instead records inherited source-turn provenance. Direct starting lineage, starting context, and the first attempt are fixed only at eligibility after every earlier ordered turn terminates. | Blocking before queued-turn implementation | S01, S03, S08, S09 | [ADR-0027](decisions/0027-input-delivery-lifecycle.md) | Owner decision pending; lifecycle/configuration reviewers unassigned |
| Which context frontier does queued or interrupt-created input observe after each predecessor outcome? | Success, refusal, failure, cancellation, and interruption may produce different authoritative content and effect evidence; an implicit “latest” frontier would make recovery nondeterministic. | One terminal frontier rule; outcome-specific frontier rules; explicit frontier selected when new work becomes eligible | Proposed for accepted-input-origin turns: durable acceptance positions and typed priority relations determine total queue order without freezing direct predecessor pointers at acceptance. At eligibility, session ancestry is used only for the first turn; every later turn fixes starting lineage through the terminal turn immediately before it and includes that predecessor's explicit completed, refused, failed, cancelled, or reconciliation-required marker. Interrupt and reclassified-steering insertions therefore precede every affected frontier without relinking fixed state. Future non-input origins keep separate lineage and context decisions | Blocking before queued-turn and interrupt semantics | S03, S07, S09 | [ADR-0027](decisions/0027-input-delivery-lifecycle.md) | Owner decision pending; domain/reliability reviewers unassigned |
| May queued input be edited, canceled, reordered, or have its delivery policy changed? | These operations can alter durable intent, idempotency, queue identity, and client convergence, but are not required to define baseline queued-turn semantics. | Immutable FIFO input; cancellation only; replacement commands; editable/reorderable queue | Proposed baseline: none are supported; any addition requires a future queue-management ADR and explicit dispositions | Later scope; does not block initial queued turns if excluded | S09 | [ADR-0027](decisions/0027-input-delivery-lifecycle.md) for the scope boundary; future queue-management ADR if added | Owner decision pending; product/client reviewers unassigned |
| Which session states may be archived, how is nonterminal work handled, and what lifecycle state is restored? | Archive could otherwise abandon active effects, violate the single-progressing-turn rule, or revive a session into an unexplained state. | Reject while nonterminal; wait; request cancellation then archive; explicit per-state matrix; restore prior, inactive, or active state | Preserve identity and history and never silently abandon work; archive eligibility, cancellation/wait behavior, and restore target remain open | Blocking before archive/restore implementation | S25 | ADR-0028 Session archive lifecycle | Owner decision pending; lifecycle/reliability reviewers unassigned |
| How does archive affect delegated children and other related sessions? | Implicit cascade or coupling would conflict with independent session identity and could hide or stop related work. | Archive independently; cascade; block while relationships are active; explicit relationship policy or owner choice | Require an explicit typed policy and visible outcomes; no cascade or independence rule is yet selected | Blocking before archive supports delegated or related sessions; standalone archival may proceed only if the ADR excludes this case | S18, S19, S25 | ADR-0028 Session archive lifecycle | Owner decision pending; lifecycle/product reviewers unassigned |
| What destructive retention or purge behavior exists beyond ordinary archive? | Deletion changes provenance, ancestry, delegated relationships, and recovery guarantees, while archive is intended to preserve identity and history. | No purge; retention windows; owner-requested purge; tombstones or retained provenance | Keep destructive deletion separate from ordinary archive; exact retention and purge policy is later scope | Later scope; blocking only before destructive deletion or automated retention | S17, S25 | ADR-0029 Retention and destructive deletion | Owner decision pending; storage/security reviewers unassigned |

## Retry and regeneration

| Question | Why it matters | Plausible choices | Current leaning | Timing | Scenarios affected | Proposed ADR | Owner/reviewer status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| When does recovery add a turn attempt versus create new logical work? | It defines user intent identity and prevents startup bookkeeping from masquerading as resumed execution. | Failure matrix; always same turn; always new turn | Proposed: startup scans and ends every prior-process nonterminal turn attempt as `Lost`, separately classifies all physical operations, and creates no attempt. An abandoned tenure with neither operation ambiguity nor stronger terminal evidence fails the turn. A replacement attempt in the same turn exists only after resolving evidence closes the wait with unfinished work, or an exact-set owner decision accepts unresolved duplicate risk; both preserve origin, configuration, history, and evidence. | Blocking before recovery implementation | S03–S06 | [ADR-0004](decisions/0004-turn-and-attempt-lifecycle.md) | Owner decision pending; reliability reviewer unassigned |
| Is manual regeneration new logical work linked to the original? | It is user-visible and may use changed context or configuration. | New turn relation; new attempt; replace old output | Proposed identity rule: always new logical work and never reopen the original; command acceptance, queue placement, source frontier, and relation representation remain for a future regeneration ADR | Blocking only before regeneration feature; excluded from first turn state machine | S26 | [ADR-0004](decisions/0004-turn-and-attempt-lifecycle.md) for identity; future regeneration lifecycle ADR | Owner decision pending; product/domain reviewers unassigned |
| How do provider-call retries differ from turn retries? | Transport retry may duplicate billing/effects without changing orchestration intent. | Adapter-internal; explicit model calls; grouped call attempts | Proposed: resolve and pin an exact target before creating each distinct call authorization. Version one never automatically retries known or ambiguous provider failure. Known failure supplies terminal failure evidence and fails the turn once no unacknowledged ambiguity requires waiting or reconciliation; only an exact-set owner decision after ambiguity may authorize another call and attempt in the same turn. That decision closes the wait, consumes eligible steering, creates the replacement attempt and prepared call, and transfers conversational-outcome authority in one transaction serialized against resolving prior-call evidence. Prior-call results after transfer remain audit/reconciliation evidence and cannot compete with the replacement. Any future automatic replacement policy inherits this rule unless it defines a typed multi-result lifecycle. | Blocking before provider implementation | S04, S20–S23 | [ADR-0005](decisions/0005-model-call-retry-semantics.md) | Owner decision pending; provider reviewer unassigned |
| What happens to a turn after any issued operation has an ambiguous outcome when neither accepted cancellation nor fatal mismatch prohibits continuation? | Waiting retains the session slot while terminal reconciliation releases it; leaving the choice implicit makes queue behavior depend on recovery timing or effect kind. | Always await owner decision; terminal reconciliation immediately; effect-policy choice | Proposed: enter `Active(AwaitingRecoveryDecision)` with the exact nonempty blocking set. An exact-set owner command chooses reconciliation or continue-with-duplicate-risk, which preserves each physical ambiguity and adds `DuplicateRiskAccepted`. Separate evidence records resolve blocking references without rewriting physical outcomes, retaining the wait for an exact nonempty subset. Accepted cancellation or fatal stop instead yields reconciliation; no ambiguity retry is automatic. | Blocking before provider or tool recovery | S04, S06, S07, S09 | [ADR-0004](decisions/0004-turn-and-attempt-lifecycle.md), [ADR-0005](decisions/0005-model-call-retry-semantics.md), ADR-0013 Tool outcome and retry | Owner decision pending; reliability/provider/tool-safety reviewers unassigned |
| Does a model change create a call, attempt, or new logical turn? | Model choice is part of effective intent and provenance. | New call in turn; new turn attempt; new logical work by cause | Proposed baseline: freeze alias definition/version at origin acceptance, resolve and pin the first exact target before model-call creation, and treat any manual or implicit target/configuration change as new logical work unless a later accepted frozen policy expressly authorizes it | Blocking before model switching | S20–S23, S26 | [ADR-0005](decisions/0005-model-call-retry-semantics.md) | Owner decision pending; domain reviewer unassigned |
| Which turn disposition follows a provider refusal? | Treating refusal as completion hides an unmet request, while treating it as infrastructure failure may authorize unsafe retry or fallback. | Completed with refusal outcome; failed; distinct refused disposition; typed remediation wait | Proposed: without fatal stop, an outcome-authoritative refusal with no target mismatch atomically ends call, attempt, and turn with distinct refusal dispositions; serial orchestration guarantees no unrelated owned work can keep that ordinary path active. A continuation that races refusal after another call triggered fatal mismatch remains physically refused but non-authoritative, and fatal failure/reconciliation controls the turn. If resolving non-mismatched evidence arrives after call/attempt ambiguity, those terminal physical records remain ambiguous while the waiting turn may become `Refused`. Mismatch delivered with ordinary refusal is classified before commit and selects known failure; later evidence cannot rewrite a terminal refusal. No path authorizes fallback. | Blocking before provider state machine | S04, S23 | [ADR-0005](decisions/0005-model-call-retry-semantics.md) | Owner decision pending; provider/domain reviewers unassigned |

## Model fallback and substitution

| Question | Why it matters | Plausible choices | Current leaning | Timing | Scenarios affected | Proposed ADR | Owner/reviewer status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Does version one support automatic fallback? | It changes predictability, cost, and provenance. | None; availability-only; configurable matrix | No automatic fallback until explicit policy is justified | Deferrable for first provider slice | S22, S23 | ADR-0006 Model fallback policy | Owner decision pending; product reviewer unassigned |
| Which failures permit fallback? | Authentication, capacity, malformed requests, and refusal have different meaning. | Allowlist; denylist; provider-specific mapping | Narrow allowlist of classified availability failures if fallback exists | Blocking if fallback is included | S22, S23 | ADR-0006 Model fallback policy | Owner decision pending; provider reviewer unassigned |
| Is availability/capacity different from safety refusal? | Treating refusal as outage can evade intended safety behavior. | Same retry class; distinct classes; never fallback on refusal | Distinct classes; refusal alone never authorizes fallback | Blocking before fallback | S22, S23 | ADR-0006 Model fallback policy | Owner decision pending; safety reviewer unassigned |
| How is fallback configured and exposed? | The owner must know what may run and what did run. | Session policy; per-turn confirmation; global aliases; combinations | Explicit session/turn policy with per-call provenance and clear UI | Blocking before fallback | S20, S22 | ADR-0006 Model fallback policy | Owner decision pending; client reviewer unassigned |
| How is provider-reported substitution or mismatch recorded and handled? | The provider may report an identity different from the exact hub-resolved target, while its hidden physical backend may remain unknowable. | Observable identity field; separate provenance record; classify or fail a reported mismatch | Proposed baseline: preserve requested, exact resolved, and provider-reported values as distinct facts; mismatch is never allowed fallback. First trusted observation while the outcome-eligible call is nonterminal immediately selects known failure and requests best-effort cancellation. After terminal ambiguity it preserves physical state, failing the turn when no other unacknowledged ambiguity remains and requiring reconciliation otherwise. After current-authority completion during an active turn it preserves call/history, appends typed invalidation, and stops new/outstanding work before failure or reconciliation. An ordinary outcome-authoritative refusal without fatal stop terminalizes the baseline turn atomically, so mismatch delivered with it is classified first and later evidence cannot rewrite it; refusal raced under a prior or same-scan fatal cause remains physical and non-authoritative. After terminal known failure/cancellation it preserves that state and existing precedence; after authority transfer it is non-authoritative; after turn terminalization it cannot rewrite committed history. ADR-0007 retains identifier normalization and detailed provenance representation | Blocking before provider provenance schema | S20–S23 | [ADR-0005](decisions/0005-model-call-retry-semantics.md) for disposition; ADR-0007 Model provenance for representation | Owner direction recorded; provider reviewer unassigned |

## Scheduling and runner placement

| Question | Why it matters | Plausible choices | Current leaning | Timing | Scenarios affected | Proposed ADR | Owner/reviewer status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| What is the exact runner capability, evidence, and placement model? | Eligibility and user-visible guarantees require declared, configured, verified, and per-dispatch effective properties to remain distinct. | Declaration-only matching with claim-only disclosure; trusted configured profiles; verified evidence with policy predicates; combinations | Typed core properties with explicit evidence levels and extensible attributes; effective guarantees never exceed supporting evidence | Blocking before runner protocol | S05–S16 | ADR-0008 Runner capabilities and placement | Owner decision pending; runner/security reviewers unassigned |
| What are runner pinning and workspace-affinity rules? | A workspace may only exist on one runner and may move over time. | Session pin; turn pin; tool-level choice; affinity hints | Explicit session/turn pin where locality matters, with observable failure | Blocking before workspace tools | S05–S16 | ADR-0008 Runner capabilities and placement | Owner decision pending; runner reviewer unassigned |
| May more than one runner participate in one turn? | It complicates fencing, locality, approvals, and failure recovery. | Exactly one; multiple sequentially; multiple concurrently | Permit at most one selected runner initially unless hub-local tools are counted separately | Deferrable but constrain version one | S13–S16 | ADR-0008 Runner capabilities and placement | Owner decision pending; scheduler reviewer unassigned |
| What fencing rejects stale dispatch/results? | Reconnected or delayed runners must not overwrite current work. | Monotonic generation; leases; attempt tokens; combinations | Durable attempt identity plus generation/equivalent compare-and-set | Blocking before runner dispatch | S05, S06, S12 | ADR-0009 Dispatch fencing | Owner decision pending; database reviewer unassigned |
| Is Postgres alone sufficient for the initial scheduler? | Coordination and recovery complexity should match real load. | Postgres queue/locks; external broker; workflow engine | Start with Postgres if correctness and wake-up tests pass; preserve adapter boundary | Deferrable until scheduler prototype | S03, S05, S12 | ADR-0010 Initial scheduler mechanics | Owner decision pending; operations reviewer unassigned |
| Which pending or waiting states retain the session's single progressing-turn slot? | Pending safe-point steering, confirmation, future delegation, and recovery affect ordering and whether queued input may start. | All nonterminal turns retain it; only executing states retain it; explicit per-state policy | Proposed: every implemented `Active` phase retains the slot. Running owns one current attempt whose `StopRequested` variant distinguishes cancellation-only from fatal mismatch with a nonempty failure set and optional cancellation; typed terminal variants prevent a fatal stop from becoming completed/refused/cancelled. All operations are classified before an attempt yields to a typed wait; prepared/running/wait cancellation have distinct transitions; startup scans create no attempt; and terminalization closes every attempt, operation, and wait. Future child waits require ADR-0002 unless it defines branching semantics. | Blocking before scheduler state machine | S04, S07–S11, S18, S19 | [ADR-0004](decisions/0004-turn-and-attempt-lifecycle.md) for baseline semantics; ADR-0002 for child waits; ADR-0010 for enforcement mechanics | Owner decision pending; domain/scheduler reviewers unassigned |

## Tool safety

| Question | Why it matters | Plausible choices | Current leaning | Timing | Scenarios affected | Proposed ADR | Owner/reviewer status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| What is the tool-risk classification? | Confirmation and retry cannot depend on vague “risky” labels. | Effect taxonomy; tool allowlists; argument-aware rules | Argument-aware effect taxonomy with conservative unknown class | Blocking before tool execution | S05, S06, S10, S11, S15, S16 | ADR-0011 Tool risk and approval | Owner decision pending; security reviewer unassigned |
| Which operations require confirmation? | Too broad blocks use; too narrow exposes owner resources. | Always; risk-based; runner-policy based; per-tool grants | Hub risk policy considering arguments, placement, and prior scoped grants | Blocking before tool execution | S10, S11, S13–S16 | ADR-0011 Tool risk and approval | Owner decision pending; security reviewer unassigned |
| May an LLM judge influence approval policy? | Automated advice can be mistaken for owner consent. | No judge; advisory only; bounded auto-policy input | Advisory or bounded policy signal only, never human approval identity | Deferrable | S10, S11 | ADR-0012 Automated safety recommendations | Owner decision pending; safety reviewer unassigned |
| What is the retry policy for side-effecting commands/tools? | Lost responses can duplicate irreversible effects. | Never retry; idempotency-aware; reconciliation workflow | Classify effect and evidence; never auto-retry ambiguous writes | Blocking before tool retry | S05, S06, S12 | ADR-0013 Tool outcome and retry | Owner decision pending; reliability reviewer unassigned |
| What sandboxing is required initially? | “Runner” and a self-declared label alone say nothing about containment. | Ambient allowed; minimum container; evidence-backed profiles by deployment | Support explicit ambient and restricted profiles only to the strength justified by effective evidence; exact minimums and verification mechanisms remain open | Blocking before runner release | S13, S14 | ADR-0014 Runner execution boundaries | Owner decision pending; security reviewer unassigned |
| How should an ambient-user runner behave? | It can intentionally wield broad owner authority. | Same policy; extra confirmations; separate trust mode; capability limits | Explicit selection and visible boundary, likely stricter policy for material effects | Blocking before ambient runner | S13 | ADR-0014 Runner execution boundaries | Owner decision pending; product/security reviewers unassigned |

## Identity, credentials, and resource governance

| Question | Why it matters | Plausible choices | Current leaning | Timing | Scenarios affected | Proposed ADR | Owner/reviewer status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| How does the owner authenticate clients and revoke access? | Single-owner deployment still exposes durable conversations, approvals, and credentials over a network. | Identity-aware proxy; hub-issued credentials; external identity provider; platform credentials | Keep the hub's authorization model single-owner while choosing a remotely safe authentication boundary from deployment evidence | Blocking before any remote client | S01, S10, S24, S25 | ADR-0015 Identity and client authentication | Owner decision pending; security reviewer unassigned |
| How are runners enrolled, authenticated, and revoked? | An outbound connection must not let an impostor advertise privileged capabilities or submit results. | One-time enrollment plus certificates; scoped tokens; external workload identity | Strong runner identity distinct from capability claims, with revocation and rotation | Blocking before remote runners | S05, S06, S12–S16 | ADR-0016 Runner identity and enrollment | Owner decision pending; security/operations reviewers unassigned |
| Where and how are provider and integration credentials stored and rotated? | Hub ownership does not by itself prevent leakage or define operational recovery. | Kubernetes-integrated secrets; external secret manager; encrypted database records; combinations | Keep secret values outside ordinary session records and expose them only to the responsible hub adapter | Blocking before real provider/tool credentials | S02, S15, S20–S23 | ADR-0017 Credential lifecycle | Owner decision pending; security/operations reviewers unassigned |
| Which resource limits exist in the first release? | A single owner can still trigger runaway cost, concurrency, storage, or tool execution. | Global quotas; per-session/runner budgets; provider limits; layered controls | Begin with explicit bounded concurrency and configurable usage limits at effect boundaries | Blocking before public release; exact advanced quotas deferrable | S02–S06, S13–S18 | ADR-0018 Resource governance | Owner decision pending; operations/product reviewers unassigned |

## Protocols and persistence representation

| Question | Why it matters | Plausible choices | Current leaning | Timing | Scenarios affected | Proposed ADR | Owner/reviewer status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Protobuf/gRPC or another process protocol? | It affects streaming, code generation, compatibility, and browsers. | Protobuf/gRPC; Connect; JSON/HTTP; custom combination | Define semantics and fixtures before selecting transport | Blocking before cross-process implementation | S01, S02, S12, S24 | ADR-0019 Process protocol | Owner decision pending; protocol reviewer unassigned |
| What browser transport is used? | Browser constraints differ from native and runner links. | WebSocket; SSE plus commands; Connect-Web; other | Preserve authoritative snapshot plus transient stream semantics; technology open | Blocking before web client | S02, S24 | ADR-0020 Client streaming transport | Owner decision pending; web reviewer unassigned |
| How do peers negotiate protocol versions and capabilities? | Independent client/runner updates need explicit failure behavior. | Version handshake; capability negotiation; URL versioning | Version plus capability handshake with explicit incompatibility | Blocking before remote clients/runners | S12, S24 | ADR-0021 Compatibility policy | Owner decision pending; protocol reviewer unassigned |
| What storage representation is stable? | Domain evolution and recovery should not be coupled to wire messages. | Normalized tables; append log plus projections; hybrid | Postgres-native schema with explicit migrations; event/log use only where justified | Blocking before persistence implementation | S03, S04, S25 | ADR-0022 Persistence representation | Owner decision pending; database reviewer unassigned |
| How are Swift client types generated or maintained? | Native compatibility and ergonomic types may pull against one another. | Generated wire types; hand-written API layer; hybrid | Generated boundary types mapped to hand-written client domain types | Deferrable until Swift client | S01, S24 | ADR-0023 Swift protocol integration | Owner decision pending; Swift reviewer unassigned |
| What compatibility policy applies across releases? | Public clients and remote runners will not upgrade atomically. | Lockstep; N/N-1; capability-based window | Small documented compatibility window with fixtures; exact window open | Blocking before first public release | S12, S24 | ADR-0021 Compatibility policy | Owner decision pending; release reviewer unassigned |

## Client scope

| Question | Why it matters | Plausible choices | Current leaning | Timing | Scenarios affected | Proposed ADR | Owner/reviewer status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Which client follows the hub foundation first? | It determines the earliest vertical slice and protocol pressure. | CLI/TUI; web; Swift | A thin terminal client is plausible, but no choice is accepted | Deferrable until hub slice is framed | S01, S02, S24 | ADR-0024 First client and vertical slice | Owner decision pending; product reviewer unassigned |
| Is the first interface CLI/TUI, web, or Swift? | “Terminal” itself can mean scriptable CLI or stateful TUI. | CLI; TUI; web; macOS/iOS | Prefer the smallest interface that exercises reconnect, approval, and provenance | Deferrable | S01, S10, S24 | ADR-0024 First client and vertical slice | Owner decision pending; client reviewer unassigned |
| How is native Apple code organized? | Shared Swift code and platform targets affect repository boundaries. | One package/project; workspace with shared modules; separate apps | Defer until protocol and first native slice are known | Deferrable | S01, S24 | ADR-0025 Apple client organization | Owner decision pending; Swift reviewer unassigned |
| Does the web client use Rust or TypeScript? | It affects build tooling, shared types, and contributor surface. | Rust/Wasm; TypeScript; hybrid | No leaning until browser protocol and product slice are measured | Deferrable | S01, S02, S24 | ADR-0026 Web client technology | Owner decision pending; web reviewer unassigned |

## Ledger maintenance

An ADR must update or close every ledger row it decides, link the affected scenarios and invariants, and list explicit non-decisions. A prototype may inform a leaning without changing a row to decided.
