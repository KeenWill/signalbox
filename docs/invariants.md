# Invariant catalog

This catalog indexes constraints the design must make enforceable. For an invariant accepted by an ADR, the ADR is the normative statement and the row here is a summary plus an enforcement index; for the rest, the row itself is the statement of record. "Accepted" records current direction; "Provisional" means the constraint or its exact boundary needs a decision. Each invariant has a primary classification; actual enforcement may use several layers, and the enforcement column accumulates links to the tests, types, and constraints that enforce the invariant as slices are implemented.

## Domain and identity

| ID | Invariant | Class | Status | Enforcement |
| --- | --- | --- | --- | --- |
| INV-001 | Owner-global durable-command identifiers and identifiers for sessions, accepted inputs, turns, turn attempts, model calls, tool requests, tool attempts, runners, semantic transcript entries, and context-frontier snapshots are distinct and not interchangeable. A claimed durable-command identifier names one canonical typed caller payload and its terminal applied-or-rejected result; it is not by itself proof that the command was applied, and lifecycle authority uses purpose-specific proof types constructed from matching applied results. | Domain/type-level | Accepted; normative: [ADR-0001](decisions/0001-domain-terminology-and-identity.md), [ADR-0030](decisions/0030-context-frontier-snapshots.md) for context-frontier identity, and [ADR-0033](decisions/0033-identity-generation-supply-and-encoding.md) for UUID mechanics; persistence reconstitution: [ADR-0035](decisions/0035-domain-owned-persistence-reconstitution.md) | Separate semantically opaque domain types. The ADR-0001 identity types are private UUID-backed newtypes in [`crates/domain/src/lib.rs`](../crates/domain/src/lib.rs); runner identity remains undecided. Distinct private UUID-backed semantic-entry and context-frontier identities, composite frontier ownership, and raw-construction rejection: INV-001-tagged tests and compile-fail examples in [`crates/domain/src/context_frontier.rs`](../crates/domain/src/context_frontier.rs). Boundary conversion validates kind, ownership, canonical construction, result, and scope independently of storage or wire encoding. Applied-interrupt authority opacity and applied-versus-rejected correlation: INV-001-tagged tests and compile-fail examples in [`crates/domain/src/applied_interrupt.rs`](../crates/domain/src/applied_interrupt.rs). |
| INV-002 | Domain types are separate from transport messages, storage records, and framework types. | Domain/type-level | Accepted; persistence mapping: [ADR-0022](decisions/0022-persistence-representation.md); reconstitution boundary: [ADR-0035](decisions/0035-domain-owned-persistence-reconstitution.md) | Explicit mapping modules and dependency checks; no generated or ORM type in domain transitions. Purpose-specific complete domain projections reconstruct opaque values without admitting SQL rows or framework types. Positive full-`u64` ordinal decoding and kind-specific UUID conversion: INV-002-tagged tests in [`crates/persistence/src/mapping.rs`](../crates/persistence/src/mapping.rs). Domain-owned checked ordinal reconstitution over the complete positive `u64` domain: INV-002-tagged tests in [`crates/domain/src/configuration.rs`](../crates/domain/src/configuration.rs) and [`crates/domain/src/queue_order.rs`](../crates/domain/src/queue_order.rs). |
| INV-003 | Session creation cause and transcript ancestry are independent facts. | Domain/type-level | Accepted; normative: ADR-0003 | Separate fields/types; no single mutually exclusive enum combining both concepts. Independent typed cause and ancestry facts, plus sealed S01 baseline preparation and complete reconstitution: INV-003-tagged tests and compile-fail examples in [`crates/domain/src/session.rs`](../crates/domain/src/session.rs). The admitted owner/no-ancestry storage slice keeps independently constrained columns: S01/INV-003 tests in [`crates/persistence/tests/postgres_integration.rs`](../crates/persistence/tests/postgres_integration.rs); transaction-level atomicity remains later work. |
| INV-004 | Logical work and every physical attempt have separate identities and lifecycle records; every turn has one typed durable origin, while accepted steering remains separately identified. A tool request belongs to one turn, and each tool attempt belongs to one request and one issuing turn attempt. Identity is selected by typed transitions, never by semantic comparison of free-form intent. | Domain/type-level | Accepted; normative: ADR-0001 | State-machine types prevent a physical retry from overwriting logical identity or steering from masquerading as a new turn. Accepted-input disposition transitions preserve identity: INV-004-tagged tests in [`crates/domain/src/accepted_input.rs`](../crates/domain/src/accepted_input.rs). Current-to-ended attempt transitions preserve the physical-attempt identity: INV-004-tagged tests in [`crates/domain/src/turn_attempt.rs`](../crates/domain/src/turn_attempt.rs). Model-call transitions preserve the physical call identity and pinned target: INV-004-tagged tests in [`crates/domain/src/model_call.rs`](../crates/domain/src/model_call.rs). |
| INV-005 | Semantic conversation content, operational coordination, audit/recovery evidence, transient streaming, and client presentation are distinct representations even when correlated. | Domain/type-level | Accepted; initial semantic-entry boundary: [ADR-0036](decisions/0036-initial-semantic-transcript-entries.md); accepted-input content: [ADR-0037](decisions/0037-baseline-user-content.md) | Purpose-specific domain projections; no universal event type serving all roles. Origin accepted-input and failed-turn entries reference their authoritative domain facts without becoming storage records, operational evidence, or presentation messages. |
| INV-006 | Each transition is valid only from explicitly permitted prior states; terminal turns and physical outcomes do not return to running. No turn terminalizes until every owned logical dependency is durably closed, every issued physical operation is classified, and its current attempt is ended; ordinary terminal outcomes additionally require every physical ambiguity to be resolved or owner-accepted, and `ReconciliationRequired` carries the exact nonempty ambiguity set and a typed cause. | Domain/type-level | Accepted; normative: [ADR-0004](decisions/0004-turn-and-attempt-lifecycle.md) and its direct-fatal refinement [ADR-0031](decisions/0031-direct-fatal-terminalization.md); persistence reconstitution: [ADR-0035](decisions/0035-domain-owned-persistence-reconstitution.md) | Total transition functions over typed states plus persistence guards. Accepted-input disposition transitions reject invalid prior states: INV-006-tagged tests in [`crates/domain/src/accepted_input.rs`](../crates/domain/src/accepted_input.rs). Canonical nonempty stop causes, cause-specific terminal values, the attempt-local predecessor matrix, and terminal irreversibility: INV-006-tagged tests and compile-fail examples in [`crates/domain/src/turn_attempt.rs`](../crates/domain/src/turn_attempt.rs). Canonical exact-operation ambiguity sets, proof-bearing disposition and marker values, closed active-phase shapes, and fatal marker construction: INV-006-tagged tests and compile-fail examples in [`crates/domain/src/turn_lifecycle.rs`](../crates/domain/src/turn_lifecycle.rs); aggregate guards remain later. The call-local predecessor matrix and terminal-call irreversibility: INV-006-tagged tests and compile-fail proofs in [`crates/domain/src/model_call.rs`](../crates/domain/src/model_call.rs). Sealed post-evidence fatal closure derives complete causes, exact unfinished blockers, and an independently exact blocking-ambiguity remainder: S27 / INV-006-tagged tests in [`crates/domain/src/fatal_mismatch.rs`](../crates/domain/src/fatal_mismatch.rs). A consuming binding couples unfinished facts to the exact fatal stop and closed facts to a matching attempt-end/disposition candidate, with exact marker construction and fatal-stop fallback: S27 / INV-006-tagged tests in [`crates/domain/src/fatal_mismatch/lifecycle.rs`](../crates/domain/src/fatal_mismatch/lifecycle.rs). The separate prepared binding couples exact completed-call invalidation to an atomic-only known-failure candidate and rejects every physical blocker unchanged: S21 / INV-006-tagged tests in [`crates/domain/src/fatal_mismatch/prepared.rs`](../crates/domain/src/fatal_mismatch/prepared.rs); remaining aggregate guards and committed lifecycle transitions remain later work. |

## Durability and concurrency

| ID | Invariant | Class | Status | Enforcement |
| --- | --- | --- | --- | --- |
| INV-007 | Once the hub acknowledges accepted input, its identity, content, session, explicit delivery request, ordering, and enough disposition/configuration information to recover its promised treatment are durable. | Transaction-level | Accepted; normative: ADR-0027; content value: [ADR-0037](decisions/0037-baseline-user-content.md) | Persist the accepted input and initial disposition in one committed transaction before acknowledgement. |
| INV-008 | Accepted logical work has durable, immutable effective configuration and explicit provenance sufficient to explain later semantic choices. Session model-selection defaults are versioned and affect only subsequently accepted origin input; explicitly configured origins record request, defaults version, and effective value, while steering inherits its source turn's canonical immutable value. | Transaction-level | Accepted; normative: ADR-0027 (constructible categories and reclassification rules) | Create logical work and complete provenance atomically; never reread current defaults or alias definitions as historical intent. Value-level equality and constructibility clauses: INV-008-tagged tests in [`crates/domain/src/configuration.rs`](../crates/domain/src/configuration.rs); version-bound origin-request payloads: INV-008-tagged tests in [`crates/domain/src/delivery_request.rs`](../crates/domain/src/delivery_request.rs); S01 defaults-version-one preparation and reconstitution: INV-008-tagged tests in [`crates/domain/src/session.rs`](../crates/domain/src/session.rs). Immutable version rows and a constrained current pointer for the initial CreateSession schema: S01/INV-008 tests in [`crates/persistence/tests/postgres_integration.rs`](../crates/persistence/tests/postgres_integration.rs); atomic command handling remains later work. |
| INV-009 | At most one logical turn actively progresses in a session; every accepted `Active` phase retains the slot, and `Active(Running)` owns exactly one current attempt. Immutable queue-order facts live outside lifecycle state, and starting lineage and frontier are fixed atomically at eligibility, never at acceptance. | Database-level | Accepted; normative: [ADR-0004](decisions/0004-turn-and-attempt-lifecycle.md), [ADR-0027](decisions/0027-input-delivery-lifecycle.md), [ADR-0030](decisions/0030-context-frontier-snapshots.md), and [ADR-0031](decisions/0031-direct-fatal-terminalization.md); persistence enforcement: [ADR-0022](decisions/0022-persistence-representation.md); reconstitution: [ADR-0035](decisions/0035-domain-owned-persistence-reconstitution.md) | Database uniqueness/exclusion or serialized aggregate transition, not process memory alone. Queue-fact constructibility and collection-wide total-order derivation without starting lineage: INV-009-tagged tests and compile-fail example in [`crates/domain/src/queue_order.rs`](../crates/domain/src/queue_order.rs). Applied-result immediate-successor correlation: INV-009-tagged tests in [`crates/domain/src/applied_interrupt.rs`](../crates/domain/src/applied_interrupt.rs). Closed active-phase shapes with one running attempt or one exact wait subject: INV-009-tagged test and compile-fail examples in [`crates/domain/src/turn_lifecycle.rs`](../crates/domain/src/turn_lifecycle.rs); database exclusivity and atomic start fixing remain later work. Per-session serialization discipline layered over that database enforcement: [ADR-0010](decisions/0010-initial-scheduler-mechanics.md). |
| INV-010 | Queued work, confirmation waits, and delegated-result waits remain durably represented across hub restart. | Database-level | Accepted | Nonterminal states and referenced inputs persist without dependence on a live connection. Exact approval-wait subject shape: INV-010-tagged test and compile-fail example in [`crates/domain/src/turn_lifecycle.rs`](../crates/domain/src/turn_lifecycle.rs); persistence and additional wait kinds remain later work. No durable lease or live-process reference in the initial scheduler: [ADR-0010](decisions/0010-initial-scheduler-mechanics.md). |
| INV-011 | A stale physical attempt or dispatch generation cannot advance or overwrite current logical state. Tool-result application validates the attempt's owning request, issuing turn attempt, and current dispatch generation. | Transaction-level | Accepted; tool ownership cardinalities normative: ADR-0001; result-acceptance mechanics normative: ADR-0009 | Compare-and-set against current ownership, attempt, and generation in the same result-acceptance transaction. Exact predicate, the fence pair, and duplicate/stale/conflicting dispositions: [ADR-0009](decisions/0009-dispatch-fencing.md). |
| INV-012 | Duplicate command or result delivery cannot create duplicate logical work or apply one physical outcome twice. Owner-global command identity lookup across all command kinds, sessions, and clients precedes current-state validation; the first committed handling records the canonical typed payload and terminal applied-or-rejected result, equal replay returns it, conflicting reuse is rejected, and failures before committed handling claim no identifier. | Database-level | Accepted; normative: [ADR-0001](decisions/0001-domain-terminology-and-identity.md) and [ADR-0027](decisions/0027-input-delivery-lifecycle.md); persistence enforcement: [ADR-0022](decisions/0022-persistence-representation.md) and [ADR-0034](decisions/0034-durable-command-storage-and-equality.md); reconstitution: [ADR-0035](decisions/0035-domain-owned-persistence-reconstitution.md); baseline content equality: [ADR-0037](decisions/0037-baseline-user-content.md) | Canonical boundary construction plus one owner-wide transactional uniqueness boundary and versioned typed per-command payload/result records compared as domain values; result-delivery deduplication details remain operation-specific. Structural distinctness of caller payloads and typed CreateSession applied-result correlation: INV-012-tagged tests in [`crates/domain/src/configuration.rs`](../crates/domain/src/configuration.rs), [`crates/domain/src/delivery_request.rs`](../crates/domain/src/delivery_request.rs), and [`crates/domain/src/session.rs`](../crates/domain/src/session.rs). Owner-global uniqueness and complete one-to-one typed CreateSession record constraints: INV-012 tests in [`crates/persistence/tests/postgres_integration.rs`](../crates/persistence/tests/postgres_integration.rs). Tool-result deduplication case (idempotent redelivery, conflicting delivery): [ADR-0009](decisions/0009-dispatch-fencing.md). |
| INV-013 | Archiving changes discoverability/lifecycle state but does not erase durable history; restoration preserves identity. Exact archive and restore transitions remain open. | Database-level | Provisional pending the archive lifecycle decision (reserved ADR-0028) | Preserve identity and history while the archive decision defines permitted transitions; destructive retention remains a separate future policy. |

## Model execution

| ID | Invariant | Class | Status | Enforcement |
| --- | --- | --- | --- | --- |
| INV-014 | Every hub authorization to attempt a provider interaction is a model call with durable identity recording the requested selection, frozen alias meaning when applicable, exact hub-resolved provider/model target, and context frontier, even if send preparation later fails. Target resolution precedes call creation; resolution failure creates no targetless call. Provider-reported identity and mismatch are appended as typed evidence and classified by the spec's fixed precedence; evidence never reopens terminal state or rewrites committed content. | Transaction-level | Accepted; normative: [ADR-0001](decisions/0001-domain-terminology-and-identity.md), [ADR-0005](decisions/0005-model-call-retry-semantics.md), [ADR-0030](decisions/0030-context-frontier-snapshots.md) for call-frontier binding, and the direct-fatal refinement [ADR-0031](decisions/0031-direct-fatal-terminalization.md) | Freeze alias meaning at turn acceptance; resolve and persist the target and call before sending; classify observable target evidence without reopening terminal state. Pinned-target turn fact plus exact target-and-resolved-frontier-at-creation call constructibility: INV-014-tagged tests and compile-fail proofs in [`crates/domain/src/model_call.rs`](../crates/domain/src/model_call.rs); requested-selection recording, lifecycle-correct frontier selection, target resolution, and durable persistence remain later work. Typed observation payloads, evidence-identifier replay/reuse boundaries, unique completed-call mismatch invalidation, and sealed mismatch facts coupling each failure to its exact affected call and effect: INV-014-tagged tests and compile-fail proofs in [`crates/domain/src/provider_evidence.rs`](../crates/domain/src/provider_evidence.rs). Applying those facts to a complete owned-operation projection without reopening or rewriting terminal calls: S21 / S27 / INV-014-tagged tests in [`crates/domain/src/fatal_mismatch.rs`](../crates/domain/src/fatal_mismatch.rs). Prepared completed-call invalidation binds to exact fatal known-failure history without rewriting the call: S21 / INV-014-tagged test in [`crates/domain/src/fatal_mismatch/prepared.rs`](../crates/domain/src/fatal_mismatch/prepared.rs); aggregate outcome authority and persistence remain later work. |
| INV-015 | Every model call records the exact ordered semantic context frontier it consumed; a turn is not assumed to have one immutable frontier. | Transaction-level | Accepted; normative: [ADR-0030](decisions/0030-context-frontier-snapshots.md); persistence mapping: [ADR-0022](decisions/0022-persistence-representation.md); reconstitution: [ADR-0035](decisions/0035-domain-owned-persistence-reconstitution.md) | Exact composite identity, ordered-distinct resolved projection, explicit semantic-content comparison, and sealed prefix-preserving candidate derivation: INV-015-tagged tests and compile-fail examples in [`crates/domain/src/context_frontier.rs`](../crates/domain/src/context_frontier.rs). Exact frontier required at prepared-call construction and preserved through nonterminal and terminal history: INV-015-tagged tests and compile-fail examples in [`crates/domain/src/model_call.rs`](../crates/domain/src/model_call.rs). Lifecycle-correct derivation, atomic binding, and persistence remain later work. |
| INV-016 | Safe-point steering never mutates an already-issued provider request, tool request, approval, or tool attempt, and accepted steering never disappears without a durable disposition. The baseline safe point requires every earlier issued physical operation classified and every earlier logical dependency durably closed; each prepared call consumes eligible pending steering atomically, and committed steering remains in every later frontier. | Runtime validation | Accepted; normative: ADR-0027 | Consume steering atomically at the allowed boundary, preserve it in later frontiers, or reclassify it visibly with source-turn provenance. |
| INV-017 | Any hub-controlled fallback is explicit, policy-authorized, and visible in call provenance. | Operational policy | Accepted; whether fallback exists in version one is open | No adapter-local silent fallback; persist reason and source/target resolution. |
| INV-018 | A safety refusal does not by itself authorize automatic fallback or model substitution. An outcome-authoritative refusal without fatal stop atomically commits explicit refusal content and terminalizes call, attempt, and turn; a refusal raced under fatal stop remains physical evidence but non-authoritative, and mismatch interactions follow the spec's precedence. | Operational policy | Accepted; normative: ADR-0005 (racing and mismatch interactions) | Classify target evidence and refusal separately, serialize replacement against resolving evidence, persist every call and authority transfer, and reject implicit fallback, retry, or prior-call outcome competition. Attempt terminal values admit refusal without stop or after cancellation but no fatal-stop refusal: INV-018-tagged test and compile-fail example in [`crates/domain/src/turn_attempt.rs`](../crates/domain/src/turn_attempt.rs). |

## Tools, runners, and effects

| ID | Invariant | Class | Status | Enforcement |
| --- | --- | --- | --- | --- |
| INV-019 | Approval binds to one exact logical tool request, normalized arguments, and material execution constraints. | Transaction-level | Accepted; material-constraint set provisional | Approval references a content-bound request version; any material change invalidates it. |
| INV-020 | A model or automated judge recommendation never masquerades as human approval. | Domain/type-level | Accepted | Distinct actor and decision types; policy may consume recommendations only through an explicit rule. |
| INV-021 | Every runner result identifies its tool attempt and authorized dispatch generation or equivalent fence. | Protocol compatibility | Accepted; fence semantics normative: ADR-0009; wire encoding reserved to the protocol ADRs | Required protocol fields and hub-side validation. Required envelope fields and the result-acceptance predicate: [ADR-0009](decisions/0009-dispatch-fencing.md). |
| INV-022 | Declared and configured runner properties must be truthful, but are not automatically proof. Effective properties for a dispatch, and the guarantees presented to a client, never exceed what the available declaration, trusted configuration, and verified evidence support at their distinct evidentiary strengths. | Operational policy | Accepted; enrollment, verification, and attestation mechanisms open | Hub policy derives and durably snapshots effective properties with their evidence basis when work is assigned. |
| INV-023 | Selecting an ambient-user runner is explicit and inspectable, and the system does not label it sandboxed. Any stronger isolation label requires effective properties supported by correspondingly stronger evidence. | Runtime validation | Accepted | Evidence-aware capability matching, UI disclosure, and durable effective-boundary snapshot. |
| INV-024 | Tool policy and durable outcome ownership remain in the hub for both hub-local and runner-local placement. | Domain/type-level | Accepted | Shared lifecycle transitions; executors return evidence rather than editing conversation state. |
| INV-025 | An external side effect may end with an ambiguous outcome when acknowledgement is lost. | Domain/type-level | Accepted; direct-fatal boundary: [ADR-0031](decisions/0031-direct-fatal-terminalization.md) | Outcome enum preserves ambiguity rather than coercing it to failure. Supporting tagged exact-operation references and canonical nonempty ambiguity-set/marker payloads: INV-025-tagged tests in [`crates/domain/src/turn_lifecycle.rs`](../crates/domain/src/turn_lifecycle.rs); physical outcomes and transitions remain later work. Model-call classification keeps terminal `Ambiguous` a distinct disposition: INV-025-tagged test in [`crates/domain/src/model_call.rs`](../crates/domain/src/model_call.rs). Exact post-evidence blocking-ambiguity derivation: S27 / INV-025-tagged tests in [`crates/domain/src/fatal_mismatch.rs`](../crates/domain/src/fatal_mismatch.rs). Closed live binding preserves `Ambiguous` attempt history and carries exact `U` into its reconciliation-marker candidate: S27 / INV-025-tagged test in [`crates/domain/src/fatal_mismatch/lifecycle.rs`](../crates/domain/src/fatal_mismatch/lifecycle.rs); evidence thresholds and committed recovery handling remain later work. |
| INV-026 | An ambiguous external write is not automatically retried and its ambiguous physical record is never overwritten. Under the accepted lifecycle, owner-authorized continuation separately records accepted duplicate risk, while effect policy may still prohibit repetition. | Operational policy | Accepted; normative: [ADR-0004](decisions/0004-turn-and-attempt-lifecycle.md) and [ADR-0031](decisions/0031-direct-fatal-terminalization.md) | Recovery routes to reconciliation or explicit owner action; retry transition rejected by default. Supporting exact ambiguity-wait and marker payload values, with no retry transition introduced: INV-026-tagged tests in [`crates/domain/src/turn_lifecycle.rs`](../crates/domain/src/turn_lifecycle.rs). Post-evidence closure preserves physical ambiguity while excluding only evidence-resolved or duplicate-risk-accepted operations from the blocking remainder: S27 / INV-026-tagged tests in [`crates/domain/src/fatal_mismatch.rs`](../crates/domain/src/fatal_mismatch.rs). The sealed lifecycle candidate consumes that exact remainder without introducing retry or physical-record mutation: S27 / INV-026-tagged test in [`crates/domain/src/fatal_mismatch/lifecycle.rs`](../crates/domain/src/fatal_mismatch/lifecycle.rs); committed owner decisions, acknowledgements, and operation policy remain later work. |
| INV-027 | A denied tool request cannot create an authorized physical attempt. | Transaction-level | Accepted | Attempt creation checks current policy/approval state atomically. |

## Input, ancestry, and clients

| ID | Invariant | Class | Status | Enforcement |
| --- | --- | --- | --- | --- |
| INV-028 | Input declares an explicit treatment valid for authoritative session state: start when no turn is active or, during active work, interrupt, next safe point, or after current turn; the chosen treatment is durable. | Protocol compatibility | Accepted; normative: ADR-0027; default UX provisional | Make the resulting command field explicit before persistence and reject stale state races before acknowledgement. Caller-payload algebra and the absence of independent steering configuration: INV-028-tagged tests and compile-fail example in [`crates/domain/src/delivery_request.rs`](../crates/domain/src/delivery_request.rs). |
| INV-029 | Interrupt is the only baseline owner action that authorizes active-turn cancellation; its applied result creates a proof tied to the exact predecessor and the immediate-successor turn in the same transaction, and no other command can supply it. State-specific transitions, steering reclassification before slot release, and ambiguity handling follow the spec; no standalone active-turn cancellation command exists in the baseline. | Operational policy | Accepted; normative: ADR-0004 and ADR-0027 (state-specific transitions); persistence reconstitution: [ADR-0035](decisions/0035-domain-owned-persistence-reconstitution.md) | State-specific cancellation transitions plus honest outcome classification; successor activation waits for predecessor terminal disposition where necessary. Staged proof opacity and pure interrupt/predecessor/session/origin/successor correlation: INV-029-tagged tests and compile-fail examples in [`crates/domain/src/applied_interrupt.rs`](../crates/domain/src/applied_interrupt.rs); matching committed-result and same-transaction enforcement remains with the later aggregate. Proof-bearing stop/end values, interrupt/fatal cause union, and attempt-local cancellation transitions: INV-029-tagged tests in [`crates/domain/src/turn_attempt.rs`](../crates/domain/src/turn_attempt.rs). Sealed fatal-cause derivation retains the exact applied proof when cancellation-only stop upgrades or existing fatal causes grow: S07 / INV-029-tagged test in [`crates/domain/src/fatal_mismatch.rs`](../crates/domain/src/fatal_mismatch.rs). The lifecycle candidate carries that same proof through exact fatal `F`, matching attempt history, and the retained fatal-stop fallback: S07 / INV-029-tagged test in [`crates/domain/src/fatal_mismatch/lifecycle.rs`](../crates/domain/src/fatal_mismatch/lifecycle.rs). Proof-bearing cancellation and interrupt-reconciliation disposition/reason shapes: INV-029-tagged tests and compile-fail examples in [`crates/domain/src/turn_lifecycle.rs`](../crates/domain/src/turn_lifecycle.rs); state-specific aggregate transitions remain later work. Proof-correlated cancellation of an unsent prepared call: INV-029-tagged test in [`crates/domain/src/model_call.rs`](../crates/domain/src/model_call.rs). |
| INV-030 | A fork identifies an immutable source session and transcript frontier; later source changes do not rewrite the fork. | Database-level | Accepted for single-source ancestry; resolution boundary: [ADR-0030](decisions/0030-context-frontier-snapshots.md); persistence mapping: [ADR-0022](decisions/0022-persistence-representation.md) | Exact immutable source-session/transcript-frontier values with equality over both facts: INV-030-tagged tests in [`crates/domain/src/session.rs`](../crates/domain/src/session.rs). Source-qualified semantic-entry value representation, exact retention of supplied inherited references, and a new consuming-session-owned candidate projection: INV-030-tagged tests in [`crates/domain/src/context_frontier.rs`](../crates/domain/src/context_frontier.rs). Resolving an actual `TranscriptFrontier` and persistence remain later work. |
| INV-031 | A delegated child has a distinct session identity, is independently browsable, and has an explicit relationship to the parent work. This invariant does not determine whether related sessions archive independently, cascade, or block archival. | Domain/type-level | Accepted; archival coupling pending the archive lifecycle decision (reserved ADR-0028), with result and cancellation semantics also provisional | Separate session identity plus typed relationship, never embedded as transient substate only; the archive decision must define archival behavior. |
| INV-032 | A reconnecting client can reconstruct authoritative durable state and replace any transient draft without treating lost deltas as final content. | Protocol compatibility | Accepted | Snapshot/version protocol plus clearly marked transient updates. |

## Compatibility and operations

| ID | Invariant | Class | Status | Enforcement |
| --- | --- | --- | --- | --- |
| INV-033 | Unknown or incompatible protocol versions and required fields fail explicitly rather than being silently reinterpreted. | Protocol compatibility | Accepted; normative: [ADR-0019](decisions/0019-process-protocol.md) and [ADR-0021](decisions/0021-compatibility-and-negotiation.md) | Negotiation rules and compatibility fixtures remain to be implemented. |
| INV-034 | An idempotent startup scan derives the complete evidence and stop-cause set, ends every prior-process nonterminal turn attempt with disposition `Lost` in its matching terminal variant, and separately classifies each interrupted physical operation; the scan is not an attempt and issues no semantic effects. Live and startup classification share the single outcome-precedence order defined by the spec, and mismatch evidence never reopens a terminal call. | Runtime validation | Accepted; normative: [ADR-0004](decisions/0004-turn-and-attempt-lifecycle.md), [ADR-0005](decisions/0005-model-call-retry-semantics.md), and the live/startup boundary in [ADR-0031](decisions/0031-direct-fatal-terminalization.md); reconstitution: [ADR-0035](decisions/0035-domain-owned-persistence-reconstitution.md) | Idempotent startup scan and explicit durable transitions; no cancellation-only or classification-only replacement attempt and no reopening terminal operations. Matching typed `Lost` terminal values, complete fatal causes, and attempt-local predecessor checks: INV-034-tagged tests in [`crates/domain/src/turn_attempt.rs`](../crates/domain/src/turn_attempt.rs); startup derivation remains later work. Scan-before-scheduling startup ordering and the single-hub abandonment rule: [ADR-0010](decisions/0010-initial-scheduler-mechanics.md). |
| INV-035 | Provider credentials remain under hub control in the initial architecture and are not sent to clients or general-purpose runners. | Operational policy | Accepted; normative: ADR-0017 | Secret access boundary and adapter interfaces; sanitized logs and protocol schemas. Normative boundary: [ADR-0017](decisions/0017-credential-lifecycle.md) (credential access port keyed by non-secret references, reference-only storage and wire mappings, sanitized adapter errors and logs); the same boundary governs hub-local integration credentials as well as provider credentials, so no integration token reaches client or runner state either. Test-backed enforcement lands with the provider adapter slice. |

## Review rule

A feature proposal must cite the invariants it relies on and add a scenario when it introduces a new lifecycle edge. If an implementation cannot enforce an accepted invariant, the change must revise the design through an ADR rather than weakening the invariant implicitly.
