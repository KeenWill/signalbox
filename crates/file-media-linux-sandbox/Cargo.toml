[package]
name = "signalbox-file-media-linux-sandbox"
version = "0.0.0"
edition = "2024"
rust-version = "1.97.0"
license = "MIT"
repository = "https://github.com/KeenWill/signalbox"
publish = false

[workspace]

[dependencies]
libc = "0.2.186"

[lints.rust]
# This excluded crate is the separately governed Linux syscall boundary.
unsafe_code = "allow"
