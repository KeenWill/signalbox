load("//tooling/bazel:package.bzl", "rust_package")
load("//tooling/bazel:rust.bzl", "rust_library")
load("//tooling/bazel:testing.bzl", "rust_doc_test", "rust_integration_test", "rust_quality_tests", "rust_unit_test")

rust_package()

rust_library(
    name = "signalbox_conversation_import_codex",
    deps = [
        "//crates/application:signalbox_application",
        "//crates/conversation-import-json:signalbox_conversation_import_json",
        "//crates/domain:signalbox_domain",
    ],
)

rust_unit_test(
    name = "unit_tests",
    crate = ":signalbox_conversation_import_codex",
)

rust_doc_test(
    name = "doc_tests",
    crate = ":signalbox_conversation_import_codex",
)

rust_integration_test(
    name = "real_rollouts",
    srcs = glob(["tests/**/*.rs"]),
    args = ["--test-threads=4"],
    crate_root = "tests/real_rollouts.rs",
    data = glob(
        ["tests/**"],
        allow_empty = True,
        exclude = ["tests/**/*.rs"],
    ) + [],
    rustc_env = {"CARGO_MANIFEST_DIR": "crates/conversation-import-codex"},
    deps = [
        "//crates/application:signalbox_application",
        "//crates/conversation-import-codex:signalbox_conversation_import_codex",
        "//crates/conversation-import-json:signalbox_conversation_import_json",
        "//crates/domain:signalbox_domain",
    ],
)

rust_quality_tests(documented_crates = [":signalbox_conversation_import_codex"])
