DerivedData/
build/
.build/
xcuserdata/
*.xcuserstate
# Any DerivedData tree, whatever suffix a build script gives it. Enumerating
# these one at a time let `.derivedDataRelease/` slip past the list.
.derivedData*/
