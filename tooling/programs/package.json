{
  "name": "@signalbox/programs",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "check:fixtures": "bash check-fixtures.sh",
    "test": "node --test check-fixtures.test.mjs"
  },
  "devDependencies": {
    "typescript": "7.0.2"
  }
}
