# devenv's direnv integration: auto-loads the developer environment defined
# in devenv.nix. Requires direnv and the devenv CLI, plus a one-time
# `direnv allow` in this directory. See https://devenv.sh/integrations/direnv/.
eval "$(devenv direnvrc)"

use devenv
